install v1.11.2 — semantic handler port
========================================
Date: 2026-09-05
Prev origin/main HEAD: 7c1fc6c (Update README.md)

WHAT THIS RELEASE DOES

  Ports all 60 semantic handlers from install-1010.c (v1.10.5) into the
  v1.11.1 dispatch table framework. The strncasecmp if/else chain is
  replaced with switch(lookup_directive(token)). Same functionality,
  table-driven routing.

  Runs Clark's INSTALL.DAT end-to-end: project metadata, variables,
  control flow, file operations, interactive menu, system hooks,
  filesystem state, finish block — all through the 301-directive
  enum/lookup.

  Family API link target (.DEF + C0FL.OBJ) deferred to v1.11.3.

APPLY (Windows / GitHub Desktop) — 2 STEPS

  STEP 1 — Extract this zip INTO your repo root (overwrite in place).

    Modifies:
      APPLY.txt
      HISTORY.md                              (v1.11.2 entry added)
      CHECKSUMS.md5                           (regenerated)
      CHECKSUMS.sha256                        (regenerated)
      CHECKSUMS.txt                           (regenerated)
      pcb1541/install/src/install-1011.c      (semantic handlers ported)

    NO additions. NO deletions.

  STEP 2 — Open GitHub Desktop. Expected changes:

    M  APPLY.txt
    M  HISTORY.md
    M  CHECKSUMS.md5
    M  CHECKSUMS.sha256
    M  CHECKSUMS.txt
    M  pcb1541/install/src/install-1011.c

    Total: 6 modified, 0 added, 0 deleted.

  Commit + push. Suggested message:
    install v1.11.2: semantic handler port (60 handlers, table-driven dispatch)

ACCEPTANCE

  Verified in sandbox:
  - gcc -Wall -Wextra: 7 warnings (all pre-existing snprintf truncation
    from install-1010.c), zero errors
  - Ran against real INSTALL.DAT: processes all directives, runs through
    all 8 archives, handles @If/@Else/@Endif branches, @SetConfig/
    @SetAutoexec blocks, menu groups, and completes with installation
    summary. MISS warnings expected (redx not available in sandbox).
  - md5 of install-1011.c verified consistent across source/zip/checksums

--hexadecimal, install v1.11.2, 2026-09-05
