install v1.10.0 — INSTALL.EXE completion arc opens
====================================================
Date: 2026-09-05
Prev origin/main HEAD: 49525c1

SCOPE

  Opens the install v1.10 arc — completing our C reimplementation of
  Clark's INSTALL.EXE at pcb1541/install/src/install.c.

  This ship lands the reference material we need to diff against.
  No code changes to install.c yet — that starts in v1.10.1.

APPLY (Windows / GitHub Desktop) — 3 STEPS

  STEP 1 — Extract this release zip INTO your repo root.
    Adds pcb1541\install\reference\ (new dir with 3 files),
    adds docs\pcboard-internals\INSTALL-DAT-DIRECTIVES.md,
    updates HISTORY.md + APPLY.txt.

  STEP 2 — Open GitHub Desktop. Expected changes:
    M  APPLY.txt
    M  HISTORY.md                             (new sections + version labels)
    +  pcb1541/install/reference/README.md    (new)
    +  pcb1541/install/reference/INSTALL.DAT  (new, 42 KB — Clark's script)
    +  pcb1541/install/reference/INSTALL.EXE  (new, 338 KB — Clark's binary)
    +  docs/pcboard-internals/INSTALL-DAT-DIRECTIVES.md  (new, 60-directive catalog)

    Total: 2 modified, 4 added

  STEP 3 — Commit + push. Suggested message:
    install v1.10.0: reference material + directive catalog

WHAT'S IN THIS RELEASE

  pcb1541/install/reference/  (NEW dir)
    INSTALL.DAT   (42,294 B, md5 cca38d36)
       Clark's original installer script — the DAT-language program
       INSTALL.EXE interprets to lay down the 481 files of a PCBoard
       install. 72 unique @-directives (60 semantically distinct
       after case-folding EndIf/Endif + MkDir/Mkdir pairs).
       Extracted once from ../INSTALL.zip for stable diffing.

    INSTALL.EXE   (338,548 B, md5 5239767b, NE format)
       Clark's compiled 16-bit installer binary. Windows/OS2 Family
       API. Header string reveals the build source path:
         $Logfile: W:/master/install/main.c_v $
         INSTALL Ver ... Copyright (c) 1987-1995
       This is the disassembly parity target for install v1.10.6.

    README.md
       Reference-material provenance + use notes.

  docs/pcboard-internals/INSTALL-DAT-DIRECTIVES.md  (NEW)
    Canonical @-directive reference for INSTALL.DAT. Organized into
    10 categories. Every directive shows: use count in real INSTALL.DAT,
    first-line location, current implementation status, semantic purpose.
    Grown as v1.10.1 -> v1.10.5 lands each directive's implementation.

  HISTORY.md  (modified)
    Version-labels section: added install v1.8/1.9/1.10 subsystem
    numbering + pcbic v1.0 subsystem numbering.
    Timeline: added "pcbic v1.0.0" and "install v1.10" sections.
    Cumulative totals: bumped tracked-file count to 10,520.

  APPLY.txt  (rewritten — this file)

NEXT AFTER THIS PUSH

  install v1.10.1 — redx wire-up. First real code change to install.c:
   - @BeginLib / @EndLib / @File / @Files
   - @Copy / @Delete / @AppendTo / @FileAttr
   - Logic lifted from pcb1541/install/dist/target/rebuild_place.py
   - Acceptance: install.c against real INSTALL.DAT produces target/
     files matching pcb1541/install/dist/target/ md5s.

--hexadecimal & crew, install v1.10.0, 2026-09-05
