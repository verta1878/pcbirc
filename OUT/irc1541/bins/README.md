# OUT/irc1541/bins

Compiled **SDK example binaries** for this version — the small buildable
sample add-ons (a door, a monitor, a front-end/echo demo, plus any .PPE
examples) that prove the SDK builds end to end.

Separate from OUT/lib/ (the toolkit libraries). Empty until the SDK
example build scripts run. See todo/SDK-1541-IMPROVEMENTS.md.
