@CLS@@X0DEXECUTE DOS COMMAND

@X0EThis allows you to execute a DOS command without exiting to dos.

You can run most applications expect those that will cause you memory problems
or those that require keyboard interaction.

If you need either of the above, use the F5 key from local or the 9 key from
remote instead.







@X07
