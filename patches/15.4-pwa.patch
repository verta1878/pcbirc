diff -ruN pcb153/SOURCE/COMPILER/H2NAME.C pcb153/upd154/SOURCE/COMPILER/H2NAME.C
--- pcb153/SOURCE/COMPILER/H2NAME.C	2026-09-11 00:24:27.894803698 +0000
+++ pcb153/upd154/SOURCE/COMPILER/H2NAME.C	2026-09-11 00:24:27.970831507 +0000
@@ -53,7 +53,7 @@
   regs.h.ah = 0x52;      /* set up initial SFT pointer      */
   segread( &sregs );
   intdosx( &regs, &regs, &sregs );
-  ptr = *((unsigned far * far *) MK_FP( sregs.es, regs.x.bx + 4 ));
+  ptr = *((unsigned far * far *) MK_FP( sregs.es, regs.w.bx + 4 ));
 
   switch( _osmajor )     /* switch sizes, offsets for ver   */
     { case 2: sftsize = 0x28;
@@ -75,9 +75,9 @@
 
   if (htbl[h] >= 0)           /* now if handle is valid...  */
     { sftn = htbl[h];         /* get index into SFT list    */
-      while ( FP_OFF(ptr) != 0xFFFF )
+      while ( ((unsigned long)(ptr) & 0xFFFF) != 0xFFFF )
         { if (ptr[2] > sftn)  /* then target is here        */
-            { sptr = (unsigned char far *)&ptr[3];
+            { sptr = (char *)&ptr[3];
               while (sftn--)  /* so skip down to it         */
                 sptr += sftsize;
               _fmemcpy( name, &sptr[nmofs], 11 );
diff -ruN pcb153/SOURCE/COMPILER/SCOMP.CPP pcb153/upd154/SOURCE/COMPILER/SCOMP.CPP
--- pcb153/SOURCE/COMPILER/SCOMP.CPP	2026-09-11 00:24:27.894803698 +0000
+++ pcb153/upd154/SOURCE/COMPILER/SCOMP.CPP	2026-09-11 00:24:27.970831507 +0000
@@ -67,7 +67,7 @@
 
 extern unsigned _stklen = 16384U;
 
-void interrupt (* orig0x24) (...);
+void (__interrupt * orig0x24)();
 
 extern char warnFlag;
 extern char autoUVar;
@@ -140,14 +140,19 @@
 	getcountryspecs(1,0);
 
 	orig0x24 = getvect(0x24);
-	setvect(0x24,CEH);
+	setvect(0x24,(void (__interrupt *)()) CEH);
 
 	newline();
 	print("PPLC ");
 #ifdef PCB_DEMO
 	print("DEMO ");
 #endif
-	println("Version 3.30 - PCBoard Programming Language Compiler");
+	println("Version 3.40 - PCBoard Programming Language Compiler");  /* 15.4: bumped from 3.30 */
+	{
+	  static char PplcBetaVer[] = "15.4";
+	  static char PplcBetaFlag[] = "Beta";
+	  (void) PplcBetaVer; (void) PplcBetaFlag;
+	}
 	print("Copyright (C) 1993-95, Clark Development Company, Inc.");
 
 	if (argc < 2)
diff -ruN pcb153/SOURCE/DISPLAY/ANSI.C pcb153/upd154/SOURCE/DISPLAY/ANSI.C
--- pcb153/SOURCE/DISPLAY/ANSI.C	2026-09-11 00:24:27.894803698 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/ANSI.C	2026-09-11 00:24:27.970831507 +0000
@@ -138,6 +138,7 @@
 }
 
 
+#undef changeupdateinterval
 void LIBENTRY changeupdateinterval(int Interval) {
   if (Hidden)
     return;
diff -ruN pcb153/SOURCE/DISPLAY/DISPLAY.C pcb153/upd154/SOURCE/DISPLAY/DISPLAY.C
--- pcb153/SOURCE/DISPLAY/DISPLAY.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/DISPLAY.C	2026-09-11 00:24:27.971069106 +0000
@@ -158,8 +158,8 @@
 
   setdefaultpriority();
 
-/* create the semaphore the first time thru, but since we'll never close it, */
-/* don't bother creating it again by checking to see if it is opened. */
+  // create the semaphore the first time thru, but since we'll never close it,
+  // don't bother creating it again by checking to see if it is opened.
   if (! ActSemaphore.opened()) {
     if (! ActSemaphore.createunique("ACT")) {
       ThreadState = TH_EXITED;
@@ -230,7 +230,7 @@
 
   switch (Type) {
     case ACTSHOW :
-                   #ifndef __OS2__  /* the OS/2 uses the activity thread */
+                   #ifndef __OS2__  // the OS/2 uses the activity thread
                      #ifdef PCBCOMM
                        if (Status.TerseMode) {
                          /* don't display dots in terse mode, just get out */
@@ -696,7 +696,7 @@
   /* but under OS/2 it will create a lot of overhead!  A new way needs to be */
   /* discovered for temporarily stopping the keyboard timer.                 */
 
-  #ifndef __OS2__  /* I think this has been coded elsewhere now.... */
+  #ifndef __OS2__  // I think this has been coded elsewhere now....
     renewkbdtimer();
   #endif
 }
@@ -975,7 +975,7 @@
 
   #ifdef COMM
     if (! SkipRipDetect) {
-/*    printcom("\r[!\r   \r"); */
+//    printcom("\r[!\r   \r");
       printcom("[!   ");
 
       /* watching for RIPSCRIPxxyyvs */
@@ -1014,8 +1014,8 @@
           }
         }
 
-/* if we may have received a portion of the RIP sequence, then extend */
-/* the timer to wait for the rest of it to come in */
+        // if we may have received a portion of the RIP sequence, then extend
+        // the timer to wait for the rest of it to come in
         if (NeedToExtend && strchr(Buffer,'R') != NULL) {
           NeedToExtend = FALSE;
           settimer(8,TWOSECONDS);
diff -ruN pcb153/SOURCE/DISPLAY/FILES.C pcb153/upd154/SOURCE/DISPLAY/FILES.C
--- pcb153/SOURCE/DISPLAY/FILES.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/FILES.C	2026-09-11 00:24:27.971069106 +0000
@@ -19,13 +19,13 @@
   #include <direct.h>
   #include <borland.h>
 #else
-/*  #include <dir.h> */
+//  #include <dir.h>
 #endif
 
 #include "wild.h"
 
 #ifndef LIB
-/*  #include <account.h> */
+//  #include <account.h>
   #include "newscr.h"
   #include "menu.h"
 #endif
@@ -63,8 +63,8 @@
 
 extern struct ffblk DTA;  /* declared in exist.c */
 
-/* These two functions are used to maintain compatibility with pre-15.0 code */
-/* where the PWRD file had FEWER FIELDS than it does now. */
+// These two functions are used to maintain compatibility with pre-15.0 code
+// where the PWRD file had FEWER FIELDS than it does now.
 
 static bool _NEAR_ LIBENTRY yes(char *Str, bool Default) {
   if (Str == NULL || *Str == 0)
@@ -191,8 +191,8 @@
           if (! PcbData.IgnoreDropSecLevel) {
             if (Status.ActStatus == ACT_ENFORCE && Status.Balance <= 0) {
               if (Status.CurSecLevel != UsersData.Account.DropSecLevel) {
-/* temporarily set curseclevel, processuserrecord() will */
-/* reset this value again the normal way */
+                // temporarily set curseclevel, processuserrecord() will
+                // reset this value again the normal way
                 Status.CurSecLevel = UsersData.Account.DropSecLevel;
                 Status.UseDropSecLevel = TRUE;
                 dosfseek(&File,0,SEEK_SET);
@@ -242,20 +242,20 @@
 
   #ifdef PCB152
   {
-/*  long ActMinLeft; */
+//  long ActMinLeft;
 
-/*  // If the caller didn't have sufficient credits to use up one minute, */
-/*  // he'd be logged off.  But that prevents him from using up the REST of */
-/*  // his credits so PCBoard couldn't give him the drop-sec-level.  The */
-/*  // +1 below works around this by giving him one extra minute to use */
-/*  // up credits and, potentially, go negative ... but at *least* it will */
-/*  // run the account dry so that the drop-sec-level can be used */
-
-/*  if (Status.ActStatus == ACT_ENFORCE && AccountRates.ChargeForTime != 0) { */
-/*    ActMinLeft = (long) (Status.Balance / AccountRates.ChargeForTime) + 1; */
-/*    if (NewTime > ActMinLeft) */
-/*      NewTime = (int) ActMinLeft; */
-/*  } */
+//  // If the caller didn't have sufficient credits to use up one minute,
+//  // he'd be logged off.  But that prevents him from using up the REST of
+//  // his credits so PCBoard couldn't give him the drop-sec-level.  The
+//  // +1 below works around this by giving him one extra minute to use
+//  // up credits and, potentially, go negative ... but at *least* it will
+//  // run the account dry so that the drop-sec-level can be used
+
+//  if (Status.ActStatus == ACT_ENFORCE && AccountRates.ChargeForTime != 0) {
+//    ActMinLeft = (long) (Status.Balance / AccountRates.ChargeForTime) + 1;
+//    if (NewTime > ActMinLeft)
+//      NewTime = (int) ActMinLeft;
+//  }
   }
   #endif
 
@@ -485,8 +485,8 @@
     return(NULL);
 
   while (dosfgets(Buf,sizeof(Buf),&File) != -1) {
-    criteria(Buf,Criteria);  /* set up a wildcard matching criteria */
-    if (wildmatch(Name,Criteria)) {  /* does our filename match the criteria? */
+    criteria(Buf,Criteria);         // set up a wildcard matching criteria
+    if (wildmatch(Name,Criteria)) { // does our filename match the criteria?
       dosfclose(&File);
       return(Buf);
     }
@@ -609,8 +609,8 @@
     Status.FsecMultiplier = 1;
   #endif
 
-/* if someone is just uploading a file into the message editor or attaching */
-/* a file then skip the UPSEC check. */
+  // if someone is just uploading a file into the message editor or attaching
+  // a file then skip the UPSEC check.
 
   if (Status.EnteringMessage)
     return(0);
@@ -876,7 +876,7 @@
   char   Masks[ALT_TOTAL][20];
   char   Found[ALT_TOTAL][20];
   #ifdef __OS2__
-  int DirHandle = MAKEDIRHANDLE;  /*lint !e569 */
+  int DirHandle = MAKEDIRHANDLE;       //lint !e569
   #endif
 
   stripright(FileName,' ');
@@ -987,27 +987,27 @@
   First = ALT_TOTAL;
 
   while (1) {
-/* loop through each file that was found */
+    // loop through each file that was found
     for (Count = NumFilesFound, pblk = blk; Count > 0; Count--, pblk++) {
-/* then loop through each mask to find out which of the files we found */
-/* should actually be considered for display purposes */
+      // then loop through each mask to find out which of the files we found
+      // should actually be considered for display purposes
       for (X = 0; X < First; X++) {
-/* if the mask is non-blank then the filename that was found matches */
-/* the mask, then we may want to record it */
+        // if the mask is non-blank then the filename that was found matches
+        // the mask, then we may want to record it
         if (Masks[X][0] != 0 && stricmp(Masks[X],pblk->ff_name) == 0) {
-/* but first, check to see that the record size is okay... */
-/* if no record size was requested, then it's okay, otherwise check */
-/* the size of the file and make sure that the size of the file is */
-/* an even multiple of the record size */
+          // but first, check to see that the record size is okay...
+          // if no record size was requested, then it's okay, otherwise check
+          // the size of the file and make sure that the size of the file is
+          // an even multiple of the record size
           if (RecordSize == 0 || recordsizeokay(pblk->ff_fsize,RecordSize)) {
-/* finally, we're happy with this file that we found, so record */
-/* it in our Found[] array */
+            // finally, we're happy with this file that we found, so record
+            // it in our Found[] array
             strcpy(Found[X],pblk->ff_name);
-/* and if the array element we found is closer to the top of the */
-/* array than the previous, adjust our limits so that we don't */
-/* bother searching for matches that are further down the list */
-/* (i.e. the more generic ones are at the bottom, the more specific */
-/* ones are at the top, and we want to find the most specific) */
+            // and if the array element we found is closer to the top of the
+            // array than the previous, adjust our limits so that we don't
+            // bother searching for matches that are further down the list
+            // (i.e. the more generic ones are at the bottom, the more specific
+            // ones are at the top, and we want to find the most specific)
             if (X < First)
               First = X;
           }
@@ -1020,13 +1020,13 @@
       break;
 
     #ifndef __OS2__
-/* the DOS version needs to keep the blk parameter in order to find */
-/* the next file */
+      // the DOS version needs to keep the blk parameter in order to find
+      // the next file
       blk[0] = blk[NumFilesFound-1];
     #endif
 
-/* just in case there are more files to be found, go back and check for */
-/* more files right now and loop back up if there is more */
+    // just in case there are more files to be found, go back and check for
+    // more files right now and loop back up if there is more
     if (dosfindnext(blk,&NumFilesFound PDIRHANDLE2) == -1)
       break;
   }
@@ -1170,7 +1170,7 @@
   #endif
 
   #ifndef LIB
-/* make sure it is not one of our special files (users, users.inf, etc) */
+    // make sure it is not one of our special files (users, users.inf, etc)
     if (specialfile(FileName)) {
       char Str[80];
       writelog("Error: Attempted to display PCBoard file",SPACERIGHT);
@@ -1179,10 +1179,10 @@
       return(0);
     }
 
-/* Check to see if the file is already open and, if so, avoid going */
-/* recursive by getting out *now* instead of displaying the file again */
-/* solves the problem of a %FILESPEC that includes itself, or of a */
-/* !FILESPEC.PPE that displays the file that launched it. */
+    // Check to see if the file is already open and, if so, avoid going
+    // recursive by getting out *now* instead of displaying the file again
+    // solves the problem of a %FILESPEC that includes itself, or of a
+    // !FILESPEC.PPE that displays the file that launched it.
     if (fileisopen(FileName))
       return(0);
   #endif
@@ -1217,11 +1217,11 @@
 
   while ((Okay = dosfgets(Buffer,sizeof(Buffer)-1,&InFile)) != -1) {
 
-/* DISABLESUBFILES disables the display of any sub-file specifications */
-/* such as %filespec to display a file, !filespec to run a PPE, $filespec */
-/* to run a menu and also @hangup@ */
+    // DISABLESUBFILES disables the display of any sub-file specifications
+    // such as %filespec to display a file, !filespec to run a PPE, $filespec
+    // to run a menu and also @hangup@
     if ((Type & DISABLESUBFILES) != 0) {
-/* simply display the line */
+      // simply display the line
       printxlated(Buffer);
     } else {
       switch (Buffer[0]) {
diff -ruN pcb153/SOURCE/DISPLAY/PCBTEXT.C pcb153/upd154/SOURCE/DISPLAY/PCBTEXT.C
--- pcb153/SOURCE/DISPLAY/PCBTEXT.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/PCBTEXT.C	2026-09-11 00:24:27.971069106 +0000
@@ -338,7 +338,7 @@
     for (PcbTextBuffer = p, Num = 1; Num <= TXT_NUMPROMPTS; Num++) {
       PcbText[Num] = (pcbtexttype *) p;
       if (isset(UseText,Num) != 0)
-        p = strchr(p+1,0) + 1;  /*lint !e613 skip over color byte, search for NULL terminator, point to next character */
+        p = strchr(p+1,0) + 1;   //lint !e613 skip over color byte, search for NULL terminator, point to next character
     }
   }
 
@@ -579,11 +579,11 @@
         println("");
       }
 
-/* if we're about to run a PPE or an MNU, then we need to restore */
-/* the online status in case further interaction with the caller will */
-/* be required by the PPE or MNU */
+      // if we're about to run a PPE or an MNU, then we need to restore
+      // the online status in case further interaction with the caller will
+      // be required by the PPE or MNU
       if ((*p == '!' && *(p+1) != '|') || *p == '$') {
-        Asy.Online = SaveOnline;  /* restore true online status */
+        Asy.Online = SaveOnline;  // restore true online status
         redisplaystatusline();
       }
     }
diff -ruN pcb153/SOURCE/DISPLAY/SCREEN.C pcb153/upd154/SOURCE/DISPLAY/SCREEN.C
--- pcb153/SOURCE/DISPLAY/SCREEN.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/SCREEN.C	2026-09-11 00:24:27.971069106 +0000
@@ -224,7 +224,7 @@
     return;
 
   #ifdef __OS2__
-    ScrnBuff = (char *) 1;  /* use non-zero value to indicate screen is off */
+    ScrnBuff = (char *) 1;     // use non-zero value to indicate screen is off
     toggleoff(NULL);
   #else
     if ((ScrnBuff = (char _FAR_ *) farmalloc(4000)) == NULL)
@@ -296,7 +296,7 @@
   printcom(meganum((Y2-1) * FontY));
   printcom(Invert ? "1" : "0");
   printcom(Clear  ? "1" : "0");
-  printcom("00000");  /* reserved */
+  printcom("00000");  // reserved
   printcom(Text);
 #endif
 }
diff -ruN pcb153/SOURCE/DISPLAY/STATUS.C pcb153/upd154/SOURCE/DISPLAY/STATUS.C
--- pcb153/SOURCE/DISPLAY/STATUS.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/STATUS.C	2026-09-11 00:24:27.971069106 +0000
@@ -41,20 +41,20 @@
 #define STATUS_COLOR    BLACK + (LIGHTGRAY << 4)
 
 #ifdef __OS2__
-/* under OS/2, we don't have memory values on the status line so push the */
-/* max file handle number to the right 4 spaces */
+  // under OS/2, we don't have memory values on the status line so push the
+  // max file handle number to the right 4 spaces
   const int Column = 64;
 #else
   const int Column = 60;
 #endif
 
 #ifdef PCB153
-/* this function is used to limit the size of the comma delimited string to */
-/* just 12 characters, so even if the number is greater than 12 digits */
-/* including the characters) it can still be represented on screen using either */
-/* a 'K' or an 'M' (for i.e. 1K or 1M) as an abbreviation. */
-/*          10        20 */
-/* 1,345,789,123,567,901 */
+// this function is used to limit the size of the comma delimited string to
+// just 12 characters, so even if the number is greater than 12 digits
+// including the characters) it can still be represented on screen using either
+// a 'K' or an 'M' (for i.e. 1K or 1M) as an abbreviation.
+//          10        20
+// 1,345,789,123,567,901
 static char * _NEAR_ LIBENTRY dlimcomma(char *Str, double Num) {
   int  Len;
   char Temp[20];
@@ -198,7 +198,7 @@
                          sprintf(Str,"(%s) %s - %s",Tmp1,Status.DisplayName,usercity());
                          fastprint(Loc,Status.StatusLine1,Str,STATUS_COLOR);
                          #ifndef LIB
-/*                         fastprint(54,Status.StatusLine1,"ALT-H=Help",STATUS_COLOR); */
+//                         fastprint(54,Status.StatusLine1,"ALT-H=Help",STATUS_COLOR);
                            fastprint(59,Status.StatusLine1,"ALT-H=Help",STATUS_COLOR);
                          #else
                            fastprint(64-strlen(DoorName),Status.StatusLine1,DoorName,STATUS_COLOR);
diff -ruN pcb153/SOURCE/DISPLAY/XLATE.C pcb153/upd154/SOURCE/DISPLAY/XLATE.C
--- pcb153/SOURCE/DISPLAY/XLATE.C	2026-09-11 00:24:27.895224936 +0000
+++ pcb153/upd154/SOURCE/DISPLAY/XLATE.C	2026-09-11 00:24:27.971069106 +0000
@@ -180,26 +180,26 @@
   char  SrchStr[2];
   char  RepStr[6];
 
-/* make room for the @domain characters. */
+  // make room for the @domain characters.
   MaxLen -= (strlen(PcbData.uucpDomainName)+1);
 
-  strcpy(Dest,Srce);  /* copy current name */
-  strlwr(Dest);  /* change to lowercase letters */
-  substitute(Dest,"#" ,"#h#",MaxLen);  /* change # to #h# */
-  substitute(Dest,"." ,"#d#",MaxLen);  /* change . to #d# */
-  substitute(Dest,"_" ,"#u#",MaxLen);  /* change _ to #u# */
-  substitute(Dest,"(" ,"#l#",MaxLen);  /* change ( to #l# */
-  substitute(Dest,")" ,"#r#",MaxLen);  /* change ) to #r# */
-  substitute(Dest,"," ,"#m#",MaxLen);  /* change , to #m# */
-  substitute(Dest,":" ,"#c#",MaxLen);  /* change : to #c# */
-  substitute(Dest,"\\","#b#",MaxLen);  /* change \ to #b# */
-  substitute(Dest,"=" ,"#e#",MaxLen);  /* change = to #e# */
-  substitute(Dest,"/" ,"#s#",MaxLen);  /* change / to #s# */
-  substitute(Dest,">" ,"#o#",MaxLen);  /* change > to #o# */
-  substitute(Dest,"<" ,"#p#",MaxLen);  /* change < to #p# */
-  substitute(Dest,"*" ,"#i#",MaxLen);  /* change * to #i# */
+  strcpy(Dest,Srce);                        // copy current name
+  strlwr(Dest);                             // change to lowercase letters
+  substitute(Dest,"#" ,"#h#",MaxLen);       // change # to #h#
+  substitute(Dest,"." ,"#d#",MaxLen);       // change . to #d#
+  substitute(Dest,"_" ,"#u#",MaxLen);       // change _ to #u#
+  substitute(Dest,"(" ,"#l#",MaxLen);       // change ( to #l#
+  substitute(Dest,")" ,"#r#",MaxLen);       // change ) to #r#
+  substitute(Dest,"," ,"#m#",MaxLen);       // change , to #m#
+  substitute(Dest,":" ,"#c#",MaxLen);       // change : to #c#
+  substitute(Dest,"\\","#b#",MaxLen);       // change \ to #b#
+  substitute(Dest,"=" ,"#e#",MaxLen);       // change = to #e#
+  substitute(Dest,"/" ,"#s#",MaxLen);       // change / to #s#
+  substitute(Dest,">" ,"#o#",MaxLen);       // change > to #o#
+  substitute(Dest,"<" ,"#p#",MaxLen);       // change < to #p#
+  substitute(Dest,"*" ,"#i#",MaxLen);       // change * to #i#
 
-/* substitute high ascii characters */
+  // substitute high ascii characters
   for (p = Dest, CurLen = strlen(Dest); p < &Dest[CurLen]; p++) {
     if (*p > 127) {
       SrchStr[0] = *p;
@@ -210,9 +210,9 @@
     }
   }
 
-  change(Dest,' ',PcbData.uucpSeparator[0]);  /* change spaces to periods (or whatever is defined) */
-  addchar(Dest,'@');  /* add @ */
-  strcat(Dest,PcbData.uucpDomainName);  /* add domain name */
+  change(Dest,' ',PcbData.uucpSeparator[0]); // change spaces to periods (or whatever is defined)
+  addchar(Dest,'@');                         // add @
+  strcat(Dest,PcbData.uucpDomainName);       // add domain name
 }
 
 
diff -ruN pcb153/SOURCE/DOS/DOSTIME.C pcb153/upd154/SOURCE/DOS/DOSTIME.C
--- pcb153/SOURCE/DOS/DOSTIME.C	2026-09-11 00:24:27.897410487 +0000
+++ pcb153/upd154/SOURCE/DOS/DOSTIME.C	2026-09-11 00:24:27.972464269 +0000
@@ -61,7 +61,7 @@
 }
 
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
 static int StartTime[13];
 static int EndTime[13];
 
@@ -101,7 +101,7 @@
 
 this function is no longer used
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
   typedef union {
     long Whole;
     struct {
@@ -140,7 +140,7 @@
 
 long LIBENTRY dosdifflongtime(long t1, long t2) {
   long        Diff;
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     bool        Borrow;
     septimetype T1;
     septimetype T2;
@@ -149,7 +149,7 @@
   if (t1 < t2)
     t1 += 0x18000000L;  /* add 24 hours */
 
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     T1.Whole = t1;
     T2.Whole = t2;
 
@@ -279,7 +279,7 @@
   mydelay(550);
   t2 = dosgetlongtime();
   printf("t2 = %ld\nt1 = %ld\ndiff = %ld\n",t2,t1,dosdifflongtime(t2,t1));
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     septimetype T1,T2;
     T1.Whole = t1;
     T2.Whole = t2;
diff -ruN pcb153/SOURCE/FIDO/FIDOFUNC.C pcb153/upd154/SOURCE/FIDO/FIDOFUNC.C
--- pcb153/SOURCE/FIDO/FIDOFUNC.C	2026-09-11 00:24:27.897727973 +0000
+++ pcb153/upd154/SOURCE/FIDO/FIDOFUNC.C	2026-09-11 00:24:27.972708727 +0000
@@ -564,8 +564,6 @@
 			/// Note, this needs to be worked on
 			telink_block.crc = xmodem((char *)&telink_block.file_length,130 /*sizeof(telink_block)-3*/);
 
-			telink_block.crc	= _AH;
-			telink_block.crc = _AL;
 		 #endif
 	}
 	else
diff -ruN pcb153/SOURCE/FIDO/HILOW.C pcb153/upd154/SOURCE/FIDO/HILOW.C
--- pcb153/SOURCE/FIDO/HILOW.C	2026-09-11 00:24:27.897727973 +0000
+++ pcb153/upd154/SOURCE/FIDO/HILOW.C	2026-09-11 00:24:27.972708727 +0000
@@ -19,6 +19,7 @@
 #include <string.h>
 #include <stdlib.h>
 #include <ctype.h>
+#include "pcboard.h"
 
 void scanHiAscii(char * str,int strip);
 
@@ -52,7 +53,7 @@
     buf =  new char[strlen(str)*3];
     if(!buf)
     {
-      writelog("Could not allocate high ascii buffer."SPACERIGHT);
+      writelog("Could not allocate high ascii buffer.", SPACERIGHT);
       return;
     }
     buf[0] = '\x0';
diff -ruN pcb153/SOURCE/FIDO/OLDSEEN.C pcb153/upd154/SOURCE/FIDO/OLDSEEN.C
--- pcb153/SOURCE/FIDO/OLDSEEN.C	2026-09-11 00:24:27.897727973 +0000
+++ pcb153/upd154/SOURCE/FIDO/OLDSEEN.C	2026-09-11 00:24:27.972708727 +0000
@@ -1,3 +1,4 @@
+#include <stddef.h>
 /*!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!*/
 /* The source code in this module is proprietary software belonging to       */
 /* Clark Development Company and is part of the PCBoard source code library. */
diff -ruN pcb153/SOURCE/FIDO/PACKMSG.CPP pcb153/upd154/SOURCE/FIDO/PACKMSG.CPP
--- pcb153/SOURCE/FIDO/PACKMSG.CPP	2026-09-11 00:24:27.897727973 +0000
+++ pcb153/upd154/SOURCE/FIDO/PACKMSG.CPP	2026-09-11 00:24:27.972708727 +0000
@@ -12,6 +12,8 @@
 /*!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!*/
 
 
+#include <pcbtools.h>
+#include <stdio.h>
 #include <structs.h>
 
 DIRECTORIES        dirs;
@@ -21,6 +23,8 @@
       unsigned int filever = 0;
 const unsigned int curver = 3;
 
+extern pcbdattype PcbData;
+void packmsg_init(void) {
 sprintf(cfgfilename,"%sPCBFIDO.CFG",PcbData.FidoLoc);
 
 if(fileexist(cfgfilename) != 255 && dosfopen(cfgfilename,OPEN_READ | OPEN_DENYNONE,&dirfile) == 0)
@@ -29,13 +33,9 @@
   if(filever == curver)
   {
       dosfread(&dirs,sizeof(dirs),&dirfile);
-      maxstrcpy(msgdir,dirs.outgoing_msg);
+      maxstrcpy(msgdir,dirs.outgoing_msg,sizeof(msgdir));
   }
   //else                 report error
   dosfclose(&dirfile);
 }
-
-
-
-
-
+}
diff -ruN pcb153/SOURCE/FIDO/SDR.CPP pcb153/upd154/SOURCE/FIDO/SDR.CPP
--- pcb153/SOURCE/FIDO/SDR.CPP	2026-09-11 00:24:27.900526354 +0000
+++ pcb153/upd154/SOURCE/FIDO/SDR.CPP	2026-09-11 00:24:27.972708727 +0000
@@ -12,11 +12,12 @@
 /*!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!*/
 
 
-#include    <dir.h> #include    <stdio.h> #include    <string.h>
+#include    <dir.h>
+#include    <stdio.h>
+#include    <string.h>
 
-#ifdef LIB
+#include    <borland.h>
 #include    <pcbtools.h>
-#endif
 
 char * pascal makeUniqueFZipName ( char * name )
 {
diff -ruN pcb153/SOURCE/FIDO/TOSSMISC.C pcb153/upd154/SOURCE/FIDO/TOSSMISC.C
--- pcb153/SOURCE/FIDO/TOSSMISC.C	2026-09-11 00:24:27.900526354 +0000
+++ pcb153/upd154/SOURCE/FIDO/TOSSMISC.C	2026-09-11 00:24:27.972708727 +0000
@@ -1240,6 +1240,7 @@
   char	   attchfilename[200];
   maxstrcpy(attchfilename,directory_info.outgoing_packets,sizeof(attchfilename));
   strcat(attchfilename,afixresfile);
+    extern char *mktemp(char *);
   mktemp(attchfilename);
   char * t = strrchr(attchfilename,'\\');
   if(t != NULL) maxstrcpy(afixresfile,t,sizeof(afixresfile)); // Now copy just the unique file name back into it's buffer complete with mangled name
diff -ruN pcb153/SOURCE/H/BUG.H pcb153/upd154/SOURCE/H/BUG.H
--- pcb153/SOURCE/H/BUG.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/BUG.H	2026-09-11 00:24:27.978481742 +0000
@@ -34,7 +34,7 @@
 *****************************************************************************/
 
 #ifdef __OS2__
-/*#define timerexpired(n) (gettimer(n) <= 0) */
+//#define timerexpired(n) (gettimer(n) <= 0)
 #else
   #ifdef CPU386
     extern long BC386BUG;
diff -ruN pcb153/SOURCE/H/CLEARBUF.HPP pcb153/upd154/SOURCE/H/CLEARBUF.HPP
--- pcb153/SOURCE/H/CLEARBUF.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/CLEARBUF.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -36,7 +36,7 @@
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include    <mem.h>
 
@@ -44,7 +44,7 @@
 
 /******************************************************************************/
 
-/* Clear Buffer Functions */
+// Clear Buffer Functions
 
 template <class T>
 inline void clearBuffer ( T & t )
@@ -59,5 +59,5 @@
 
 /******************************************************************************/
 
-#endif  /* ___CLEARBUF_HPP___ */
+#endif // ___CLEARBUF_HPP___
 
diff -ruN pcb153/SOURCE/H/DATA.HPP pcb153/upd154/SOURCE/H/DATA.HPP
--- pcb153/SOURCE/H/DATA.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/DATA.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -31,8 +31,8 @@
 #ifdef PCBSETUP
 #define writeFidolog(a,b)
 #endif
-/* headers needed for data type in this header */
-/* PCBoard headers */
+// headers needed for data type in this header
+// PCBoard headers
 #ifndef PCBSETUP
 #include "pcboard.h"
 #include "misc.h"
@@ -64,7 +64,7 @@
 				bool		removeFile		 (void);
 				void		packFile		 (void);
 				uint		getVersion		 (void){return curver;}
-/*		virtual   bool		  isDupe		 (const T & rec); */
+//		virtual   bool		  isDupe		 (const T & rec);
 				void		open			 (void){if(!f.isOpen() && f.open(datafile,OPEN_RDWR| OPEN_DENYNONE) != 0) err = TRUE;}
 				void		close			 (void){if(f.isOpen()) f.close();}
 				bool		lockHdr 		 (void){ return( f.lockChk(0,sizeof(filever)) == 0 );}
@@ -79,14 +79,14 @@
 	 cDOSFILE		 f;
 
 	private:
-/* Data */
+	  // Data
 	  char			  datafile[MAXFLEN];
 	  bool			  openedbyme,err,needtopack;
 	  uint			  filever;
 	  uint			  curver;
 	  uint			  recsize,totrecs;
 
-/* Functions */
+	  // Functions
 				bool		createFile		 (void);
 				uint		getFileVer		 (void);
 				long		seekToRec		 (unsigned int recnum);
@@ -98,21 +98,21 @@
 template<class T>
 cCONFIGBASE<T>::cCONFIGBASE(const char * filename, const bool path)
 {
-/* Generic initializations */
+	// Generic initializations
 	openedbyme = needtopack = FALSE;
 	recsize = totrecs = curver = filever = err = 0;
 	memset(datafile,'Z',sizeof(datafile));
 
-/* The current file version is the same for all derived objects. This should */
-/* Be updated when the file format changes */
+	// The current file version is the same for all derived objects. This should
+	// Be updated when the file format changes
 	curver = 3;
 
-/* Record size is the size of the template object */
+	// Record size is the size of the template object
 	recsize = sizeof(T);
 
 	if(!path)
 	{
-/* Build datafile name using filaname passed as parameter */
+	  // Build datafile name using filaname passed as parameter
 	  maxstrcpy(datafile,PcbData.FidoLoc,sizeof(datafile));
 	  char * t = findstartofname(datafile);
 	  if(t) *t = '\x0';
@@ -121,7 +121,7 @@
 	else
 	  maxstrcpy(datafile,(char *)filename,sizeof(datafile));
 
-/* Now attempt to open the file if it is not already open */
+	// Now attempt to open the file if it is not already open
 	if(fileexist(datafile) == 255) createFile();
 	if(!f.isOpen() && f.open(datafile,OPEN_RDWR | OPEN_DENYNONE ) == 0)
 	{
@@ -133,7 +133,7 @@
 	else err = TRUE;
 
 
-/* Check the datfile's version number and compare against current version number */
+	// Check the datfile's version number and compare against current version number
 	if(!err && getFileVer() != curver)
 	{
 	char logBuf[70];
@@ -168,7 +168,7 @@
 {
 	filever = 0;
 
-/* If an error hasn't occured yet seek the file begin and read file version */
+	// If an error hasn't occured yet seek the file begin and read file version
 	if(!err)
 	{
 	  f.seek(0,SEEK_SET);
@@ -178,7 +178,7 @@
 		 err = TRUE;
 	  }
 	}
-/* Returns 0 if there was an error */
+	// Returns 0 if there was an error
 	return filever;
 }
 
@@ -189,11 +189,11 @@
 
 	if(f.isOpen()) f.close();
 	if(f.open(datafile,OPEN_RDWR | OPEN_CREATE | OPEN_DENYNONE)!= 0) err = TRUE;
-/*f.lockChk(0L,2L); */
+	//f.lockChk(0L,2L);
 	f.write(&curver,sizeof(curver));
-/*f.unlock(0L,2L); */
+	//f.unlock(0L,2L);
 
-/*f.close(); */
+	//f.close();
 	return err;
 }
 template< class T >
@@ -227,12 +227,12 @@
 	memset(&mtyrec,0,sizeof(mtyrec));
 	memset(&rec,0,sizeof(rec));
 
-/* Build a temporary file to copy valid records into */
+	// Build a temporary file to copy valid records into
 	tfile.setName(f.getName());
 	tfile.setExt(".TMP");
 
 	tfile.unlink();
-/* Open temporary file and start copying valid records into it */
+	// Open temporary file and start copying valid records into it
 	if(tfile.openChk(OPEN_RDWR | OPEN_CREATE | OPEN_DENYRDWR) != 0) return;
 
 	f.close();
@@ -255,7 +255,7 @@
 	  tfile.write(&rec,sizeof(rec));
 	}
 
-/* Now rename the temp file to the datafile */
+	// Now rename the temp file to the datafile
 	  tfile.close();
 	  f.close();
 	  if(!err)
@@ -292,11 +292,11 @@
 template< class T >
 bool cCONFIGBASE<T>::modifyRec(T & rec,unsigned int recnum)
 {
-/* Seek to the record number and write out the new record */
+	// Seek to the record number and write out the new record
 	if(err == TRUE) return FALSE;
 	seekToRec(recnum);
 	if(f.write(&rec,sizeof(rec)) != sizeof(rec)) err = TRUE;
-	return (! (err == TRUE) );  /* Return FALSE if err == TRUE */
+	return (! (err == TRUE) );		   // Return FALSE if err == TRUE
 }
 
 
@@ -310,7 +310,7 @@
 	if(f.write(&mtyrec,sizeof(mtyrec)) != sizeof(mtyrec)) err = TRUE;
 	f.flush();
 	if(!err) needtopack = TRUE;
-	return (! (err == TRUE) );  /* Return FALSE if err == TRUE */
+	return (! (err == TRUE) );		   // Return FALSE if err == TRUE
 }
 
 /*
@@ -343,7 +343,7 @@
 	f.seek(0,SEEK_END);
 	if(f.write((void *)&rec,sizeof(rec)) != sizeof(rec)) err = TRUE;
 	unlockHdr();
-	return (! (err == TRUE) );  /* Return FALSE if err == TRUE */
+	return (! (err == TRUE) );		   // Return FALSE if err == TRUE
 }
 
 class cAREAINDEX : public cCONFIGBASE<AINDEX>
@@ -359,9 +359,9 @@
 
   private:
 	cAREAINDEX	index;
-	AINDEX *	irecs;  /* Pointer to keep conference index in memory */
-	bool		irecsinmem;  /* Is the index durrently in memory */
-/* bool 	   err; 	 PC-Lint reported that this hides the err defined in cCONFIGBASE */
+	AINDEX *	irecs;			// Pointer to keep conference index in memory
+	bool		irecsinmem; 	// Is the index durrently in memory
+	// bool 	   err; 	 PC-Lint reported that this hides the err defined in cCONFIGBASE
   public:
 				 cAREAS 		  (void) : cCONFIGBASE<NAREA_STRUCT>("AREAS.DAT",FALSE){irecs=NULL;irecsinmem=FALSE;Err(FALSE);}
 				~cAREAS 		  (void){delete(irecs);}
@@ -456,7 +456,7 @@
 {
   public:
 				 cFREQPATHS(void) : cCONFIGBASE<NFREQ_PATH>("FREQPATH.DAT",FALSE){};
-/*~cFREQPATHS(void){}; */
+				//~cFREQPATHS(void){};
 unsigned int	 findRec(const char * path);
 
   protected:
@@ -475,7 +475,7 @@
   private:
 };
 
-#endif  /* ifndef PCBFU */
+#endif // ifndef PCBFU
 
 class cFREQDENY : public cCONFIGBASE<NADDRESS>
 {
@@ -487,7 +487,7 @@
   private:
 };
 
-/*#ifndef PCBSETUP */
+//#ifndef PCBSETUP
 class cSEND : public cCONFIGBASE<FSEND>
 {
   public:
@@ -495,7 +495,7 @@
 	unsigned int findRec	   (const char * entry);
 	void		 process	   (void);
 };
-/*#endif */
+//#endif
 
 class cCONFIG
 {
diff -ruN pcb153/SOURCE/H/DBASE.HPP pcb153/upd154/SOURCE/H/DBASE.HPP
--- pcb153/SOURCE/H/DBASE.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/DBASE.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -36,7 +36,7 @@
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #ifdef __OS2__
   #pragma pack(1)
@@ -54,7 +54,7 @@
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 template <class T, class U>
 class cINFSTRUCT
@@ -329,7 +329,7 @@
 
 /******************************************************************************/
 
-/* cINFSTRUCT Template Class Member Functions */
+// cINFSTRUCT Template Class Member Functions
 
 template <class T, class U>
 cINFSTRUCT<T,U>::cINFSTRUCT(void) :
diff -ruN pcb153/SOURCE/H/DEFINES.H pcb153/upd154/SOURCE/H/DEFINES.H
--- pcb153/SOURCE/H/DEFINES.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/DEFINES.H	2026-09-11 00:24:27.978481742 +0000
@@ -76,7 +76,7 @@
 #define         ABORT_STRING           ""
 #define         LINE_LEN                79
 #define         HDR_SIZE                9
-#define         MNU_TOPX                22  /* For FIDO display menus */
+#define         MNU_TOPX                22          // For FIDO display menus
 #define         MNU_TOPY                5
 #define         MNU_BOTX                57
 #define         MNU_BOTY                19
@@ -107,11 +107,11 @@
 #define     CONFIG_FILE         PcbData.FidoConfig
 #define     WORK_DIR            directory_info.work_directory
 #define     IN_DIR              directory_info.incoming_packets
-/*#define     QUEUE_FILE          "FIDOQUE.DAT"       /* Name of the Queue    */ */
-/*#define     MASTER_QUEUE_FILE   "MASTER.DAT" */
+//#define     QUEUE_FILE          "FIDOQUE.DAT"       /* Name of the Queue    */
+//#define     MASTER_QUEUE_FILE   "MASTER.DAT"
 #define     LOGFILE             "FIDOLOG.IN"        /* log file             */
 #define     INCOMING_MASK       "*.PKT"             /* Packet files         */
-/*#define     QUEUE_FILE          "FIDOQUE.DAT" */
+//#define     QUEUE_FILE          "FIDOQUE.DAT"
 #define     OUTBOUND_FILE       "PCBDSZ.LST"
 #define     ZM_SEND             "ZMSEND.EXE"
 #define     ZM_RECV             "ZMRECV.EXE"
@@ -305,8 +305,8 @@
 #define AFIXRESPONSE 6
 #define AFIXFORWARD  7
 
-/* Maximum file.directory length */
-#define MAXFLEN      66  /* 66 from the DOS version of Borland MAXDIR */
+// Maximum file.directory length
+#define MAXFLEN      66           // 66 from the DOS version of Borland MAXDIR
 
 #define SEENBYS      1
 #define PRESENT      2
diff -ruN pcb153/SOURCE/H/DOSCLASS.HPP pcb153/upd154/SOURCE/H/DOSCLASS.HPP
--- pcb153/SOURCE/H/DOSCLASS.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/DOSCLASS.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,7 +37,7 @@
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include    <stdio.h>
 #include    <stdlib.h>
@@ -60,7 +60,7 @@
 
 /******************************************************************************/
 
-/* Constants for End Of Line Markers */
+// Constants for End Of Line Markers
 
 extern const char endOfLineStr  [];
 extern const char endOfULineStr [];
@@ -68,33 +68,33 @@
 
 /******************************************************************************/
 
-/* Pure Virtual Base File Class */
+// Pure Virtual Base File Class
 
 class cFILEBASE
 {
 
     public:
 
-/* Constructor to make sure the name is initialized to empty */
+        // Constructor to make sure the name is initialized to empty
         cFILEBASE ( void ) { _name[0] = NUL; }
 
-/* Virtual destructor to ensure that derived destructors are called */
+        // Virtual destructor to ensure that derived destructors are called
         virtual ~cFILEBASE ( void ) { }
 
-/* SET / GET / make a TeMPorary name for the file */
+        // SET / GET / make a TeMPorary name for the file
               void   setName ( const char * name ) { close();
                                                      if (name)
                                                        strcpy(_name,name); }
         const char * getName ( void )              { return _name; }
         const char * tmpName ( const char * path );
 
-/* NOTE: tmpName hasn't been implemented yet, just stubbed in */
-/*       Intended functionality is that a path (C:\PCB\) is passed */
-/*       in and a random name (XXXXXXXX.XXX) is appended to the */
-/*       specified path. */
+        // NOTE: tmpName hasn't been implemented yet, just stubbed in
+        //       Intended functionality is that a path (C:\PCB\) is passed
+        //       in and a random name (XXXXXXXX.XXX) is appended to the
+        //       specified path.
 
-/* Functions to fully qualify a path name, make/split the name, */
-/* and set/get parts of the name */
+        // Functions to fully qualify a path name, make/split the name,
+        // and set/get parts of the name
 
         const char * fullName ( void )
             { char _tmpname [ _MAX_PATH ];
@@ -127,17 +127,17 @@
         char * getFName ( char * p ) { getParts(0,0,p,0); return p; }
         char * getExt   ( char * p ) { getParts(0,0,0,p); return p; }
 
-/* Miscellaneous functions to unlink, rename, move, copy, seek, etc */
+        // Miscellaneous functions to unlink, rename, move, copy, seek, etc
         int unlink ( void )              { close();
                                            return ::unlink(getName()); }
         int rename ( const char * name ) { close();
                                            int rv = ::rename(getName(),name);
                                            if (rv == 0) setName(name);
                                            return rv; }
-/* move? copy? */
+        // move? copy?
 
-/* Functions to seek to a specified character, implicit SEEK_SET, */
-/* seek followed by read/write, etc. */
+        // Functions to seek to a specified character, implicit SEEK_SET,
+        // seek followed by read/write, etc.
 
         int seek ( char c );
 
@@ -153,12 +153,12 @@
         int seekWrite ( long dist, const void * buf, unsigned size )
             { return seekWrite(dist,SEEK_SET,buf,size); }
 
-/* Quick & Dirty access to get/set the absolute file pointer position */
+        // Quick & Dirty access to get/set the absolute file pointer position
         long pos ( void )   { return seek(0,SEEK_CUR); }
         void pos ( long p ) {        seek(p,SEEK_SET); }
 
-/* Append/Create/Open a file (optionally auto error checking) using */
-/* an existing file name */
+        // Append/Create/Open a file (optionally auto error checking) using
+        // an existing file name
         int append    ( int flags ) { close();
                                       return _append(_name,flags); }
         int appendChk ( int flags ) { close();
@@ -174,7 +174,7 @@
         int openChk   ( int flags ) { close();
                                       return _openChk(_name,flags); }
 
-/* Append/Create/Open a file (optionally auto error checking) */
+        // Append/Create/Open a file (optionally auto error checking)
         int append    ( const char * name,
                               int    flags ) { setName(name);
                                                return append(flags); }
@@ -196,13 +196,13 @@
                               int    flags ) { setName(name);
                                                return openChk(flags); }
 
-/* Close a file if it is open */
+        // Close a file if it is open
         int close ( void ) { return (isOpen() ? _close() : 0); }
 
-/* Routine to output a single character */
+        // Routine to output a single character
         int putch ( char c ) { return writeChk(&c,sizeof(c)); }
 
-/* Functions for line oriented input/output */
+        // Functions for line oriented input/output
         virtual int getln   (       char * str, unsigned size );
         virtual int getuln  (       char * str, unsigned size );
         virtual int getnln  (       char * str, unsigned size );
@@ -221,15 +221,15 @@
                                                               rv = putch(NUL);
                                                           return rv; }
 
-/* Functions for formatted input/output */
+        // Functions for formatted input/output
         int scanf  ( const char * fmt, ... );
         int printf ( const char * fmt, ... );
 
-/* Functions to write a given character a specified number of times */
+        // Functions to write a given character a specified number of times
         int write    ( const char c, unsigned size );
         int writeChk ( const char c, unsigned size );
 
-/* These are all pure virtual and must be defined in derived classes */
+        // These are all pure virtual and must be defined in derived classes
         virtual int  commit   (       void )                         = 0;
         virtual int  flush    (       void )                         = 0;
         virtual bool isOpen   (       void )                         = 0;
@@ -246,7 +246,7 @@
         virtual int  write    ( const void   * buf,  unsigned size ) = 0;
         virtual int  writeChk ( const void   * buf,  unsigned size ) = 0;
 
-/* These are to maintain backward compatibility */
+        // These are to maintain backward compatibility
         int appendchk ( const char   * name,
                               int      flags ) { return appendChk(name,
                                                    flags); }
@@ -265,9 +265,9 @@
 
     protected:
 
-/* These are all pure virtual and must be defined in derived classes */
-/* They are protected because they should not be used directly, but */
-/* only by members of this and derived classes */
+        // These are all pure virtual and must be defined in derived classes
+        // They are protected because they should not be used directly, but
+        // only by members of this and derived classes
         virtual int _append    ( const char * name, int flags )           = 0;
         virtual int _appendChk ( const char * name, int flags )           = 0;
         virtual int _close     ( void )                                   = 0;
@@ -276,18 +276,18 @@
         virtual int _open      ( const char * name, int flags )           = 0;
         virtual int _openChk   ( const char * name, int flags )           = 0;
 
-/* Function to read a line of text using a supplied end of line marker */
+        // Function to read a line of text using a supplied end of line marker
         int _getline ( char * str, unsigned size, const char * eol );
 
     private:
 
-        char _name [ _MAX_PATH ];  /* _MAX_PATH (among others) from stdlib.h */
+        char _name [ _MAX_PATH ]; // _MAX_PATH (among others) from stdlib.h
 
 };
 
 /******************************************************************************/
 
-/* Derived Classes for Handle and Stream Based File Access */
+// Derived Classes for Handle and Stream Based File Access
 
 class cFILESTREAM;
 
@@ -403,7 +403,7 @@
 
 /******************************************************************************/
 
-/* Aliases to maintain backward compatibility */
+// Aliases to maintain backward compatibility
 
 typedef cFILE       cDOS;
 typedef cFILESTREAM cDOSFILE;
diff -ruN pcb153/SOURCE/H/DUPECHEC.HPP pcb153/upd154/SOURCE/H/DUPECHEC.HPP
--- pcb153/SOURCE/H/DUPECHEC.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/DUPECHEC.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -39,8 +39,8 @@
 {
 
   public:
-/*                    LIBENTRY      cDUPECHECK        (void){} */
-/*                    LIBENTRY      ~cDUPECHECK       (void){} */
+//                    LIBENTRY      cDUPECHECK        (void){}
+//                    LIBENTRY      ~cDUPECHECK       (void){}
 
   bool              LIBENTRY      checkPath         (const char * msg, uint orgzone);
   bool              LIBENTRY      checkID           (const char * msg,pcbconftype * conf);
diff -ruN pcb153/SOURCE/H/EVENT.H pcb153/upd154/SOURCE/H/EVENT.H
--- pcb153/SOURCE/H/EVENT.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/EVENT.H	2026-09-11 00:24:27.978481742 +0000
@@ -39,15 +39,15 @@
 #ifndef EVENT_H
 #define EVENT_H
 
-#define MAX_EVENTS      64  /* This is _THE_ maximum number of events */
-/* MaxEvents is a variable which is used */
-/* instead of MAX_EVENTS when reading in */
-/* the file because it might not be 64 */
-
-#ifdef  PCB_MAXNODES  /* test to see if compiling PCBoard */
-#define MAXNODES        PCB_MAXNODES  /* MAXNODES is a constant */
-#else  /* nope, compiling PCBSetup instead */
-#define MAXNODES        MaxNodes  /* MAXNODES is a variable in PCBSetup */
+#define MAX_EVENTS      64            // This is _THE_ maximum number of events
+                                      // MaxEvents is a variable which is used
+                                      // instead of MAX_EVENTS when reading in
+                                      // the file because it might not be 64
+
+#ifdef  PCB_MAXNODES                  // test to see if compiling PCBoard
+#define MAXNODES        PCB_MAXNODES  // MAXNODES is a constant
+#else                                 // nope, compiling PCBSetup instead
+#define MAXNODES        MaxNodes      // MAXNODES is a variable in PCBSetup
 #endif
 
 
@@ -64,29 +64,29 @@
 #define TOTALEVENTSIZE       (LASTDATEOFFSET + LASTDATESIZE)
 
 
-/* This is the offset of the Last Date Record ... When accessing an Array of */
-/* Last Date values, use it directly.  When accessing the disk, it needs to be */
-/* multiplied by the size of the Last Date record which is sizeof(unsigned) */
- /* */
-/* NOTE:  EventNum is a 0-based number (i.e. the first event is #0) */
-/*        However, Node Number is a 1-based number (i.e. node #1 is 1) */
+// This is the offset of the Last Date Record ... When accessing an Array of
+// Last Date values, use it directly.  When accessing the disk, it needs to be
+// multiplied by the size of the Last Date record which is sizeof(unsigned)
+//
+// NOTE:  EventNum is a 0-based number (i.e. the first event is #0)
+//        However, Node Number is a 1-based number (i.e. node #1 is 1)
 
 #define LASTDATERECPOS(EventNum,NodeNum)  (((EventNum) * (long) MAXNODES) + ((long) (NodeNum) - 1))
 
 
-/* This is the offset of the Event Record on disk. */
- /* */
-/* NOTE: EventNum is a 0-based number (i.e. the first event is #0) */
+// This is the offset of the Event Record on disk.
+//
+// NOTE: EventNum is a 0-based number (i.e. the first event is #0)
 
 #define EVENTRECPOS(EventNum)   ((long) (EventNum) * sizeof(EventType))
 
 
-/* This macro calculates the EXACT date by multiplying a julian date value */
-/* by the number of minutes in a day and then adding the hour and minute values */
-/* to it. */
- /* */
-/* NOTE:  For some reason Louis subtracted one from the julian date, I don't */
-/*        no why, but I've copied it into the macro verbatim */
+// This macro calculates the EXACT date by multiplying a julian date value
+// by the number of minutes in a day and then adding the hour and minute values
+// to it.
+//
+// NOTE:  For some reason Louis subtracted one from the julian date, I don't
+//        no why, but I've copied it into the macro verbatim
 
 #define MINUTESPERHOUR    60
 #define HOURSPERDAY       24
diff -ruN pcb153/SOURCE/H/EXEC.HPP pcb153/upd154/SOURCE/H/EXEC.HPP
--- pcb153/SOURCE/H/EXEC.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/EXEC.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -32,11 +32,11 @@
 /******************************************************************************/
 
 
-/* Defined Macros */
+// Defined Macros
 
 #ifdef ERRBRK
 
-/*#define EVAL(a,b,c)            {b = evalarg(); c(b); a = *b;} */
+//#define EVAL(a,b,c)            {b = evalarg(); c(b); a = *b;}
 #define EVAL(a,b,c)         { (a = *evalArg()); }
 
 #define nullErrReturn(a)  if (a == NULL) { /*err = TRUE;*/ return; }
@@ -73,7 +73,7 @@
 #define MAX_COLS			80
 #define MAX_ROWS			(Scrn_BottomRow+1)
 
-/* Macro to ensure that v is in the range l through h inclusive */
+// Macro to ensure that v is in the range l through h inclusive
 #define inRange(v,l,h) if (v < l) v = l; else if (v > h) v = h
 
 #define HIWORD(L) ((unsigned int)(((unsigned long)(L) >> 16) & 0xFFFF))
diff -ruN pcb153/SOURCE/H/FCONFIG.H pcb153/upd154/SOURCE/H/FCONFIG.H
--- pcb153/SOURCE/H/FCONFIG.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/FCONFIG.H	2026-09-11 00:24:27.978481742 +0000
@@ -21,17 +21,17 @@
 #define     LIST_RECS     16
 #define     FREQ_RECS     32
 
-/*#ifdef __cplusplus */
-/*extern "C" { */
-/*#endif */
+//#ifdef __cplusplus
+//extern "C" {
+//#endif
 
 extern bool pascal read_fido_config(int flags);
 extern bool pascal write_fido_config(void);
 extern bool pascal make_fido_config(void);
 extern void pascal free_fido_memory(void);
 
-/*#ifdef __cplusplus */
-/*} */
-/*#endif */
+//#ifdef __cplusplus
+//}
+//#endif
 
 #endif
diff -ruN pcb153/SOURCE/H/FIDOMSG.HPP pcb153/upd154/SOURCE/H/FIDOMSG.HPP
--- pcb153/SOURCE/H/FIDOMSG.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/FIDOMSG.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -25,8 +25,8 @@
 class cFIDOMSG
 {
 public:
-/*             cFIDOMSG(); */
-/*            ~cFIDOMSG(){message.freeMsgBuf();} */
+//             cFIDOMSG();
+//            ~cFIDOMSG(){message.freeMsgBuf();}
 bool         MsgtoFile(long msgNum);
 bool         FiletoMsg(const char * filename);
 bool         fillMSGHdr(msgheadertype & Hdr);
diff -ruN pcb153/SOURCE/H/LABEL.HPP pcb153/upd154/SOURCE/H/LABEL.HPP
--- pcb153/SOURCE/H/LABEL.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/LABEL.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,11 +37,11 @@
 
 /****************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /****************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #ifdef __BORLANDC__
   #include  <alloc.h>      
@@ -55,11 +55,11 @@
 
 /****************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 /****************************************************************************/
 
-/* Types */
+// Types
 
 class cLABEL
 {
@@ -95,19 +95,19 @@
 
 /****************************************************************************/
 
-/* Constants */
+// Constants
 
 /****************************************************************************/
 
-/* Variables */
+// Variables
 
 /****************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 /****************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /****************************************************************************/
 
diff -ruN pcb153/SOURCE/H/LIST.HPP pcb153/upd154/SOURCE/H/LIST.HPP
--- pcb153/SOURCE/H/LIST.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/LIST.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,18 +37,18 @@
 
 /******************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include    <stddef.h>
 #include    "types.hpp"
 
 /******************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 #define DD_NO       0
 #define DD_DEF      1
@@ -58,7 +58,7 @@
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 template <class T>
 class cNODE
@@ -144,23 +144,23 @@
 
 /******************************************************************************/
 
-/* Constants */
+// Constants
 
 /******************************************************************************/
 
-/* Variables */
+// Variables
 
 /******************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 /******************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /******************************************************************************/
 
-/* Template Functions */
+// Template Functions
 
 template <class T>
 cLIST<T>::cLIST(void) : head(NULL)
@@ -228,28 +228,28 @@
 {
     cNODE<T> * node;
 
-/*    for (int X = 0; X < CACHESIZE; X++) */
-/*        if (match(Cache[X]->data,data)) */
-/*            return(&(Cache[X]->data)); */
+//    for (int X = 0; X < CACHESIZE; X++)
+//        if (match(Cache[X]->data,data))
+//            return(&(Cache[X]->data));
 
     for (node = head; node != NULL; node = node->next)
-/*    { */
+//    {
         if (match(node->data,data))
-/*        { */
-/*            for (X = CACHESIZE-1; X > 0; X--) */
-/*                Cache[X] = Cache[X-1]; */
-/*            Cache[0] = node; */
+//        {
+//            for (X = CACHESIZE-1; X > 0; X--)
+//                Cache[X] = Cache[X-1];
+//            Cache[0] = node;
             return(&(node->data));
-/*        } */
-/*    } */
+//        }
+//    }
 
     return(NULL);
 
-/*    for (node = head; node != NULL; node = node->next) */
-/*        if (match(node->data,data)) */
-/*            return(&(node->data)); */
- /* */
-/*    return(NULL); */
+//    for (node = head; node != NULL; node = node->next)
+//        if (match(node->data,data))
+//            return(&(node->data));
+//
+//    return(NULL);
 }
 
     /*--------------------------------------------------------------------*/
@@ -370,7 +370,7 @@
 
 /******************************************************************************/
 
-/* Test Code */
+// Test Code
 
 #ifdef    ___TEST___
 
diff -ruN pcb153/SOURCE/H/MESSAGES.H pcb153/upd154/SOURCE/H/MESSAGES.H
--- pcb153/SOURCE/H/MESSAGES.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/MESSAGES.H	2026-09-11 00:24:27.978481742 +0000
@@ -262,6 +262,15 @@
 void LIBENTRY freehdrs(void);
 void LIBENTRY makemsgheader(msgheadertype *Header, char Status, unsigned short NumBytes, char *Date, char *Time, char *To, long RefNum, unsigned short ReadDate, char *ReadTime, char ReadStatus, char *From, char *Subject, char *Password, bool Echo);
 
+/* 15.4 v0.026: PPL bridge helpers — implementations in MSG/MSGPPL.C.
+ * Field indices for get/set follow SYSOP_154.TXT section 4.4:
+ *   1=From, 2=To, 3=Subject, 4=RefNum, 5=Password, 6=Status, 7=Date, 8=Time.
+ * Return 0 on success; negative on error (see MSGPPL.C header comment).
+ */
+int LIBENTRY pplgetmsghdr(long msgnum, int field, char *out, int outsize);
+int LIBENTRY pplsetmsghdr(long msgnum, int field, const char *value);
+int LIBENTRY pplmovemsg  (long msgnum, unsigned short destconf);
+
 /*
 #ifdef __cplusplus
 }
diff -ruN pcb153/SOURCE/H/NEWQ.HPP pcb153/upd154/SOURCE/H/NEWQ.HPP
--- pcb153/SOURCE/H/NEWQ.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/NEWQ.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -44,17 +44,17 @@
           bool  create             (void);
 
           void  view               (void);
-/*          bool  isOpen             (void){return (Qfile.handle != 0);} */
+//          bool  isOpen             (void){return (Qfile.handle != 0);}
           int   getOutboundCount   (void);
 
 protected:
 
 private:
 
-static    cDOSFILE Qfile;  /* The QUEUE file object */
-static    int instance;  /* How many objects are active */
-          char    QfileName[50];  /* Name of QUEUE file */
-          int     slotSize;  /* Size of queue file record entry (including header) */
+static    cDOSFILE Qfile;           // The QUEUE file object
+static    int instance;        // How many objects are active
+          char    QfileName[50];    // Name of QUEUE file
+          int     slotSize;         // Size of queue file record entry (including header)
           int     QSize;
           int     fileOpen;
 static    QUEUE_HEADER   h;
diff -ruN pcb153/SOURCE/H/NEWSCR.H pcb153/upd154/SOURCE/H/NEWSCR.H
--- pcb153/SOURCE/H/NEWSCR.H	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/NEWSCR.H	2026-09-11 00:24:27.978481742 +0000
@@ -69,16 +69,16 @@
 
 /* Function Prototypes */
 
-/* #ifdef __cplusplus */
-/* extern "C" { */
-/* #endif */
+// #ifdef __cplusplus
+// extern "C" {
+// #endif
 
 int  LIBENTRY doScript      (char * scrFile, char * ansFile, int numToks);
 void LIBENTRY cleanupScript (void);
 
-/* #ifdef __cplusplus */
-/* } */
-/* #endif */
+// #ifdef __cplusplus
+// }
+// #endif
 
 /******************************************************************************/
 
diff -ruN pcb153/SOURCE/H/NEWSCR.HPP pcb153/upd154/SOURCE/H/NEWSCR.HPP
--- pcb153/SOURCE/H/NEWSCR.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/NEWSCR.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,11 +37,11 @@
 
 /******************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include	<dos.h>
 
@@ -52,18 +52,24 @@
 #include	"label.hpp"
 #include	"var.hpp"
 
+/* 15.4: URead now has ShortDesc field, personal_psa_t is defined for the
+ * new PSA, and accounttype exists for the Account PSA.  All come in via
+ * pcbmisc.hpp -> users.h (included above).  cSCRIPT class members
+ * ptrURead / ptrPersonal / ptrAccount rely on those types.
+ */
+
 #if defined(DBASE) && !defined(___COMP___)
 #include	"dbase.hpp"
 #endif
 
 /******************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 #ifdef	PCB_DEMO
 #define SCR_BUF_SIZE	(1024-1)
 #else
-#define SCR_BUF_SIZE	(65279L-1)  /* 65535-256 is the magic number */
+#define SCR_BUF_SIZE	(65279L-1)		// 65535-256 is the magic number
 #endif
 
 #define SCR_VER 		1530L
@@ -104,9 +110,9 @@
 #define MAX_FILE_CHAN	8
 
 #define MAX_DBF_CHANNELS 8
-/* #define LA_NONE		   0 */
-/* #define LA_READ		   1 */
-/* #define LA_WRITE 	   2 */
+// #define LA_NONE		   0
+// #define LA_READ		   1
+// #define LA_WRITE 	   2
 
 #define NO_USER_GOT 	-1
 #define CUR_USER_GOT	0
@@ -312,7 +318,7 @@
 #define TOK_OP_SHOWSTAT    -160
 #define TOK_OP_PAGESTAT    -161
 
-/* The following were added for PPL 2.0 */
+// The following were added for PPL 2.0
 
 #define TOK_OP_REPSTR	   -162
 #define TOK_OP_STRSTR	   -163
@@ -358,7 +364,7 @@
 #define TOK_OP_LOMSGNUM    -203
 #define TOK_OP_HIMSGNUM    -204
 
-/* The following were added for PPL 2.0+1 */
+// The following were added for PPL 2.0+1
 
 #define TOK_OP_DRIVESPACE  -205
 #define TOK_OP_OUTBYTES    -206
@@ -370,7 +376,7 @@
 #define TOK_OP_STACKLEFT   -212
 #define TOK_OP_STACKERR    -213
 
-/* The following were added for PPL 3.0 */
+// The following were added for PPL 3.0
 
 #define TOK_OP_DGETALIAS   -214
 #define TOK_OP_DBOF 	   -215
@@ -388,7 +394,7 @@
 #define TOK_OP_FNEXT	   -227
 #define TOK_OP_DNEXT	   -228
 #define TOK_OP_TODDATE	   -229
-#define TOK_OP_DCLOSEALL   -230  /*FIRST DUP */
+#define TOK_OP_DCLOSEALL   -230 	//FIRST DUP
 #define TOK_OP_DOPEN	   -231
 #define TOK_OP_DCLOSE	   -232
 #define TOK_OP_DSETALIAS   -233
@@ -447,11 +453,20 @@
 #define TOK_OP_FTELL	   -285
 #define TOK_OP_OS		   -286
 
+// 15.4 additions (per RE of PPLC.EXE 15.4b — new tokens sorted alphabetically
+// against 15.3 v0.015 baseline).  Semantics inferred from name + call-site
+// argument count observed in the compiled table.
+#define TOK_OP_GETBANKVAL  -287     /* GETBANKVAL(x)      1 arg  — read banking PSA value */
+#define TOK_OP_GETMSGHDR   -288     /* GETMSGHDR(msg,fld) 2 args — read msg header field */
+#define TOK_OP_SETMSGHDR   -289     /* SETMSGHDR(m,f,v)   3 args — write msg header field */
+#define TOK_OP_SHORTDESC   -290     /* SHORTDESC(x)       1 arg  — user's short-desc field */
+#define TOK_OP_U_PERSONAL  -291     /* U_PERSONAL(idx)    1 arg  — read personal PSA field */
+
 
-#define MAX_OP_TOK			286
+#define MAX_OP_TOK			291
 
-/* CONFINF()  -> return a piece of information about the conference */
-/* UN_BROAD() -> return broadcast information */
+// CONFINF()  -> return a piece of information about the conference
+// UN_BROAD() -> return broadcast information
 
 	/*--------------------------------------------------------------------*/
 
@@ -584,7 +599,7 @@
 #define TOK_PAGEON		110
 #define TOK_PAGEOFF 	111
 
-/* The following were added for PPL 2.0 */
+// The following were added for PPL 2.0
 
 #define TOK_FSEEK		112
 #define TOK_FFLUSH		113
@@ -637,7 +652,7 @@
 #define TOK_BRAG		160
 #define TOK_FREALTUSER	161
 
-/* the following were added by Stan after 02/14/94 */
+// the following were added by Stan after 02/14/94
 
 #define TOK_SETLMR		162
 #define TOK_SETENV		163
@@ -652,39 +667,39 @@
 #define TOK_STATIC		172
 #define TOK_STACKABORT	173
 
-/* The following were added for PPL 3.0 */
+// The following were added for PPL 3.0
 
-#define TOK_DCREATE 	174  /* dbfchan,name,exclusive, fieldinfo, taginfo */
-#define TOK_DOPEN		175  /* dbfchan,name, exclusive */
-#define TOK_DCLOSE		176  /* dbfchan */
-#define TOK_DSETALIAS	177  /* dbfchan,name */
-#define TOK_DPACK		178  /* dbfchannel */
-#define TOK_DCLOSEALL	179 /* */
-#define TOK_DLOCK		180  /* dbfchannel */
-#define TOK_DLOCKF		180  /* dbfchannel	 dlock & dlockf do the same thing */
-#define TOK_DLOCKR		181  /* dbfchannel, recno */
-#define TOK_DLOCKG		182  /* dbfchannel, recnos,count */
-#define TOK_DUNLOCK 	183  /* dbfchannel */
-#define TOK_DNCREATE	184  /* dbfchannel,name,taginfo */
-#define TOK_DNOPEN		185  /* dbfchannle, name */
-#define TOK_DNCLOSE 	186  /* dbfchannel, name */
-#define TOK_DNCLOSEALL	187  /* channel */
-#define TOK_DNEW		188  /* channel */
-#define TOK_DADD		189  /* channel */
-#define TOK_DAPPEND 	190  /* channel */
-#define TOK_DTOP		191  /* channel */
-#define TOK_DGO 		192  /* channel, recno */
-#define TOK_DBOTTOM 	193  /* channel */
-#define TOK_DSKIP		194  /* channel, number */
-#define TOK_DBLANK		195  /* channel */
-#define TOK_DDELETE 	196  /* channel */
-#define TOK_DRECALL 	197  /* channel */
-#define TOK_DTAG		198  /* channel, name */
-#define TOK_DSEEK		199  /* channel, expression */
-#define TOK_DFBLANK 	200  /* channel, name */
-#define TOK_DGET		201  /* channel, name , var */
-#define TOK_DPUT		202  /* channel, name, expression */
-#define TOK_DFCOPY		203  /* channel, name, channel, name */
+#define TOK_DCREATE 	174 			// dbfchan,name,exclusive, fieldinfo, taginfo
+#define TOK_DOPEN		175 			// dbfchan,name, exclusive
+#define TOK_DCLOSE		176 			// dbfchan
+#define TOK_DSETALIAS	177 			// dbfchan,name
+#define TOK_DPACK		178 			// dbfchannel
+#define TOK_DCLOSEALL	179 			//
+#define TOK_DLOCK		180 			// dbfchannel
+#define TOK_DLOCKF		180 			// dbfchannel	 dlock & dlockf do the same thing
+#define TOK_DLOCKR		181 			// dbfchannel, recno
+#define TOK_DLOCKG		182 			// dbfchannel, recnos,count
+#define TOK_DUNLOCK 	183 			// dbfchannel
+#define TOK_DNCREATE	184 			// dbfchannel,name,taginfo
+#define TOK_DNOPEN		185 			// dbfchannle, name
+#define TOK_DNCLOSE 	186 			// dbfchannel, name
+#define TOK_DNCLOSEALL	187 		   // channel
+#define TOK_DNEW		188 			// channel
+#define TOK_DADD		189 			// channel
+#define TOK_DAPPEND 	190 			// channel
+#define TOK_DTOP		191 			// channel
+#define TOK_DGO 		192 			// channel, recno
+#define TOK_DBOTTOM 	193 			// channel
+#define TOK_DSKIP		194 			// channel, number
+#define TOK_DBLANK		195 			// channel
+#define TOK_DDELETE 	196 			// channel
+#define TOK_DRECALL 	197 			// channel
+#define TOK_DTAG		198 			// channel, name
+#define TOK_DSEEK		199 			// channel, expression
+#define TOK_DFBLANK 	200 			// channel, name
+#define TOK_DGET		201 			// channel, name , var
+#define TOK_DPUT		202 			// channel, name, expression
+#define TOK_DFCOPY		203 			// channel, name, channel, name
 
 #define TOK_EVAL		204
 #define TOK_ACCOUNT 	205
@@ -709,23 +724,30 @@
 #define TOK_FDOQADD 	224
 #define TOK_FDOQDEL 	225
 #define TOK_SOUNDDELAY	226
-
-#define MAX_TOK 		226
-
-/* INPUTFMT color,var,text,mask,valid */
-/* color and var are as expected */
-/* text would be the default string with literals "(   )    -    " */
-/* mask would be the input mask 				  "LNNNLLNNNLNNNN" */
-/*	 L = literal,		A = alpha		w/force upper, a = alpha */
-/*	 N = numeric,		C = alnum		w/force upper, c = alnum */
-/*	 O = numeric+space, X = any 		w/force upper, x = any */
-/*						B = alpha+space w/force upper, b = alpha+space */
-/*						D = alnum+space w/force upper, d = alnum+space */
-/* valid is as expected */
+/* 15.4 additions — statement forms discovered by diffing PPLC.EXE
+ * 15.4b against 15.3 v0.015 PPLC.  Argument counts read from the
+ * compiled token-table layout.
+ */
+#define TOK_MOVEMSG          227     /* MOVEMSG msgnum destconf         (2 args) */
+#define TOK_SETBANKVAL       228     /* SETBANKVAL idx value            (2 args) */
+#define TOK_SHORTDESCSTMT    229     /* SHORTDESC value                 (1 arg)  */
+
+#define MAX_TOK 		229
+
+// INPUTFMT color,var,text,mask,valid
+// color and var are as expected
+// text would be the default string with literals "(   )    -    "
+// mask would be the input mask 				  "LNNNLLNNNLNNNN"
+//	 L = literal,		A = alpha		w/force upper, a = alpha
+//	 N = numeric,		C = alnum		w/force upper, c = alnum
+//	 O = numeric+space, X = any 		w/force upper, x = any
+//						B = alpha+space w/force upper, b = alpha+space
+//						D = alnum+space w/force upper, d = alnum+space
+// valid is as expected
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 typedef unsigned char byte;
 
@@ -785,7 +807,7 @@
 
 typedef cSTACK<int> 	   tTS;
 typedef cISTACK<cVARVAL>   tVS;
-typedef struct {  /* structure to hold select case info on a nested statement */
+typedef struct {			  // structure to hold select case info on a nested statement
 	char * old;
 	int   cntr;
 } ss;
@@ -800,7 +822,7 @@
 {
 	public:
 
-/* Script interface functions */
+		// Script interface functions
 
 		cSCRIPT  (int numToks);
 
@@ -813,7 +835,7 @@
 
 
 
-/* Execute support data */
+		// Execute support data
 
 #ifdef	___EXEC___
 
@@ -823,7 +845,7 @@
 
 	private:
 
-/* Compile support data */
+		// Compile support data
 
 #ifdef	___COMP___
 
@@ -863,7 +885,7 @@
 		int 				sysLabelCount;
 
 		cSTACK<ss *>		  selectStack;
-/*cSTACK<int>		caseStack; */
+		//cSTACK<int>		caseStack;
 		bool				inCase;
 
 
@@ -871,7 +893,7 @@
 
 #endif
 
-/* Execute support data */
+		// Execute support data
 
 #ifdef	___EXEC___
 
@@ -892,7 +914,7 @@
 
 #endif
 
-/*cSTACK<char _FAR_ *>	retAddr; */
+		//cSTACK<char _FAR_ *>	retAddr;
 		cSTACK<long>		retAddr;
 
 		tVS *				varStack;
@@ -929,7 +951,7 @@
 
 		nodetype			unInfo;
 
-		#ifndef __OS2__  /* may need to finish (REGS and SREGS are not defined) */
+		#ifndef __OS2__  // may need to finish (REGS and SREGS are not defined)
 		REGS				intrREGS;
 		SREGS				intrSEGS;
 		#endif
@@ -940,6 +962,16 @@
 		char  * 			ptrUReg;
 		long  * 			ptrULMR;
 
+		/* 15.4 additions — pointers into the current user's PSA blocks
+		 * populated by cSCRIPT::loadUser() alongside ptrUData.
+		 * ptrURead points to the URead record (has ShortDesc field);
+		 * ptrPersonal is the personal PSA (Gender/Birthdate/Email/Web);
+		 * ptrAccount is the banking PSA (for GETBANKVAL).
+		 */
+		URead *				ptrURead;
+		personal_psa_t *		ptrPersonal;
+		accounttype *			ptrAccount;
+
 		bool				searchAvailable;
 		bool				searchInUse;
 		int 				searchTokens;
@@ -966,7 +998,7 @@
 		bool				rled;
 #endif
 
-/* Info & Scan Support Data */
+		// Info & Scan Support Data
 
 #if defined(___INFO___) || defined(___SCAN___)
 
@@ -979,13 +1011,13 @@
 
 #endif
 
-/* Other data */
+		// Other data
 
 		char _FAR_ *		scriptBuffer;
 		char _FAR_ *		scriptPointer;
-		uint				scriptSize;  /* DWT: changed from long to uint */
+		uint				scriptSize; 		  // DWT: changed from long to uint
 		int 				swapScriptPointer;
-		uint				swapScriptSize;  /* DWT: changed from long to uint */
+		uint				swapScriptSize; 	  // DWT: changed from long to uint
 		long				swapOffset;
 
 		sint				varCount;
@@ -995,7 +1027,7 @@
 
 
 
-/* Compile support functions */
+		// Compile support functions
 
 #ifdef	___COMP___
 
@@ -1049,7 +1081,7 @@
 
 		static int	  LIBENTRY tokenVARS		   (cSCRIPT & obj, sKEYWORD & inf, char * args, int line, int pass);
 
-		static int	  LIBENTRY isCall			   (cSCRIPT & obj, char * s, int line, int pass, bool procOnly, bool funcOnly,int *err);  /* added by stan */
+		static int	  LIBENTRY isCall			   (cSCRIPT & obj, char * s, int line, int pass, bool procOnly, bool funcOnly,int *err); // added by stan
 
 		static char * LIBENTRY standardizeLine	   (char * l, int & q, int & p);
 
@@ -1111,7 +1143,7 @@
 
 #endif
 
-/* Execute support functions */
+		// Execute support functions
 
 #ifdef	___EXEC___
 
@@ -1153,6 +1185,10 @@
         cVARVAL *     LIBENTRY AccountSub          (cVARVAL *p1);
         cVARVAL *     LIBENTRY PcbAccountSub       (cVARVAL *p1);
         cVARVAL *     LIBENTRY ScanMsgHdrSub       (cVARVAL *p1, cVARVAL *p2, cVARVAL *p3, cVARVAL *p4);
+        /* 15.4 additions (v0.028) — see EVALP.CPP for implementations. */
+        cVARVAL *     LIBENTRY GetMsgHdrSub        (cVARVAL *p1, cVARVAL *p2, cVARVAL *p3);
+        cVARVAL *     LIBENTRY SetMsgHdrSub        (cVARVAL *p1, cVARVAL *p2, cVARVAL *p3, cVARVAL *p4);
+        int           LIBENTRY pplmovemsg          (unsigned conf, long msgnum, int movetype);
 
 		int 		  LIBENTRY doForItems		   (int LIBENTRY (* itemfunc)(cSCRIPT & obj, int num));
 		static int	  LIBENTRY redimArray		   (cSCRIPT & obj, int num);
@@ -1194,7 +1230,7 @@
 
 #endif
 
-/* Info & Scan Support Functions */
+		// Info & Scan Support Functions
 
 #if defined(___INFO___) || defined(___SCAN___)
 
@@ -1229,7 +1265,7 @@
 		static void   LIBENTRY tokenIFWHILE 	   (cSCRIPT & obj, sKEYWORD & inf);
 		static void   LIBENTRY tokenFOR 		   (cSCRIPT & obj, sKEYWORD & inf);
 
-		static void   LIBENTRY tokenSELECT		   (cSCRIPT & obj, sKEYWORD & inf);  /* Added by Stan */
+		static void   LIBENTRY tokenSELECT		   (cSCRIPT & obj, sKEYWORD & inf); // Added by Stan
 		static void   LIBENTRY tokenCASE		   (cSCRIPT & obj, sKEYWORD & inf);
 		static void   LIBENTRY tokenDEFAULT 	   (cSCRIPT & obj, sKEYWORD & inf);
 		static void   LIBENTRY tokenSLCTEND 	   (cSCRIPT & obj, sKEYWORD & inf);
@@ -1263,30 +1299,30 @@
 
 #endif
 
-/* Other functions */
+		// Other functions
 
 		void		  LIBENTRY initialize		   (int pass);
 
-/*		  static void	LIBENTRY countVars			 (cVAR & obj); */
+//		  static void	LIBENTRY countVars			 (cVAR & obj);
 		static void   LIBENTRY writeVars		   (cVAR & obj);
 
 };
 
 /******************************************************************************/
 
-/* Constants */
+// Constants
 
 /******************************************************************************/
 
-/* Variables */
+// Variables
 
 /******************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 /******************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /******************************************************************************/
 
diff -ruN pcb153/SOURCE/H/PASSTHRU.HPP pcb153/upd154/SOURCE/H/PASSTHRU.HPP
--- pcb153/SOURCE/H/PASSTHRU.HPP	2026-09-11 00:24:27.903372910 +0000
+++ pcb153/upd154/SOURCE/H/PASSTHRU.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -41,15 +41,15 @@
 #endif
 #include "structs.h"
 
-/* Prototypes */
+// Prototypes
 
 typedef struct
 {
-	unsigned char 	R :1;  /* R registration flag */
-	unsigned char 	S :1;  /* S registration flag */
-	unsigned char 	A :1;  /* Active flag. This is so I know ehn this record should be packed out */
-	unsigned char  reserved : 5;  /*Reserved */
-}REGREC;  /* This structure should be one byte in length */
+	unsigned char 	R :1; 				// R registration flag
+	unsigned char 	S :1; 				// S registration flag
+	unsigned char 	A :1; 				// Active flag. This is so I know ehn this record should be packed out
+	unsigned char  reserved : 5; //Reserved
+}REGREC;		// This structure should be one byte in length
 
 
 class cPTINFO
diff -ruN pcb153/SOURCE/H/PCBMISC.HPP pcb153/upd154/SOURCE/H/PCBMISC.HPP
--- pcb153/SOURCE/H/PCBMISC.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PCBMISC.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,21 +37,21 @@
 
 /******************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 #ifdef LIB
 
 #include    "misc.h"
 
-/*#ifdef  ___EXEC___         // commented out this line and */
-#ifdef LIB  /* changed to LIB on 4/26/95 DWT */
+//#ifdef  ___EXEC___         // commented out this line and
+#ifdef LIB                          // changed to LIB on 4/26/95 DWT
 #include    "pcbtools.h"
-/*#include      "pcboard.h" */
-/*#include      "newdata.h" */
-/*#include      "users.h" */
+//#include      "pcboard.h"
+//#include      "newdata.h"
+//#include      "users.h"
 #else
 #include    "country.h"
 #include    "dosfunc.h"
@@ -75,7 +75,7 @@
 
 /******************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 
 
@@ -93,8 +93,8 @@
 #define VerifySupport   UserSys.VerifySupport
 #define StatsSupport    UserSys.StatsSupport
 #define NotesSupport    UserSys.NotesSupport
-/*#define AccountSupport  UserSys.AccountSupport */
-/*#define QwkSupport      UserSys.QwkSupport */
+//#define AccountSupport  UserSys.AccountSupport
+//#define QwkSupport      UserSys.QwkSupport
 
 #define DLDRMonth()     (ptrUData->DateLastDirRead.Month)
 #define DLDRDay()       (ptrUData->DateLastDirRead.Day)
@@ -140,16 +140,16 @@
 #define clearBuf(b) memset(&b,0,sizeof(b))
 
 
-/* If subdir or volume bits are set then it's not a file */
+// If subdir or volume bits are set then it's not a file
 #define fexist(f)   ((fileexist(f) & 0x18) == 0)
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 #ifdef LIB
 
-/*typedef enum { SHOW,       HIDE }        showtype; */
+//typedef enum { SHOW,       HIDE }        showtype;
 typedef enum { NLOGOFF,    ALOGOFF }     logtype;
 typedef enum { UPLOAD,     DOWNLOAD }    xfertype;
 typedef enum { CHECKDIRTY, IGNOREDIRTY } dirtytype;
@@ -173,11 +173,11 @@
   long     Offset;           /* Offset in User Record where TPA record begins    */
 } apptype;
 
-/*typedef struct { */
-/*  long HighMsgNum; */
-/*  long LowMsgNum; */
-/*  long NumActiveMsgs; */
-/*} msgbasestattype; */
+//typedef struct {
+//  long HighMsgNum;
+//  long LowMsgNum;
+//  long NumActiveMsgs;
+//} msgbasestattype;
 #endif
 
 #if defined(___COMP___) || defined(___INFO___) || defined(___SCAN___)
@@ -191,15 +191,15 @@
 
 /******************************************************************************/
 
-/* Constants */
+// Constants
 
 /******************************************************************************/
 
-/* Variables */
+// Variables
 
 #if defined(___COMP___) || defined(___INFO___) || defined(___SCAN___)
 
-/*extern sSTATUS Status; */
+//extern sSTATUS Status;
 extern char OpenFileNames[MAXHANDLES+1][MAXNAMELEN];
 
 #endif
@@ -216,7 +216,7 @@
 
 /******************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 #if defined(LIB) && defined(___EXEC___)
 
@@ -269,13 +269,13 @@
 
 void   LIBENTRY xlatetext               ( char * New, char * Old );
 
-/*char * LIBENTRY countrydate             ( char * s ); */
-/*char * LIBENTRY countrydate2            ( char * s ); */
-/*char * LIBENTRY uncountrydate           ( char * s ); */
-/*char * LIBENTRY uncountrydate2          ( char * s ); */
+//char * LIBENTRY countrydate             ( char * s );
+//char * LIBENTRY countrydate2            ( char * s );
+//char * LIBENTRY uncountrydate           ( char * s );
+//char * LIBENTRY uncountrydate2          ( char * s );
 
-/*void   LIBENTRY openmodem               ( showtype Show ); */
-/*void   LIBENTRY closemodem              ( bool TurnOffDTR ); */
+//void   LIBENTRY openmodem               ( showtype Show );
+//void   LIBENTRY closemodem              ( bool TurnOffDTR );
 
 void   LIBENTRY printcom                ( char * s );
 
@@ -299,10 +299,10 @@
 void   LIBENTRY setripmouseregion       ( int Num, int X1, int Y1, int X2, int Y2, int FontX, int FontY, bool Invert, bool Clear, char * Text );
 int    LIBENTRY checkscreenforfilenames ( int StartLine, char * FileName );
 
-/*bool   LIBENTRY insearch                ( void ); */
-/*int    LIBENTRY tokenscan               ( char * Input, char * CodedInput, bool CaseSensitive ); */
-/*bool   LIBENTRY parsersearch            ( char * Text, int TextLen, char * CodedInput, bool RetVal, int LastToken ); */
-/*void   LIBENTRY stopsearch              ( void ); */
+//bool   LIBENTRY insearch                ( void );
+//int    LIBENTRY tokenscan               ( char * Input, char * CodedInput, bool CaseSensitive );
+//bool   LIBENTRY parsersearch            ( char * Text, int TextLen, char * CodedInput, bool RetVal, int LastToken );
+//void   LIBENTRY stopsearch              ( void );
 void   LIBENTRY printfoundtext          ( char * Str );
 
 int    LIBENTRY gettpaheader            ( char * KeyWord, apptype * AppHeader );
diff -ruN pcb153/SOURCE/H/PCBMSGS.HPP pcb153/upd154/SOURCE/H/PCBMSGS.HPP
--- pcb153/SOURCE/H/PCBMSGS.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PCBMSGS.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -31,61 +31,61 @@
 #pragma pack(1)
 struct sBASEHDR
 {
-    cBSREAL       high;  /*     0 bsreal   4 High Message Number       (0 to 16,700,000) */
-    cBSREAL       low;  /*     4 bsreal   4 Low Message Number        (0 to 16,700,000) */
-    cBSREAL       active;  /*     8 bsreal   4 Number of Active Messages (0 to 32,767) */
-    cBSREAL       callers;  /*    12 bsreal   4 Number of System Callers (Main Message Base Only) */
-    char          locked [   6 ];  /*    16 str      6 The "LOCKED" field for pre-14.2 systems (see note 1) */
-    char          res    [ 106 ];  /*    22 str    106 Reserved for future use */
+    cBSREAL       high;           //     0 bsreal   4 High Message Number       (0 to 16,700,000)
+    cBSREAL       low;            //     4 bsreal   4 Low Message Number        (0 to 16,700,000)
+    cBSREAL       active;         //     8 bsreal   4 Number of Active Messages (0 to 32,767)
+    cBSREAL       callers;        //    12 bsreal   4 Number of System Callers (Main Message Base Only)
+    char          locked [   6 ]; //    16 str      6 The "LOCKED" field for pre-14.2 systems (see note 1)
+    char          res    [ 106 ]; //    22 str    106 Reserved for future use
 };
 #pragma pack()
 
 #pragma pack(1)
 struct sMSGHDR
 {
-    char          status;  /*     0 char     1 Message Status Flag (see note 2) */
-    cBSREAL       number;  /*     1 bsreal   4 Message Number   (0 to 16,700,000) */
-    cBSREAL       refer;  /*     5 bsreal   4 Reference Number (0 to 16,700,000) */
-    unsigned char blocks;  /*     9 char     1 Number of 128 Byte Blocks in Message (see note 3) */
-    char          dmsg   [   8 ];  /*    10 str      8 Date of Message Entry (in "mm-dd-yy" format) */
-    char          tmsg   [   5 ];  /*    18 str      5 Time of Message Entry (in "hh:mm" format) */
-    char          tname  [  25 ];  /*    23 str     25 Name of the User to whom the Message is Addressed */
-    cBSREAL       dread;  /*    48 bsreal   4 Date of the Reply Message (in yymmdd format) */
-    char          tread  [   5 ];  /*    52 str      5 Time of the Reply Message (in "hh:mm" format) */
-    char          reply;  /*    57 char     1 The Letter "R" if the Message has a Reply */
-    char          fname  [  25 ];  /*    58 str     25 Name of the User who wrote the Message */
-    char          subj   [  25 ];  /*    83 str     25 Subject of the Message */
-    char          pwd    [  12 ];  /*   108 str     12 Password Need to Read the Message (if any) */
-    char          active;  /*   120 char     1 Active Status (225 = active, 226 = inactive) */
-    char          echo;  /*   121 char     1 The Letter "E" if the Message is to be Echoed */
-    char          res1   [   4 ];  /* | 122 str      4 Reserved for future use */
-    char          exthdr;  /* | 127 bitmap   1 Extended Header Flags */
-    char          res2;  /* | 128 char     1 Reserved for future use */
+    char          status;         //     0 char     1 Message Status Flag (see note 2)
+    cBSREAL       number;         //     1 bsreal   4 Message Number   (0 to 16,700,000)
+    cBSREAL       refer;          //     5 bsreal   4 Reference Number (0 to 16,700,000)
+    unsigned char blocks;         //     9 char     1 Number of 128 Byte Blocks in Message (see note 3)
+    char          dmsg   [   8 ]; //    10 str      8 Date of Message Entry (in "mm-dd-yy" format)
+    char          tmsg   [   5 ]; //    18 str      5 Time of Message Entry (in "hh:mm" format)
+    char          tname  [  25 ]; //    23 str     25 Name of the User to whom the Message is Addressed
+    cBSREAL       dread;          //    48 bsreal   4 Date of the Reply Message (in yymmdd format)
+    char          tread  [   5 ]; //    52 str      5 Time of the Reply Message (in "hh:mm" format)
+    char          reply;          //    57 char     1 The Letter "R" if the Message has a Reply
+    char          fname  [  25 ]; //    58 str     25 Name of the User who wrote the Message
+    char          subj   [  25 ]; //    83 str     25 Subject of the Message
+    char          pwd    [  12 ]; //   108 str     12 Password Need to Read the Message (if any)
+    char          active;         //   120 char     1 Active Status (225 = active, 226 = inactive)
+    char          echo;           //   121 char     1 The Letter "E" if the Message is to be Echoed
+    char          res1   [   4 ]; // | 122 str      4 Reserved for future use
+    char          exthdr;         // | 127 bitmap   1 Extended Header Flags
+    char          res2;           // | 128 char     1 Reserved for future use
 };
 #pragma pack()
 
 #pragma pack(1)
 struct sMSGEXTHDR
 {
-    sint          id;  /*   int  Ident;        16639 (40FF hex) */
-    char          funct  [   7 ];  /* | char Function[7];  TO,TO2,FROM,FROM2,SUBJECT,ATTACH,LIST,ROUTE,ORIGIN,REQRR,ACKRR,ACKNAME,PACKOUT */
-    char          colon;  /*   char Colon;        always set to ":" */
-    char          desc   [  60 ];  /*   char Descript[60]; user name, subject, filename */
-    char          stat;  /*   char Status;       NONE, READ (if LIST) */
-    char          cr;  /*   char CarReturn;    0xE3 or 0x0D */
+    sint          id;             //   int  Ident;        16639 (40FF hex)
+    char          funct  [   7 ]; // | char Function[7];  TO,TO2,FROM,FROM2,SUBJECT,ATTACH,LIST,ROUTE,ORIGIN,REQRR,ACKRR,ACKNAME,PACKOUT
+    char          colon;          //   char Colon;        always set to ":"
+    char          desc   [  60 ]; //   char Descript[60]; user name, subject, filename
+    char          stat;           //   char Status;       NONE, READ (if LIST)
+    char          cr;             //   char CarReturn;    0xE3 or 0x0D
 };
 #pragma pack()
 
 #pragma pack(1)
 struct sMSGIDX
 {
-    long          off;  /* |   0 long     4 Offset (0 if none, >0 if active, <0 if killed) */
-    long          num;  /* |   4 long     4 Message Number */
-    char          to     [ 25 ];  /* |   8 str     25 TO Name */
-    char          from   [ 25 ];  /* |  33 str     25 FROM Name */
-    char          status;  /* |  58 char     1 Status Character (from Message Header) */
-    uint          date;  /* |  59 int      2 Date of Message (in julian date format) */
-    char          res    [  3 ];  /* |  61 str      3 Reserved for future use */
+    long          off;            // |   0 long     4 Offset (0 if none, >0 if active, <0 if killed)
+    long          num;            // |   4 long     4 Message Number
+    char          to     [ 25 ];  // |   8 str     25 TO Name
+    char          from   [ 25 ];  // |  33 str     25 FROM Name
+    char          status;         // |  58 char     1 Status Character (from Message Header)
+    uint          date;           // |  59 int      2 Date of Message (in julian date format)
+    char          res    [  3 ];  // |  61 str      3 Reserved for future use
 };
 #pragma pack()
 
@@ -112,7 +112,7 @@
         long         LIBENTRY highMsgNum    ( void );
 
         int          LIBENTRY readHdr       ( long off );
-/* int          LIBENTRY readHdr       ( void ); */
+     // int          LIBENTRY readHdr       ( void );
         int          LIBENTRY readBody      ( unsigned size = 0 );
 
         int          LIBENTRY writeHdr      ( long off = -1 );
@@ -124,7 +124,7 @@
         sMSGEXTHDR * LIBENTRY getExtHdr     ( int    num );
 
         char       * LIBENTRY getLine       ( void );
-/* int          LIBENTRY addLine       ( char * line ); */
+     // int          LIBENTRY addLine       ( char * line );
 
         void         LIBENTRY resetLine     ( void );
 
diff -ruN pcb153/SOURCE/H/PCBOARD.H pcb153/upd154/SOURCE/H/PCBOARD.H
--- pcb153/SOURCE/H/PCBOARD.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PCBOARD.H	2026-09-11 00:24:27.978481742 +0000
@@ -120,7 +120,26 @@
 
 /* note: YESNO=2049 ... the input functions figure out that ECHODOTS and WORDWRAP would never be used together */
 /* note: NOTBLANK=32768U, same as AUTO ... the displaypcbtext() function figures out this value would never be used as AUTO */
-typedef enum {DEFAULTS=0,ECHODOTS=1,FIELDLEN=2,UPCASE=4,STACKED=8,ERASELINE=16,NEWLINE=32,LFBEFORE=64,LFAFTER=128,LOGIT=256,LOGITLEFT=512,GUIDE=1024,WORDWRAP=2048,YESNO=2049,NOCLEAR=4096,BELL=8192,HIGHASCII=16384,AUTO=32768U,NOTBLANK=32768U} displaytype;
+#ifndef _DISPLAYTYPE_DEFINED
+#define _DISPLAYTYPE_DEFINED
+#ifdef __WATCOMC__
+/* Watcom: avoid macro conflicts with enum member names */
+#pragma push_macro("DEFAULTS")
+#pragma push_macro("WORDWRAP")
+#undef DEFAULTS
+#undef WORDWRAP
+#endif
+#ifdef __WATCOMC__
+typedef enum {DEFAULTS=0,ECHODOTS=1,FIELDLEN=2,UPCASE=4,STACKED=8,ERASELINE=16,NEWLINE=32,LFBEFORE=64,LFAFTER=128,LOGIT=256,LOGITLEFT=512,GUIDE=1024,WORDWRAP=2048,_PCBYESNO=2049,NOCLEAR=4096,BELL=8192,HIGHASCII=16384,AUTO=32768,NOTBLANK=32768} displaytype;
+#define YESNO _PCBYESNO
+#else
+typedef enum {DEFAULTS=0,ECHODOTS=1,FIELDLEN=2,UPCASE=4,STACKED=8,ERASELINE=16,NEWLINE=32,LFBEFORE=64,LFAFTER=128,LOGIT=256,LOGITLEFT=512,GUIDE=1024,WORDWRAP=2048,YESNO=2049,NOCLEAR=4096,BELL=8192,HIGHASCII=16384,AUTO=-32768,NOTBLANK=-32768} displaytype;
+#endif
+#ifdef __WATCOMC__
+#pragma pop_macro("WORDWRAP")
+#pragma pop_macro("DEFAULTS")
+#endif
+#endif
 
 typedef enum {NOCHANGE,FORCENONSTOP,FORCECOUNTLINES} startdisplaytype;
 typedef enum {ACTSHOW, ACTBEGIN, ACTEND, ACTSUSPEND, ACTRESUME} acttype;
@@ -131,9 +150,15 @@
 typedef enum {USER, SYSOP } usertype;
 typedef enum {SHOW, HIDE } showtype;
 typedef enum {INPUTOKAY, INPUTREFUSEANSWER } inputtype;
+#ifdef PROGRAM
+#undef PROGRAM
+#endif
 typedef enum {PROGRAM, USERSELECT } callfromtype;
 typedef enum {LOGON, NLOGOFF, ALOGOFF, REMOTEDOS, FILEVIEWSHELL, DOOR, RUNSETUP, RUNFILER, RUNSM, RUNEVENT, RUNFAX} logtype;
-typedef enum {LEFTJUSTIFY, SPACERIGHT } padtype;
+typedef enum {LEFTJUSTIFY, SPACERIGHT, SPACERIGHTAT } padtype;
+#ifdef TEXT
+#undef TEXT
+#endif
 typedef enum {FNAM, FSIZ, FDTE, FDSC, HEAD, TEXT, DUPE, DLTD, OFLN, NEWF} ColorType;
 typedef enum {NEWTIME, CONFTIME, CREDTIME, PREVTIMEON, ACCTTIME } timereasontype;
 typedef enum {SHELLDIRECT, SHELLVIACOMMAND } shelltype;
@@ -496,11 +521,11 @@
   long    Size;
   long    Seconds;
   unsigned short CPS;
-  uint    Success;  /* holds values of trntype */
-/*trntype Success; */
-/*#if defined(__OS2__) && defined(__BORLANDC__) */
-/*  char  Filler1; */
-/*#endif */
+  uint    Success;     // holds values of trntype
+//trntype Success;
+//#if defined(__OS2__) && defined(__BORLANDC__)
+//  char  Filler1;
+//#endif
   bool    HasDesc;
   bool    Checked;
   bool    Found;
@@ -511,11 +536,11 @@
   bool    Failed;
   char    OldName[13];
   bool    Duplicate;
-  uint    CapType;  /* holds values of captype */
-/*captype CapType; */
-/*#if defined(__OS2__) && defined(__BORLANDC__) */
-/*  char  Filler2; */
-/*#endif */
+  uint    CapType;      // holds values of captype
+//captype CapType;
+//#if defined(__OS2__) && defined(__BORLANDC__)
+//  char  Filler2;
+//#endif
   short   Multiplier;
   bool    NoCost;
   char    Reserved[11];
@@ -801,9 +826,7 @@
 int  LIBENTRY kbdinkey(void);
 int  LIBENTRY cinkey(void);
 int  LIBENTRY waitforkey(void);
-#if defined(__cplusplus)
-int  LIBENTRY waitforkey(int TimeToWait);   /* C++ overload */
-#endif
+int  LIBENTRY waitforkey(int TimeToWait);
 void LIBENTRY checkstatus(void);
 void LIBENTRY watchstatuswhilesending(void);
 
@@ -1292,11 +1315,7 @@
 
 /* BGKEY.ASM */
 #ifndef __OS2__
-  #if defined(__cplusplus)
   extern "C" short LIBENTRY bgetkey2(char Option);  /* used instead of bgetkey() from SYSTEM.LIB */
-  #else
-  short LIBENTRY bgetkey2(char Option);
-  #endif
 #endif
 
 /* NOSCROLL.ASM */
diff -ruN pcb153/SOURCE/H/PCBREAL.HPP pcb153/upd154/SOURCE/H/PCBREAL.HPP
--- pcb153/SOURCE/H/PCBREAL.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PCBREAL.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,13 +37,13 @@
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include    "misc.h"
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 class cBSREAL
 {
@@ -99,18 +99,18 @@
 
 /******************************************************************************/
 
-/* Notes */
+// Notes
 
-/* The following are unused prototypes from MISC.H: */
-/* ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ */
-/* double LIBENTRY bassngltodouble(bassngl OldNum); */
-/* double LIBENTRY basdbletodouble(basdble OldNum); */
-/* void   LIBENTRY doubletobasdble(basdble *New, double Old); */
-/* void   LIBENTRY doubletobassngl(bassngl *New, double Old); */
-/* long   LIBENTRY doubletolong(void *Num); */
-/* void   LIBENTRY doubletopasreal(pasreal *New, double Old); */
-/* void   LIBENTRY longtodouble(char *F, long Num); */
-/* double LIBENTRY pasrealtodouble(pasreal OldNum); */
+// The following are unused prototypes from MISC.H:
+// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+// double LIBENTRY bassngltodouble(bassngl OldNum);
+// double LIBENTRY basdbletodouble(basdble OldNum);
+// void   LIBENTRY doubletobasdble(basdble *New, double Old);
+// void   LIBENTRY doubletobassngl(bassngl *New, double Old);
+// long   LIBENTRY doubletolong(void *Num);
+// void   LIBENTRY doubletopasreal(pasreal *New, double Old);
+// void   LIBENTRY longtodouble(char *F, long Num);
+// double LIBENTRY pasrealtodouble(pasreal OldNum);
 
 /******************************************************************************/
 
diff -ruN pcb153/SOURCE/H/PCBTOSS.HPP pcb153/upd154/SOURCE/H/PCBTOSS.HPP
--- pcb153/SOURCE/H/PCBTOSS.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PCBTOSS.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -28,14 +28,14 @@
 #define PCBTOSS_HPP
 #ifdef FIDO
 
-/* Borland include files */
+// Borland include files
 
-/* Pcboard includes files */
+// Pcboard includes files
 #include "pcboard.h"
 #include "messages.h"
 #include "dosclass.hpp"
 
-/* Fido includes files */
+// Fido includes files
 #include "structs.h"
 #include "defines.h"
 #include "data.hpp"
@@ -55,7 +55,7 @@
 
 private:
 
-/* Member Functions */
+// Member Functions
 
  bool   LIBENTRY  unarchive(char *filename,int type);
  char*  LIBENTRY  make_outbound_list(uint &len, int conf);
@@ -95,7 +95,7 @@
 
 
 
-/* Member Data */
+// Member Data
     DOSFILE                      pkt_file;
     FIDO_MESSAGE_HDR             fido_msg_hdr;
     AREA_STRUCT                  this_area;
diff -ruN pcb153/SOURCE/H/PROTOTYP.H pcb153/upd154/SOURCE/H/PROTOTYP.H
--- pcb153/SOURCE/H/PROTOTYP.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/PROTOTYP.H	2026-09-11 00:24:27.978481742 +0000
@@ -20,9 +20,9 @@
 
 
 
-/*#ifdef __cplusplus */
-/*extern "C" { */
-/*#endif */
+//#ifdef __cplusplus
+//extern "C" {
+//#endif
 
 /* Stolen prototypes from CONIO.H */
 
@@ -114,8 +114,8 @@
 bool LIBENTRY outboundRec(void);
 void LIBENTRY scanQueue(void);
 
-/*#ifdef __cplusplus */
-/*} */
-/*#endif */
+//#ifdef __cplusplus
+//}
+//#endif
 
 #endif
diff -ruN pcb153/SOURCE/H/QINT.HPP pcb153/upd154/SOURCE/H/QINT.HPP
--- pcb153/SOURCE/H/QINT.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/QINT.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -23,8 +23,10 @@
 
         qint ( void );
 
+#if !defined(__386__) && !defined(__FLAT__)
         qint ( int           i );
         qint ( unsigned int  u );
+#endif
         qint ( long          l );
         qint ( unsigned long u );
 
@@ -58,13 +60,16 @@
         int  operator >= ( qint & tr ) { return (!(*this < tr)); }
 #endif
 
+#if !defined(__386__) && !defined(__FLAT__)
         int  operator == ( int tr ) { return (*this == qint(tr)); }
         int  operator != ( int tr ) { return (*this != qint(tr)); }
         int  operator <  ( int tr ) { return (*this <  qint(tr)); }
         int  operator <= ( int tr ) { return (*this <= qint(tr)); }
         int  operator >  ( int tr ) { return (*this >  qint(tr)); }
         int  operator >= ( int tr ) { return (*this >= qint(tr)); }
+#endif
 
+#if !defined(__386__) && !defined(__FLAT__)
         int  operator == ( long tr ) { return (*this == qint(tr)); }
         int  operator != ( long tr ) { return (*this != qint(tr)); }
         int  operator <  ( long tr ) { return (*this <  qint(tr)); }
@@ -78,6 +83,7 @@
         int  operator <= ( unsigned long tr ) { return (*this <= qint(tr)); }
         int  operator >  ( unsigned long tr ) { return (*this >  qint(tr)); }
         int  operator >= ( unsigned long tr ) { return (*this >= qint(tr)); }
+#endif
 
         qint operator += ( qint & tr ) { *this = *this + tr; return *this; }
         qint operator -= ( qint & tr ) { *this = *this - tr; return *this; }
diff -ruN pcb153/SOURCE/H/SCRMISC.HPP pcb153/upd154/SOURCE/H/SCRMISC.HPP
--- pcb153/SOURCE/H/SCRMISC.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/SCRMISC.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,17 +37,17 @@
 
 /******************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include    "newscr.hpp"
 
 /******************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 #if defined(___COMP___)
 
@@ -134,15 +134,15 @@
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 /******************************************************************************/
 
-/* Constants */
+// Constants
 
 /******************************************************************************/
 
-/* Variables */
+// Variables
 
 extern char    * errText[];
 
@@ -150,7 +150,7 @@
 
 /******************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 char * LIBENTRY formattedLine (char * line);
 void   LIBENTRY dispPcbText   (int prompt, char * optext);
@@ -159,14 +159,14 @@
 long            standardizeTicks        (long ticks);
 
 
-/* extern "C" { */
+// extern "C" {
 int    LIBENTRY doScript      (char * filename, char * ansfile, int numtoks);
 void   LIBENTRY cleanupScript (void);
-/* } */
+// }
 
 /******************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /******************************************************************************/
 
diff -ruN pcb153/SOURCE/H/STACK.HPP pcb153/upd154/SOURCE/H/STACK.HPP
--- pcb153/SOURCE/H/STACK.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/STACK.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -37,21 +37,21 @@
 
 /****************************************************************************/
 
-/* Pragmas */
+// Pragmas
 
 /****************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include "list.hpp"
 
 /****************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 /****************************************************************************/
 
-/* Types */
+// Types
 
 template <class T>
 class cSTACK : public cLIST<T>
@@ -85,23 +85,23 @@
 
 /****************************************************************************/
 
-/* Constants */
+// Constants
 
 /****************************************************************************/
 
-/* Variables */
+// Variables
 
 /****************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 /****************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /****************************************************************************/
 
-/* Template Functions */
+// Template Functions
 
 template <class T>
 int LIBENTRY cSTACK<T>::push(T data)
@@ -167,15 +167,15 @@
 
 /****************************************************************************/
 
-/* Test Code */
+// Test Code
 
 #ifdef    ___TEST___
 
-/*typedef cSTACK<int>  tINTSTK; */
-/*typedef cISTACK<int> tIINTSTK; */
+//typedef cSTACK<int>  tINTSTK;
+//typedef cISTACK<int> tIINTSTK;
 
-/*tINTSTK  intStk; */
-/*tIINTSTK iIntStk(DD_DEF); */
+//tINTSTK  intStk;
+//tIINTSTK iIntStk(DD_DEF);
 
 struct s
 {
@@ -190,10 +190,10 @@
 
 };
 
-/*typedef cSTACK<s>  tSSTK; */
+//typedef cSTACK<s>  tSSTK;
 typedef cISTACK<s> tISSTK;
 
-/*tSSTK  sStk; */
+//tSSTK  sStk;
 tISSTK iSStk(DD_DEF);
 
 #endif
diff -ruN pcb153/SOURCE/H/STRUCTS.H pcb153/upd154/SOURCE/H/STRUCTS.H
--- pcb153/SOURCE/H/STRUCTS.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/STRUCTS.H	2026-09-11 00:24:27.978481742 +0000
@@ -30,7 +30,7 @@
 #define PacketType sint
 
 
-/* For capabilities flags. */
+// For capabilities flags.
 typedef struct
 {
     bool ZAP;
@@ -58,9 +58,9 @@
   char  crc_mode;
   char  fill[86];
   uint  crc;
-/*  char  crc1; */
-/*  char  crc2; */
-/*char  checksum; */
+//  char  crc1;
+//  char  crc2;
+  //char  checksum;
 }TELINK_BLOCK;
 
 typedef struct
@@ -412,7 +412,7 @@
 } THIS_ADDRESS;
 #pragma pack()
 
-/* New this_address used with new configuration modules */
+// New this_address used with new configuration modules
 #pragma pack(1)
 typedef struct
 {
@@ -443,7 +443,7 @@
 } NODE_T;
 #pragma pack()
 
-/* New node_t struct for new configuration files */
+// New node_t struct for new configuration files
 #pragma, pack(1)
 typedef struct
 {
@@ -471,7 +471,7 @@
 {
   uint          PCB_Conference;         /* PCBoard conference number    */
   char          Area_Name[AREA_SIZE];          /* name of area                 */
-/*char          Messages[MAXFLEN];       /* name of file for messages    */ */
+  //char          Messages[MAXFLEN];       /* name of file for messages    */
   char          Mreserved[MAXFLEN];       /* name of file for messages    */
   char          default_aka[25];        /* Address to use for Export    */
   char          origin[70];             /* Origin line to use           */
@@ -480,7 +480,7 @@
 } AREA_STRUCT;
 #pragma pack()
 
-/*New areastruct for new config modules */
+ //New areastruct for new config modules
 #pragma pack(1)
 typedef struct
 {
@@ -612,7 +612,7 @@
 } FREQ_MAGIC;
 #pragma pack()
 
-/* New freq_magic for new config modules */
+// New freq_magic for new config modules
 #pragma pack(1)
 typedef struct
 {
diff -ruN pcb153/SOURCE/H/TIMES.H pcb153/upd154/SOURCE/H/TIMES.H
--- pcb153/SOURCE/H/TIMES.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/TIMES.H	2026-09-11 00:24:27.978481742 +0000
@@ -17,7 +17,7 @@
 
 /*****************************************************************************/
 /* Timer values                                                              */
-/* These are mapped to David Terry's platform safe macros. */
+// These are mapped to David Terry's platform safe macros.
 
 #define         ONE_SECOND              ONESECOND
 #define         TWO_SECONDS             TWOSECONDS
diff -ruN pcb153/SOURCE/H/USERS.H pcb153/upd154/SOURCE/H/USERS.H
--- pcb153/SOURCE/H/USERS.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/USERS.H	2026-09-11 19:49:15.560192561 +0000
@@ -33,7 +33,10 @@
 typedef struct {
   unsigned short UserRec;
   char     UserName[25];    /* padded with spaces and no terminating NULL */
+#ifndef _INDEXTYPE_DEFINED
+#define _INDEXTYPE_DEFINED
 } IndexType;
+#endif
 #pragma pack()
 
 #pragma pack(1)
@@ -175,6 +178,46 @@
 } accounttype;
 #pragma pack()
 
+/*---------------------------------------------------------------------------
+ * personal_psa_t -- new in 15.4.  Layout recovered from PCBSM.EXE 15.4b field
+ * label table at data offset 0x26b21 (see v0.017 CHANGES entry).  Stored in
+ * USERS.INF as an app with keyword "PCBPERSONAL"; exposed to PPL via the
+ * U_PERSONAL(idx) accessor added in v0.021.
+ */
+#pragma pack(1)
+typedef struct {
+  char Gender;               /* ' ', 'M', or 'F' -- 15.4 default is ' ' */
+  char Birthdate[6];         /* MMDDYY packed date */
+  char Email[60];            /* Internet email, null-terminated */
+  char Web[60];              /* Personal WEB URL, null-terminated */
+} personal_psa_t;
+
+/* 15.4: timebank_psa_t -- Time/Byte Bank PSA.  12 fields per WHATSNEW.
+ * Fields 0-5 cover time (minutes), fields 6-11 cover bytes (KB).
+ * Each field is a long -- dates stored as Julian day numbers,
+ * amounts as signed quantities.
+ *   0/6 = Last Deposit Date      1/7 = Last Withdrawal Date
+ *   2/8 = Last Transaction Amt   3/9 = Amount Saved (balance)
+ *   4/10= Max Withdrawal/day     5/11= Max Stored Amount
+ */
+#pragma pack(1)
+typedef struct {
+    long TimeLastDeposit;       /* 0  */
+    long TimeLastWithdraw;      /* 1  */
+    long TimeLastTxnAmt;        /* 2  */
+    long TimeBalance;           /* 3  */
+    long TimeMaxWithdraw;       /* 4  */
+    long TimeMaxStored;         /* 5  */
+    long ByteLastDeposit;       /* 6  */
+    long ByteLastWithdraw;      /* 7  */
+    long ByteLastTxnAmt;        /* 8  */
+    long ByteBalance;           /* 9  */
+    long ByteMaxWithdraw;       /* 10 */
+    long ByteMaxStored;         /* 11 */
+} timebank_psa_t;              /* 48 bytes */
+#pragma pack()
+#pragma pack()
+
 #pragma pack(1)
 typedef struct {
   unsigned short MaxMsgs;
@@ -223,7 +266,12 @@
   unsigned char LastMsgRead[40][4]; /* 225-384 */ /* changed 14.0 was LastMsgRead[10] */
   long RecNum;                      /* 385-388 */ /* added in v14.5 to point to USERS.INF */
   reservedbyte2 Flags;              /* 389-389 */ /* reserved in v14.5a */
-  char Reserved[8];                 /* 390-397 */ /* reduced in v14.5 from 15 to 9, in v15.0 from 9 to 8 */
+  /* 15.4: consumed one byte of the 8-byte Reserved for the ShortDesc mode flag.
+   * The remaining 7 bytes stay Reserved for future compat.  On-disk layout is
+   * unchanged (bytes 390-397 still 8 bytes total).
+   */
+  char ShortDesc;                   /* 390-390 */ /* new 15.4: 1=short display, 0=long */
+  char Reserved[7];                 /* 391-397 */ /* was Reserved[8] pre-15.4 */
   unsigned short LastConf2;         /* 398-399 */ /* added in v14.5 for high conferences */
 } URead;
 #pragma pack()
@@ -326,6 +374,14 @@
 } rectype;
 #pragma pack()
 
+#ifndef _FARDATA_
+  #ifdef __LARGE__
+    #define _FARDATA_ far
+  #else
+    #define _FARDATA_
+  #endif
+#endif
+
 extern UData          _FARDATA_ UsersData;
 extern URead          _FARDATA_ UsersRead;
 extern addresstype    _FARDATA_ AddrRec;
@@ -378,6 +434,16 @@
 extern bool           QwkSupport;
 extern long           QwkOffset;
 
+/* 15.4: Personal PSA (Gender/Birthdate/Email/Web) */
+extern bool             PersonalSupport;
+extern long             PersonalOffset;
+extern personal_psa_t   _FARDATA_ Personal;
+
+/* 15.4: Time/Byte Bank PSA */
+extern bool             BankSupport;
+extern long             BankOffset;
+extern timebank_psa_t   _FARDATA_ Bank;
+
 extern long _FAR_ *MsgReadPtr;
 extern long _FAR_ *MsgReadPtrBackup;
 extern long _FAR_ *TempMsgPtr;
diff -ruN pcb153/SOURCE/H/USERSYS.H pcb153/upd154/SOURCE/H/USERSYS.H
--- pcb153/SOURCE/H/USERSYS.H	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/USERSYS.H	2026-09-11 00:24:27.978481742 +0000
@@ -162,7 +162,7 @@
   char           Reserved[13];
 } qwkconfigtype;
 #pragma pack()
-#endif  /*#ifdef EXTENDED_USER_INFO */
+#endif //#ifdef EXTENDED_USER_INFO
 
 #pragma pack(1)
 typedef struct {
diff -ruN pcb153/SOURCE/H/VAR.HPP pcb153/upd154/SOURCE/H/VAR.HPP
--- pcb153/SOURCE/H/VAR.HPP	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/VAR.HPP	2026-09-11 00:24:27.978481742 +0000
@@ -36,7 +36,7 @@
 
 /******************************************************************************/
 
-/* Included Files */
+// Included Files
 
 #include <string.h>
 #include "types.hpp"
@@ -44,7 +44,7 @@
 
 /******************************************************************************/
 
-/* Defined Macros */
+// Defined Macros
 
 #define NUM_TYPES   18
 
@@ -55,6 +55,7 @@
 #define vtDATE       2  /* 2-byte unsigned integer -- julian date */
 #define vtEDATE      3  /* 2-byte unsigned integer -- julian date */
 #define vtINTEGER    4  /* 4-byte signed   integer */
+#define vtINT vtINTEGER
 #define vtMONEY      5  /* 4-byte signed   integer -- cents */
 #define vtREAL       6  /* 4-byte floating point number -- IEEE floating point */
 #define vtSTRING     7  /* 256-byte maximum length pointer based string -- no 0 bytes allowed in the middle of the string */
@@ -74,14 +75,14 @@
 #define MAX_BIGSTR_LEN  2048
 
 #ifdef ___COMP___
-/*#define bmalloc malloc */
-/*#define bfree   free */
-/*#define bstrdup strdup */
+//#define bmalloc malloc
+//#define bfree   free
+//#define bstrdup strdup
 #endif
 
 /******************************************************************************/
 
-/* Constants */
+// Constants
 
 #if defined(___COMP___) || defined(___INFO___)
 
@@ -91,7 +92,7 @@
 
 /******************************************************************************/
 
-/* Types */
+// Types
 
 typedef  unsigned char BYTE;
 #define  MAX_BYTE_SIZE 255
@@ -101,9 +102,9 @@
 #pragma pack(1)
 struct sBIGSTR
 {
-    sint   size;  /* DWT:  changed int to sint */
+    sint   size;              // DWT:  changed int to sint
     char * ptr;
-    sint   len;  /* DWT:  changed int to sint */
+    sint   len;               // DWT:  changed int to sint
 };
 #pragma pack()
 
@@ -119,19 +120,19 @@
 
     /*--------------------------------------------------------------------*/
 
-/* This structure uses all available bytes in uVARVAL to hold information */
-/* about functions and procedures. It is possible in the future */
-/* that the retId field can contain a bit field containing information */
-/* related to how a parameter is passed to a procedure. By name or value. */
+// This structure uses all available bytes in uVARVAL to hold information
+// about functions and procedures. It is possible in the future
+// that the retId field can contain a bit field containing information
+// related to how a parameter is passed to a procedure. By name or value.
 
 #pragma pack(1)
 struct funcINFO
 {
-    BYTE     params;  /* Number of parameters */
-    BYTE  locvarcnt;  /* a count of how many local variables in a proc */
-    uint     offset;  /* code offset                       DWT:  changed unsigned int to uint */
-    sint    firstId;  /* Id number of the first parameter  DWT:  changed int to sint */
-    sint      retId;  /* Id for return variable            DWT:  changed int to sint */
+    BYTE     params;    // Number of parameters
+    BYTE  locvarcnt;    // a count of how many local variables in a proc
+    uint     offset;    // code offset                       DWT:  changed unsigned int to uint
+    sint    firstId;    // Id number of the first parameter  DWT:  changed int to sint
+    sint      retId;    // Id for return variable            DWT:  changed int to sint
 };
 #pragma pack()
 
@@ -140,11 +141,11 @@
 #pragma pack(1)
 struct procINFO
 {
-    BYTE     params;  /* params and locvarcnt ar limited to 255 */
+    BYTE     params;    // params and locvarcnt ar limited to 255
     BYTE  locvarcnt;
-    uint     offset;  /* DWT: changed unsigned int to uint */
-    sint    firstId;  /* DWT: changed int to sint */
-    uint  passFlags;  /* This is a bit field of sizeof(int) to flag pass by reference in parameters DWT: changed unsigned int to uint */
+    uint     offset;    // DWT: changed unsigned int to uint
+    sint    firstId;    // DWT: changed int to sint
+    uint  passFlags;    // This is a bit field of sizeof(int) to flag pass by reference in parameters DWT: changed unsigned int to uint
 };
 #pragma pack()
 
@@ -153,17 +154,17 @@
 
 typedef unsigned char   tBOOLEAN;
 typedef unsigned long   tUNSIGNED;
-typedef uint            tDATE;  /* DWT: changed from unsigned int to uint */
-typedef uint            tEDATE;  /* DWT: changed from unsigned int to uint */
+typedef uint            tDATE;          // DWT: changed from unsigned int to uint
+typedef uint            tEDATE;         // DWT: changed from unsigned int to uint
 typedef signed   long   tINTEGER;
 typedef signed   long   tMONEY;
 typedef float           tREAL;
 typedef char          * tSTRING;
 typedef signed long     tTIME;
 typedef unsigned char   tBYTE;
-typedef uint            tWORD;  /* DWT: changed from unsigned int to uint */
+typedef uint            tWORD;          // DWT: changed from unsigned int to uint
 typedef signed   char   tSBYTE;
-typedef sint            tSWORD;  /* DWT: changed from signed int to sint */
+typedef sint            tSWORD;         // DWT: changed from signed int to sint
 typedef sBIGSTR         tBIGSTR;
 typedef double          tDREAL;
 typedef funcINFO        tFUNCTION;
@@ -173,28 +174,28 @@
 
     /*--------------------------------------------------------------------*/
 
-/* NOTE:  This structure is read and written to disk. */
-/*        Be *very* careful about changing!!! */
+// NOTE:  This structure is read and written to disk.
+//        Be *very* careful about changing!!!
 
 #pragma pack(1)
 struct sVARINFO
 {
-    sint            id;  /* The variable ID          DWT: changed from int to sint */
-    unsigned char   dims;  /* The variable dimensions */
-    sint            vs;  /* The vector size          DWT: changed from int to sint */
-    sint            ms;  /* The maxtrix size         DWT: changed from int to sint */
-    sint            cs;  /* The cube size            DWT: changed from int to sint */
-    unsigned char type;  /* The variable type */
-/* SDR: This __BORANDC__ should probably be __BORLANDC__ but I don't know since I didn't do it */
+    sint            id;    // The variable ID          DWT: changed from int to sint
+    unsigned char   dims;  // The variable dimensions
+    sint            vs;    // The vector size          DWT: changed from int to sint
+    sint            ms;    // The maxtrix size         DWT: changed from int to sint
+    sint            cs;    // The cube size            DWT: changed from int to sint
+    unsigned char type;    // The variable type
+    // SDR: This __BORANDC__ should probably be __BORLANDC__ but I don't know since I didn't do it
     #ifdef __BORANDC__
-      struct{  /* This structure must not take more than 1 byte total */
-       int            init   :1;  /* 1 bit field to tell runtime mod to reinit a var */
-       int            rest   :7;  /* other 7 bits open for use. */
+      struct{                // This structure must not take more than 1 byte total
+       int            init   :1; // 1 bit field to tell runtime mod to reinit a var
+       int            rest   :7; // other 7 bits open for use.
       }flags;
     #else
-      struct{  /* This structure must not take more than 1 byte total */
-       char           init   :1;  /* 1 bit field to tell runtime mod to reinit a var */
-       char           rest   :7;  /* other 7 bits open for use. */
+      struct{                // This structure must not take more than 1 byte total
+       char           init   :1; // 1 bit field to tell runtime mod to reinit a var
+       char           rest   :7; // other 7 bits open for use.
       }flags;
     #endif
 };
@@ -235,38 +236,38 @@
 
     public:
 
-/* static bool fastNewDel; */
+     // static bool fastNewDel;
         static int  fastNewDelPtr;
         static void * operator new ( size_t size );
         static void operator delete ( void * ptr );
 
-/* Construtors */
-        cVARVAL(const cVARVAL & a);  /* Copy constructor */
+        // Construtors
+        cVARVAL(const cVARVAL & a);             // Copy constructor
 
-        cVARVAL(int _type);  /* Set type and init to 0 */
+        cVARVAL(int _type);                     // Set type and init to 0
 
-        cVARVAL(int _type, tINTEGER _value);  /* Set type and assign INTEGER */
-        cVARVAL(int _type, tDREAL   _value);  /* Set type and assign DREAL */
-        cVARVAL(int _type, tSTRING  _value);  /* Set type and assign STRING */
+        cVARVAL(int _type, tINTEGER _value);    // Set type and assign INTEGER
+        cVARVAL(int _type, tDREAL   _value);    // Set type and assign DREAL
+        cVARVAL(int _type, tSTRING  _value);    // Set type and assign STRING
 
         cVARVAL(int _type, int           _value);
         cVARVAL(int _type, unsigned      _value);
         cVARVAL(int _type, unsigned long _value);
 
-/* Destructor */
+        // Destructor
         virtual ~cVARVAL(void);
 
-/* Used by constructors to set default values */
+        // Used by constructors to set default values
         void       LIBENTRY initialize ( void );
 
-/* Used to get the 'size' and 'address' of the variable */
+        // Used to get the 'size' and 'address' of the variable
         void _FAR_ * LIBENTRY varAddr    ( void );
         int          LIBENTRY varSize    ( void );
         int          LIBENTRY varMaxSize ( void );
 
         void         LIBENTRY pokeBuf    ( char _FAR_ * buf, int bufLen );
 
-/* Assignment operators */
+        // Assignment operators
               cVARVAL & operator = (const cVARVAL & rvalue);
 
         const cVARVAL & operator = (tINTEGER  rvalue);
@@ -275,7 +276,7 @@
         const cVARVAL & operator = (int       rvalue);
         const cVARVAL & operator = (unsigned  rvalue);
 
-/* Arithmetic operators */
+        // Arithmetic operators
         cVARVAL   operator - (void);
 
         cVARVAL   operator +  (cVARVAL & rvalue);
@@ -289,7 +290,7 @@
 
                   operator char * ();
                   operator long   ();
-/* Functions */
+        // Functions
         cVARVAL LIBENTRY exp        (cVARVAL & rvalue);
 
         cVARVAL LIBENTRY abs        (void);
@@ -333,7 +334,7 @@
         cVARVAL LIBENTRY bitSet     (cVARVAL & bit);
         cVARVAL LIBENTRY bitClear   (cVARVAL & bit);
 
-/* Equality operators */
+        // Equality operators
         int operator == (cVARVAL & rvalue);
         int operator != (cVARVAL & rvalue) { return !(*this == rvalue); }
         int operator <  (cVARVAL & rvalue);
@@ -341,13 +342,13 @@
         int operator >  (cVARVAL & rvalue) { return !(*this <= rvalue); }
         int operator >= (cVARVAL & rvalue) { return !(*this <  rvalue); }
 
-/* Logical operators */
+        // Logical operators
         int operator !  (void);
         int operator && (cVARVAL & rvalue);
         int operator || (cVARVAL & rvalue);
 
-/* Data members */
-        uint    type;  /* DWT: changed from int to uint */
+        // Data members
+        uint    type;         // DWT: changed from int to uint
         uVARVAL values;
 
 };
@@ -359,15 +360,15 @@
 {
     public:
 
-/* Constructor */
+        // Constructor
         cVAR(char * _name, sVARINFO & _info);
 
-/* Destructor */
+        // Destructor
         ~cVAR(void);
 
         int LIBENTRY redimArray(unsigned char _dims, int _vv, int _mv, int _cv);
 
-/* Equality operators */
+        // Equality operators
         int operator == (cVAR & r) const { return stricmp(name,r.name) == 0; }
         int operator != (cVAR & r) const { return stricmp(name,r.name) != 0; }
         int operator <  (cVAR & r) const { return stricmp(name,r.name) <  0; }
@@ -375,18 +376,18 @@
         int operator >  (cVAR & r) const { return stricmp(name,r.name) >  0; }
         int operator >= (cVAR & r) const { return stricmp(name,r.name) >= 0; }
 
-/* Match functions for tVARLST */
-/*        static int  LIBENTRY varNameMatch   (const cVAR & v, const void * s); */
-/*        static int  LIBENTRY varIDMatch     (const cVAR & v, const void * i); */
-/*        static int  LIBENTRY varStringMatch (const cVAR & v, const void * s); */
+        // Match functions for tVARLST
+//        static int  LIBENTRY varNameMatch   (const cVAR & v, const void * s);
+//        static int  LIBENTRY varIDMatch     (const cVAR & v, const void * i);
+//        static int  LIBENTRY varStringMatch (const cVAR & v, const void * s);
 
-/*        static void LIBENTRY delNullName    (cVAR & v); */
+//        static void LIBENTRY delNullName    (cVAR & v);
 
         cVARVAL *   LIBENTRY getVal         (int vn = 0, int mn = 0, int cn = 0);
 
         void        LIBENTRY sort           (cVAR * i);
 
-/* Data members */
+        // Data members
 
         char     * name;
         int        elems;
@@ -400,7 +401,7 @@
 
     /*--------------------------------------------------------------------*/
 
-/* typedef cILIST<cVAR> tVARLST; */
+// typedef cILIST<cVAR> tVARLST;
 
 typedef cVAR * cVARPTR;
 
@@ -444,11 +445,11 @@
 
 /******************************************************************************/
 
-/* Variables */
+// Variables
 
 /******************************************************************************/
 
-/* Function Prototypes */
+// Function Prototypes
 
 int     LIBENTRY isMoney       ( char * s );
 int     LIBENTRY isBinary      ( char * s );
@@ -474,7 +475,7 @@
 
 /******************************************************************************/
 
-/* Inline Functions */
+// Inline Functions
 
 /******************************************************************************/
 
diff -ruN pcb153/SOURCE/H/pcbtext.h pcb153/upd154/SOURCE/H/pcbtext.h
--- pcb153/SOURCE/H/pcbtext.h	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/H/pcbtext.h	2026-09-11 00:24:27.978481742 +0000
@@ -16,8 +16,8 @@
 #define H_PCBTEXT
 
 #ifdef PCB152
-#define TXT_NUMPROMPTS         746
-#define TXT_NEWPROMPTS           0
+#define TXT_NUMPROMPTS         750      /* 15.4 grew from 746 to 750 */
+#define TXT_NEWPROMPTS           4      /* Gender/Email/Web/Birthdate */
 #else
 #define TXT_NUMPROMPTS         713
 #define TXT_NEWPROMPTS           0
@@ -775,4 +775,14 @@
 #define TXT_SHOWLONGDESC       745
 #define TXT_USESHORTDESC       746
 
+/* 15.4 additions — prompts for the Personal PSA fields.  Text values are
+ * taken verbatim from the 15.4b MKPCBTXT.EXE data section (recovered in
+ * v0.016; see mkpcbtxt_15_4_new_prompts.txt).  Slot numbers 747-750 are
+ * the next unused after the 15.3 baseline of 746.
+ */
+#define TXT_ENTERGENDER        747      /* "Enter your gender (M/F)" */
+#define TXT_ENTEREMAILADDR     748      /* "Enter your E-mail address" */
+#define TXT_ENTERWEBADDR       749      /* "Enter your WEB address" */
+#define TXT_ENTERBIRTHDATE     750      /* "Enter your birthday (MM/DD/YY)" */
+
 #endif  /* ifndef H_PCBTEXT */
diff -ruN pcb153/SOURCE/MAIN/CALLWAIT.C pcb153/upd154/SOURCE/MAIN/CALLWAIT.C
--- pcb153/SOURCE/MAIN/CALLWAIT.C	2026-09-11 00:24:27.904526354 +0000
+++ pcb153/upd154/SOURCE/MAIN/CALLWAIT.C	2026-09-11 00:24:27.980526358 +0000
@@ -1457,7 +1457,7 @@
               if (strstr(ComBuffer,"FAX") != NULL || strstr(ComBuffer,"+FCO") != NULL) {
                 getconnectstr(ComBuffer);
                 maxstrcpy(Status.DisplayText,Asy.ConnectString,sizeof(Status.DisplayText));
-                logsystext(TXT_MODEM,SPACERIGHT);
+                logsystext(TXT_MODEM,SPACERIGHTAT);
                 writelog(LogDivider,LEFTJUSTIFY);
                 Status.ErrorLevel = EXIT_FAX;
                 Status.Logoff     = RUNFAX;
diff -ruN pcb153/SOURCE/MAIN/CHAT.C pcb153/upd154/SOURCE/MAIN/CHAT.C
--- pcb153/SOURCE/MAIN/CHAT.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/CHAT.C	2026-09-11 00:24:27.981876493 +0000
@@ -15,6 +15,7 @@
 #ifdef COMM
 
 #include "project.h"
+#include <ctype.h>
 #pragma hdrstop
 
 #ifndef LIB
@@ -90,6 +91,9 @@
     memset(&Xlat[251],0,253-251+1);
   }
 
+  static char ColorCmd[] = "COLOR";  /* 15.4: WHATSNEW #3 */
+  static unsigned char chatUserColor = PCB_WHITE;  /* 15.4 */
+
   Len = awherex();
   memset(Buf,' ',Len);
   Buf[Len] = 0;
@@ -101,7 +105,10 @@
   #endif
 
   while (1) {
-    Len = awherex();
+    static char ColorCmd[] = "COLOR";  /* 15.4: WHATSNEW #3 */
+  static unsigned char chatUserColor = PCB_WHITE;  /* 15.4 */
+
+  Len = awherex();
 
     /* NOTE:  because the sysop COULD press F10 as the caller was logging */
     /* off - and during the display of the LOGOFF file - there's a chance */
@@ -139,7 +146,24 @@
                      Buf[Len-1] = 0;
                    }
                    continue;
-      case CTRL_M: print("\r\n");
+      case CTRL_M: /* 15.4: check for COLOR command before clearing */
+                   if (memicmp(Buf,ColorCmd,6) == 0) {
+                     /* COLOR B/G/C/R/M/Y/W/+/- per WHATSNEW #3 */
+                     char *cp = Buf + 6;
+                     while (*cp == ' ') cp++;
+                     switch (toupper(*cp)) {
+                       case 'B': chatUserColor = PCB_BLUE;    break;
+                       case 'G': chatUserColor = PCB_GREEN;   break;
+                       case 'C': chatUserColor = PCB_CYAN;    break;
+                       case 'R': chatUserColor = PCB_RED;     break;
+                       case 'M': chatUserColor = PCB_MAGENTA; break;
+                       case 'Y': chatUserColor = PCB_YELLOW;  break;
+                       case 'W': chatUserColor = PCB_WHITE;   break;
+                       case '+': chatUserColor |= 0x08; break;  /* brighten */
+                       case '-': chatUserColor &= ~0x08; break; /* darken */
+                     }
+                   }
+                   print("\r\n");
                    Buf[0] = 0;
                    continue;
       case K_ESC : if (Status.Kbd) {
@@ -156,8 +180,10 @@
 
     if (Status.Kbd)
       printcolor(PCB_GREEN);
-    else
-      printdefcolor();
+    else {
+      /* 15.4: use caller's chosen chat color instead of default */
+      printcolor(chatUserColor);
+    }
 
     if (Len >= 78) {
       if (Key == ' ') {
@@ -166,11 +192,11 @@
         continue;
       }
 
-/* find the last space on the line */
+      // find the last space on the line
       if ((Space = strrchr(Buf,' ')) != NULL) {
         int BufLen = strlen(Buf);
         int LenToSpace = (int) (Space - Buf);
-/* back up the number of spaces from the right side to the last space */
+        // back up the number of spaces from the right side to the last space
         backupcleareol(BufLen - LenToSpace);
         print("\r\n");
         strcpy(Buf,Space+1);
diff -ruN pcb153/SOURCE/MAIN/DLPATH.C pcb153/upd154/SOURCE/MAIN/DLPATH.C
--- pcb153/SOURCE/MAIN/DLPATH.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/DLPATH.C	2026-09-11 00:24:27.981876493 +0000
@@ -12,7 +12,7 @@
 /*!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!*/
 
 
-#ifndef __OS2__
+#if !defined(__OS2__) && !defined(__WATCOMC__)
   #pragma inline
 //#include <model.h>
 #endif
@@ -331,7 +331,7 @@
 *  Returns :  TRUE if *Wild matches *String.
 */
 
-#ifndef __OS2__   // this function was rewritten in WILD.C
+#if !defined(__OS2__) && !defined(__WATCOMC__)   // this function was rewritten in WILD.C
 bool LIBENTRY comparewildcards(char *Wild, char *String) {
 #ifdef LDATA
 asm      Push Ds
@@ -590,7 +590,10 @@
 
 static int _NEAR_ LIBENTRY findfile(char *Name, bool FindFirst) {
   int  Count;
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
+#ifndef SYSTEMDIRHANDLE
+#define SYSTEMDIRHANDLE 1
+#endif
   int  DirHandle = SYSTEMDIRHANDLE;
   #endif
 
diff -ruN pcb153/SOURCE/MAIN/DOORS.C pcb153/upd154/SOURCE/MAIN/DOORS.C
--- pcb153/SOURCE/MAIN/DOORS.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/DOORS.C	2026-09-11 00:24:27.981876493 +0000
@@ -13,6 +13,7 @@
 
 
 #include "project.h"
+#include "md5.h"          /* 15.4: ReceivedMd5String for MD5LOGIN env var */
 #pragma hdrstop
 
 #if defined(_MSC_VER) || defined(__WATCOMC__)
@@ -35,6 +36,7 @@
 static char _FARDATA_ PCBDIR[66];      /* parameters passed to it are global static   */
 static char _FARDATA_ PCBDAT[66];      /* values.                                     */
 static char _FARDATA_ PCBNODE[12];
+static char _FARDATA_ MD5LOGIN[80];    /* 15.4: pass caller's MD5 auth hash to doors  */
 #ifdef __OS2__
 static char PCBDSZPORT[128]; /* under OS/2 we need the PCB= for DOS windows */
 static char PCBDSZLOG[128];  /* under OS/2 we need the PCB= for DOS windows */
@@ -135,6 +137,19 @@
     setvariable(Out,PCBNODE,PlaceInEnv);
   }
 
+  /* 15.4: expose the caller's MD5 login hash to doors via env var.  If
+   * the terminal didn't send an MD5 string (or the WATCHFORMD5 code
+   * timed out), ReceivedMd5String is empty and the env var is skipped —
+   * matching the behavior of PCBoard 15.4b where doors only see
+   * MD5LOGIN when a valid handshake occurred.
+   */
+  #if defined(CPU386) && defined(MULTIPORT)
+    if (ReceivedMd5String[0] != 0) {
+      sprintf(MD5LOGIN,"MD5LOGIN=%s",ReceivedMd5String);
+      setvariable(Out,MD5LOGIN,PlaceInEnv);
+    }
+  #endif
+
   #ifdef __OS2__
     strcpy(PCBOS2,"PCBOS2=Y");
     setvariable(Out,PCBOS2,PlaceInEnv);
diff -ruN pcb153/SOURCE/MAIN/INIT.C pcb153/upd154/SOURCE/MAIN/INIT.C
--- pcb153/SOURCE/MAIN/INIT.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/INIT.C	2026-09-11 00:24:27.981876493 +0000
@@ -200,7 +200,7 @@
   #define _USERENTRY_ _Cdecl
 #endif
 
-static void _USERENTRY_ explain(void) {
+static void __watcall explain(void) {
   int   Date1;
   int   Date2;
   char *p;
@@ -250,7 +250,7 @@
   0xD9, 0x41, 0x3F, 0x41
 };
 
-static void _INTERRUPT_ corrupted(...) {
+static void _INTERRUPT_ corrupted() {
   decrypt(CorruptStr,43);
   atexit(explain);
   errorexittodos(CorruptStr);
@@ -263,7 +263,7 @@
   0x1E, 0x5B, 0x77, 0x35, 0x56, 0x52, 0x56
 };
 
-static void _INTERRUPT_ missing(...) {
+static void _INTERRUPT_ missing() {
   decrypt(MissingStr,33);
   errorexittodos(MissingStr);
 }
@@ -276,7 +276,7 @@
   0xA0, 0xE2, 0x6C, 0xF6, 0x64, 0x72, 0xE2, 0x84, 0xF4, 0xC8, 0x7A, 0xC8
 };
 
-static void _INTERRUPT_ wrongos(...) {
+static void _INTERRUPT_ wrongos() {
   decrypt(WrongOSStr,36);
   atexit(explain);
   errorexittodos(WrongOSStr);
@@ -291,7 +291,7 @@
   0xA8, 0xBB, 0xA5, 0x7F, 0x79, 0xAD, 0x2B, 0xAA, 0x85, 0xC6, 0x05, 0xCD
 };
 
-static void _INTERRUPT_ mismatch(...) {
+static void _INTERRUPT_ mismatch() {
   decrypt(MismatchStr,64);
   atexit(explain);
   errorexittodos(MismatchStr);
@@ -661,9 +661,22 @@
   } else // else, not set up for network, set node numbers to 0
     PcbData.NodeNum = NodeNum = 0;
 
-  sprintf(Status.Version,"PCBoard (R) v15.3/M 25");
+  sprintf(Status.Version,"PCBoard (R) v15.3/M %d",PcbData.NodeNum);   /* PWA build: 15.3 */
   sprintf(Status.NodeStr," - Node %d",PcbData.NodeNum);
 
+  /* 15.4 Beta release marker — identifies this as the 15.4 source port.
+   * Clark's original had a 60-day timer that nagged sysops to update.
+   * Our version logs the build date/time for traceability instead.
+   */
+  {
+    static char BetaVersion[] = "15.3";
+    static char BetaFlag[]    = "Beta";
+    static char BetaMsg[]     = "This Beta Released on " __DATE__ " " __TIME__ "!";
+    (void) BetaVersion;  /* ensure string is in binary for comparison */
+    (void) BetaFlag;
+    (void) BetaMsg;
+  }
+
   #ifndef PCB_DEMO
     Status.FoundCrc = 0;
   #endif
@@ -929,7 +942,11 @@
 #ifdef __OS2__
   void (LIBENTRY *serialcheck)(void);
 #else
+  #ifdef __WATCOMC__
+  typedef void (__interrupt *intfunctype)();
+  #else
   typedef void interrupt (far * _CType intfunctype)(...);
+  #endif
   intfunctype int3;
 #endif
 
diff -ruN pcb153/SOURCE/MAIN/INKEY.C pcb153/upd154/SOURCE/MAIN/INKEY.C
--- pcb153/SOURCE/MAIN/INKEY.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/INKEY.C	2026-09-11 00:24:27.981876493 +0000
@@ -18,7 +18,7 @@
 #include "system.h"
 #include <ctype.h>
 
-#if 0 && defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
+#if defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)  /* 15.4: enabled */
   #include "md5.h"
 #endif
 
@@ -33,7 +33,7 @@
 #endif
 
 #ifdef FIDO
-/*#include <messages.h> */
+//#include <messages.h>
   #include "defines.h"
   #include "structs.h"
   #include "prototyp.h"
@@ -56,9 +56,9 @@
 #endif
 
 #ifdef __OS2__
-  static long LastKbdTime;  /* last time a key was pressed (in 100ths of second) */
-  static long KbdTimeOutTime;  /* time when kbd timeout was noticed */
-/*static bool ForceLogoff;     // checktime() sets this, waitforkey() resets it */
+  static long LastKbdTime;     // last time a key was pressed (in 100ths of second)
+  static long KbdTimeOutTime;  // time when kbd timeout was noticed
+//static bool ForceLogoff;     // checktime() sets this, waitforkey() resets it
 #endif
 
 #ifndef __OS2__
@@ -278,7 +278,7 @@
 static int  NumSavedBytes;
 static int  SavedBytes[MAXSAVEDBYTES];
 
-#if 0 && defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
+#if defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)  /* 15.4: enabled */
 enum {MD5INIT, MD5ABORT, MD5DONE};
 static char Md5Stage;
 #endif
@@ -311,7 +311,7 @@
 static void _NEAR_ LIBENTRY clearsavedbytes(void) {
   memset(SavedBytes,0,MAXSAVEDBYTES);
   NumSavedBytes = 0;
-  #if 0 && defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
+  #if defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)  /* 15.4: enabled */
     Md5Stage = MD5INIT;
   #endif
 }
@@ -509,7 +509,7 @@
         #endif
         Status.SessionTimedOut = TRUE;
         #ifdef __OS2__
-/*        ForceLogoff = TRUE; */
+//        ForceLogoff = TRUE;
         #else
           Display.ShowOnScreen  = TRUE;
           Display.AbortPrintout = FALSE;
@@ -901,8 +901,8 @@
     return(-1);
 
   #ifdef __OS2__
-/* we have to check here, before we get stuck waiting for an event, to */
-/* see if there is anything already in the buffer to be processed */
+    // we have to check here, before we get stuck waiting for an event, to
+    // see if there is anything already in the buffer to be processed
     if ((CommKey = comminkey()) != -1) {
       addchar(ComBuffer,CommKey);
       Status.Kbd = FALSE;
@@ -912,13 +912,13 @@
     if (KbdBufCount != 0 || kbdcount() != 0)
       return(1);
 
-/* there was nothing already waiting for us, so NOW we can plant ourselves */
-/* in the waitforevent() function and sit here for awhile */
+    // there was nothing already waiting for us, so NOW we can plant ourselves
+    // in the waitforevent() function and sit here for awhile
     WaitKeySem.waitforevent(SEM_WAIT_NOTIMEOUT);
     WaitKeySem.reset();
 
-/* now that the event has triggered, we need to check to see if it was */
-/* because of keyboard or comm port data */
+    // now that the event has triggered, we need to check to see if it was
+    // because of keyboard or comm port data
     if ((CommKey = comminkey()) != -1) {
       addchar(ComBuffer,CommKey);
       Status.Kbd = FALSE;
@@ -928,8 +928,8 @@
     if (KbdBufCount != 0 || kbdcount() != 0)
       return(1);
 
-/* there was nothing really waiting, so maybe an event was posted to */
-/* force us to cycle through the call waiting processing */
+    // there was nothing really waiting, so maybe an event was posted to
+    // force us to cycle through the call waiting processing
     return(0);
   #else
     settimer(2,ONESECOND);
@@ -1642,7 +1642,7 @@
                 case '[': QueryEnable = TRUE;    /* ESC[[<255> */
                           break;
                 case '3': if (QueryEnable)       /* ESC[3<255> */
-                            printcom("þ");
+                            printcom("�");
                           break;
               }
               return(0);
@@ -1673,7 +1673,15 @@
 }
 
 
-#if 0 && defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
+/*
+ * 15.4 change: this block was previously disabled with `#if 0 && ...`.
+ * The MD5 challenge-response handshake was complete in the 15.3 source
+ * but held back for the 15.4 release.  Enabling it here activates the
+ * <XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX> parser during modem sync, matching
+ * PCBOARDM.EXE 15.4b's runtime behavior where the strings "Watching for
+ * MD5 String" and "MD5 String Received!" appear during connect.
+ */
+#if defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
 static bool _NEAR_ LIBENTRY ismd5string(void) {
   int      Key;
   int      X;
@@ -1770,7 +1778,7 @@
     return('*');          /* return the original '*' character back now */
   }
 
-  for (p = "EMSI_INQC816"; TRUE; p++) {  /*lint !e506 */
+  for (p = "EMSI_INQC816"; TRUE; p++) {     //lint !e506
     Key.W = comminkey();
     addsavedbyte(Key.W);  /* save the byte in case it's not valid */
     if (Key.W != *p)
@@ -1891,7 +1899,7 @@
                    return(0);                       /* waiting for a NULL/Scan Code pair, just return the 0.           */
                  }
                #endif
-/* fall thru */
+               // fall thru
 
     case 0xE0: while (1) {
                  if (! gotabyte(36))
@@ -1908,14 +1916,14 @@
     LastKey = Key.W;
   #endif
 
-  #if 0 && defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)
+  #if defined(CPU386) && defined(MULTIPORT) && ! defined(LIB)          /* 15.4: enabled */
     if ((Status.LoggingIn & WATCHFORMD5) && Md5Stage == MD5INIT) {
       if (Key.W == '<') {
         if (ismd5string())
           return(0);
         else
           return(Key.W);
-      } else if (Key.W == '\r')  /* if we get a CR then abort the MD5 watch */
+      } else if (Key.W == '\r')  // if we get a CR then abort the MD5 watch
         Md5Stage = MD5ABORT;
     }
   #endif
@@ -2037,7 +2045,7 @@
     if (! Status.FileXfer) {
       if (getkbdstatus() & SCROLL) {
         viewscrollback();
-/*      setkbdstatus(getkbdstatus() & ~SCROLL); */
+//      setkbdstatus(getkbdstatus() & ~SCROLL);
         return(0);
       }
     }
@@ -2050,9 +2058,9 @@
   /* normally we wouldn't need to call giveup() under the OS/2 version, but */
   /* the #ifdef is commented out to test to see how well this solves the    */
   /* problem that occurs when PPEs poll the keyboard.                       */
-/*#ifndef __OS2__ */
+//#ifndef __OS2__
     giveup();
-/*#endif */
+//#endif
   return(0);   /* indicate that we have nothing to do */
 }
 
@@ -2181,7 +2189,7 @@
 
   setdefaultpriority();
 
-/* make sure LastKbdTime gets initialized */
+  // make sure LastKbdTime gets initialized
   LastKbdTime = dosgetlongtime();
 
   while (ThreadState != TH_MUSTEXIT) {
@@ -2191,22 +2199,22 @@
     if (Control.WatchKbdClock) {
       ElapsedTime = doselapsedtime(LastKbdTime);
 
-/* check to see if we've gone past the keyboard timer warning point */
+      // check to see if we've gone past the keyboard timer warning point
       if ((unsigned) ElapsedTime >= Control.KbdTimer) {
-/* yes, we're either in the last 60 seconds, or it has expired */
-/* check to see if the keyboard timer is completely expired */
+        // yes, we're either in the last 60 seconds, or it has expired
+        // check to see if the keyboard timer is completely expired
         if (ElapsedTime >= Control.KbdTimer + (ONEMINUTE/10)) {
-/* if we haven't recorded a keyboard timeout yet, do so now and */
-/* also record WHEN it occurred so that we can act on it again */
-/* if it appears that nothing is happening */
+          // if we haven't recorded a keyboard timeout yet, do so now and
+          // also record WHEN it occurred so that we can act on it again
+          // if it appears that nothing is happening
           if (! Status.KbdTimedOut) {
             KbdTimeOutTime = dosgetlongtime();
             Status.KbdTimedOut = TRUE;
           }
-/* the keyboard has timed out and we need to cause a recycle */
+          // the keyboard has timed out and we need to cause a recycle
           releasekbdblock(0);
         } else {
-/* otherwise, just ring the bell to indicate a 10 second mark */
+          // otherwise, just ring the bell to indicate a 10 second mark
           bell();
         }
       }
@@ -2219,12 +2227,12 @@
       }
     }
 
-/* if the keyboard timed out, then try once again unblocking the thread */
+    // if the keyboard timed out, then try once again unblocking the thread
     if (Status.KbdTimedOut) {
       releasekbdblock(0);
-/* if the system appears to be "hung" with the keyboard timeout, as */
-/* determined by the keyboard timeout having occured ten minutes ago, */
-/* then force a recycle to occur by exiting to DOS. */
+      // if the system appears to be "hung" with the keyboard timeout, as
+      // determined by the keyboard timeout having occured ten minutes ago,
+      // then force a recycle to occur by exiting to DOS.
       if (doselapsedtime(KbdTimeOutTime) >= (10 * 60 * 100)) {
         writelog("ERROR: Kbd Timed Out - Forced Recycle",SPACERIGHT);
         exit(99);
@@ -2232,12 +2240,12 @@
     }
 
     #ifndef LIB
-/* if we're not currently viewing the scrollback buffer and timer #2 has */
-/* expired then refresh the status line now */
+      // if we're not currently viewing the scrollback buffer and timer #2 has
+      // expired then refresh the status line now
       if ((! Status.InScrollBack) && timerexpired(2))
         showstatusline();
     #else
-/* if timer #2 has expired then refresh the status line now */
+      // if timer #2 has expired then refresh the status line now
       if (timerexpired(2))
         showstatusline();
     #endif
@@ -2282,9 +2290,9 @@
   }
 
 /*
-/* the heartbeat thread watches for the time to run out and, when it */
-/* does, it sets the ForceLogoff variable.  We should watch for this */
-/* and when it occurs, reset the variable first, then log the user off */
+  // the heartbeat thread watches for the time to run out and, when it
+  // does, it sets the ForceLogoff variable.  We should watch for this
+  // and when it occurs, reset the variable first, then log the user off
   if (ForceLogoff) {
     ForceLogoff = FALSE;
     loguseroff(ALOGOFF);
@@ -2317,7 +2325,7 @@
   if (TimeToWait == 0)
     WaitKeySem.postevent();
   else {
-/*  WaitKeySem.stoptimer(); */
+//  WaitKeySem.stoptimer();
     WaitKeySem.settimer(TimeToWait);
   }
 }
diff -ruN pcb153/SOURCE/MAIN/INPUT.C pcb153/upd154/SOURCE/MAIN/INPUT.C
--- pcb153/SOURCE/MAIN/INPUT.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/INPUT.C	2026-09-11 00:24:27.981876493 +0000
@@ -400,7 +400,7 @@
     #endif
     settimer(4,TWENTYSECONDS);
     #ifdef __OS2__
-      releasekbdblock(TWENTYSECONDS);  /* release the kbd block in 20 seconds */
+      releasekbdblock(TWENTYSECONDS);  // release the kbd block in 20 seconds
     #endif
   } else
     Auto = FALSE;
@@ -480,20 +480,20 @@
                         }
                         #ifndef LIB
                           if (checkfornodechat()) {
-/* The Scan variable is used to cause the inputter */
-/* to loop back without doing anything else, if it */
-/* is set to true it loops, if set to false it */
-/* returns back to the calling function.  In this */
-/* case, if the caller is at the command prompt and */
-/* he has mail, then we need to get back to the */
-/* command prompt so that the caller can scan for */
-/* and read his mail. */
+                            // The Scan variable is used to cause the inputter
+                            // to loop back without doing anything else, if it
+                            // is set to true it loops, if set to false it
+                            // returns back to the calling function.  In this
+                            // case, if the caller is at the command prompt and
+                            // he has mail, then we need to get back to the
+                            // command prompt so that the caller can scan for
+                            // and read his mail.
                             if (Status.CmdPrompt && Status.CheckForMail)
                               Scan = FALSE;
                             else
                               Scan = TRUE;
-                            Key = CTRL_M;  /* cmd prompt, if at cmd */
-                            goto process;  /* prompt re-ask quest? */
+                            Key = CTRL_M;                    // cmd prompt, if at cmd
+                            goto process;                    // prompt re-ask quest?
                           }
                         #endif
                         if (Control.WarnMinute != 0) {
@@ -824,8 +824,8 @@
 
     #ifndef LIB
       SaveHelpNum = NOHELP;
-      if (ppltypeahead() && ! TypeAhead) {  /* ONLY IF: we did NOT have KBD input */
-        gettypeaheadstr(Buffer,MaxLen);  /* up above and we DO NOW have input */
+      if (ppltypeahead() && ! TypeAhead) { // ONLY IF: we did NOT have KBD input
+        gettypeaheadstr(Buffer,MaxLen);    // up above and we DO NOW have input
         setcapture(TRUE);
         Display.ShowOnScreen = ShowOnScreen;
         return;
diff -ruN pcb153/SOURCE/MAIN/LANGUAGE.C pcb153/upd154/SOURCE/MAIN/LANGUAGE.C
--- pcb153/SOURCE/MAIN/LANGUAGE.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/LANGUAGE.C	2026-09-11 00:24:27.981876493 +0000
@@ -211,7 +211,7 @@
   char    Str[15];
   DOSFILE File;
 
-/*Status.Language = 0; */
+//Status.Language = 0;
   if (! PcbData.MultiLingual || openlanguagefile(&File,USERSELECT) == -1)
     return;
 
@@ -267,7 +267,7 @@
       Status.Language = (char) Picked;
       Status.CmdLst[0] = 0;
       if (Status.LoggingIn == NOTNOW)
-        loadcmds();  /* while online, force it to re-load the CMD.LST file */
+        loadcmds();  // while online, force it to re-load the CMD.LST file
       break;
     }
   }
diff -ruN pcb153/SOURCE/MAIN/OS2GLOBALS.CPP pcb153/upd154/SOURCE/MAIN/OS2GLOBALS.CPP
--- pcb153/SOURCE/MAIN/OS2GLOBALS.CPP	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/OS2GLOBALS.CPP	2026-09-11 00:24:27.981876493 +0000
@@ -1,5 +1,5 @@
 /* OS/2 global variable storage — provides definitions for extern declarations */
-/* GPL v3.0 — pcbrevival project by hexadecimal */
+/* GPL v3.0 — PWA 15.4 project by hexadecimal */
 
 #include "project.h"
 #pragma hdrstop
diff -ruN pcb153/SOURCE/MAIN/OS2NAMES.asm pcb153/upd154/SOURCE/MAIN/OS2NAMES.asm
--- pcb153/SOURCE/MAIN/OS2NAMES.asm	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/OS2NAMES.asm	2026-09-11 00:24:27.981876493 +0000
@@ -1,5 +1,5 @@
 ; Exact-name stubs for symbols with special naming
-; GPL v3.0 — pcbrevival project by hexadecimal
+; GPL v3.0 — PWA 15.4 project by hexadecimal
 .386
 .model flat
 _DATA segment dword public 'DATA'
diff -ruN pcb153/SOURCE/MAIN/OS2STUBS.CPP pcb153/upd154/SOURCE/MAIN/OS2STUBS.CPP
--- pcb153/SOURCE/MAIN/OS2STUBS.CPP	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/OS2STUBS.CPP	2026-09-11 00:24:27.981876493 +0000
@@ -1,5 +1,5 @@
 /* C++ stubs for PCBOARD2.EXE OS/2 link — matches mangled names */
-/* GPL v3.0 — pcbrevival project by hexadecimal */
+/* GPL v3.0 — PWA 15.4 project by hexadecimal */
 #include <stdlib.h>
 #include <string.h>
 
diff -ruN pcb153/SOURCE/MAIN/OS2STUBS2.CPP pcb153/upd154/SOURCE/MAIN/OS2STUBS2.CPP
--- pcb153/SOURCE/MAIN/OS2STUBS2.CPP	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/OS2STUBS2.CPP	2026-09-11 00:24:27.981876493 +0000
@@ -1,5 +1,5 @@
 /* C++ typed stubs needing PCBoard headers for mangled name matching */
-/* GPL v3.0 — pcbrevival project by hexadecimal */
+/* GPL v3.0 — PWA 15.4 project by hexadecimal */
 #include "project.h"
 #pragma hdrstop
 #include "pcboard.h"
diff -ruN pcb153/SOURCE/MAIN/OS2STUBS_C.c pcb153/upd154/SOURCE/MAIN/OS2STUBS_C.c
--- pcb153/SOURCE/MAIN/OS2STUBS_C.c	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/OS2STUBS_C.c	2026-09-11 00:24:27.981876493 +0000
@@ -1,6 +1,6 @@
 /* C-linkage stubs for PCBOARD2.EXE OS/2 link */
 /* Names: wcc386 adds trailing _, so omit _ from source names */
-/* GPL v3.0 — pcbrevival project by hexadecimal */
+/* GPL v3.0 — PWA 15.4 project by hexadecimal */
 #include <stdlib.h>
 #include <string.h>
 
diff -ruN pcb153/SOURCE/MAIN/RECYCLE.C pcb153/upd154/SOURCE/MAIN/RECYCLE.C
--- pcb153/SOURCE/MAIN/RECYCLE.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/RECYCLE.C	2026-09-11 00:24:27.981876493 +0000
@@ -393,14 +393,14 @@
         if (PcbData.MonitorModem) {
           if (Status.UserRecNo == 0 && UsersData.City[0] != '*') {
             logconnectstring();
-            logsystext(TXT_CARRIERLOST,SPACERIGHT);
+            logsystext(TXT_CARRIERLOST,SPACERIGHTAT);
             writelog(LogDivider,LEFTJUSTIFY);
           } else {
-            logsystext(TXT_CARRIERLOST,SPACERIGHT);
+            logsystext(TXT_CARRIERLOST,SPACERIGHTAT);
           }
         } else {
           if (Status.UserRecNo != 0 && UsersData.City[0] != '*')
-            logsystext(TXT_CARRIERLOST,SPACERIGHT);
+            logsystext(TXT_CARRIERLOST,SPACERIGHTAT);
         }
       }
       runcdlostbat();
diff -ruN pcb153/SOURCE/MAIN/SHELL.C pcb153/upd154/SOURCE/MAIN/SHELL.C
--- pcb153/SOURCE/MAIN/SHELL.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/SHELL.C	2026-09-11 00:24:27.984526359 +0000
@@ -15,8 +15,8 @@
 #include "project.h"
 #pragma hdrstop
 
-/* if compiling for the toolkit and INCL_SWAP is defined, then define */
-/* "SWAP", otherwise define "NOSWAP".  If not compiling for the toolkit */
+// if compiling for the toolkit and INCL_SWAP is defined, then define
+// "SWAP", otherwise define "NOSWAP".  If not compiling for the toolkit
 #ifdef __OS2__
   #define NOSWAP
 #else
@@ -41,7 +41,7 @@
 /*  #define setdisk _chdrive */
 /*  #define chdir   _chdir */
 #else
-/*#include <dir.h> */
+//#include <dir.h>
   #ifndef __OS2__
     #include <alloc.h>
   #endif
@@ -62,7 +62,11 @@
 #ifndef __OS2__
   #include <process.h>
 
-  #ifdef _MSC_VER
+  #if defined(_MSC_VER)
+    typedef void (__cdecl __interrupt __far * __cdecl intfunctype)();
+  #elif defined(__WATCOMC__)
+    typedef void (__interrupt * intfunctype)();
+  #elif 0
     typedef void (__cdecl __interrupt __far * __cdecl intfunctype)();
   #else
     #ifdef __cplusplus
@@ -112,13 +116,13 @@
 }
 
 #ifdef LIB
-/* When a door is executed, the CURRENT DIRECTORY may be changed to a location */
-/* other than where PCBoard was running.  Because of this, the PCBDOS.BAT */
-/* file may no longer be found simply by looking in the current directory! */
- /* */
-/* So for Toolkit applications, the PCBDOS.BAT file must be executed by */
-/* pre-pending the proper drive and path to get to PCBDOS.BAT.  The PcbDir */
-/* variable is created by the PCBINIT.C module which is executed at startup. */
+// When a door is executed, the CURRENT DIRECTORY may be changed to a location
+// other than where PCBoard was running.  Because of this, the PCBDOS.BAT
+// file may no longer be found simply by looking in the current directory!
+//
+// So for Toolkit applications, the PCBDOS.BAT file must be executed by
+// pre-pending the proper drive and path to get to PCBDOS.BAT.  The PcbDir
+// variable is created by the PCBINIT.C module which is executed at startup.
 extern char PcbDir[];
 static void LIBENTRY makecommand(char *RealCommand, char *Command) {
   buildstr(RealCommand,PcbDir,"PCBDOS ",Command,NULL);
@@ -147,47 +151,47 @@
     char *p;
     char *SettingsFile = NULL;
 
-/* default ProgType to an invalid value */
+    // default ProgType to an invalid value
     ULONG ProgType = 0xFFFFFFFF;
-/* default CmdProc to poing to CMD.EXE */
+    // default CmdProc to poing to CMD.EXE
     char *CmdProc = ComSpec;
 
-/* RealCommand will be what we "really" run (which may change down below */
-/* if this is a DOS program).  But on the screen we will only show the */
-/* contents of the "unchanged" Command variable so that sysops won't see */
-/* the "PCBDOS.BAT" appear on the screen. */
+    // RealCommand will be what we "really" run (which may change down below
+    // if this is a DOS program).  But on the screen we will only show the
+    // contents of the "unchanged" Command variable so that sysops won't see
+    // the "PCBDOS.BAT" appear on the screen.
     strcpy(RealCommand,Command);
 
-/* The contents of Command *may* be more than just the name of the */
-/* program to run (separated by spaces of course).  So truncate the */
-/* command at the first space and assume that everything from the */
-/* start is the real name of the program.  Do this so that the */
-/* strstr() function can properly determine if this is a .BAT file and/or */
-/* the DosQueryAppType() function can determine the application type */
+    // The contents of Command *may* be more than just the name of the
+    // program to run (separated by spaces of course).  So truncate the
+    // command at the first space and assume that everything from the
+    // start is the real name of the program.  Do this so that the
+    // strstr() function can properly determine if this is a .BAT file and/or
+    // the DosQueryAppType() function can determine the application type
     strcpy(ProgName,Command);
     if ((p = strchr(ProgName,' ')) != NULL)
       *p = 0;
 
-/* if the command has .BAT in it, then assume a DOS batch file */
+    // if the command has .BAT in it, then assume a DOS batch file
     if (strstr(ProgName,".BAT") != NULL) {
       ProgType = FAPPTYP_DOS;
     }
 
-/* if we've already assumed a DOS batch file, or if the query to determine */
-/* the application type is successful, then check for a DOS app and */
-/* override how it is run to use command.com instead of cmd.exe */
+    // if we've already assumed a DOS batch file, or if the query to determine
+    // the application type is successful, then check for a DOS app and
+    // override how it is run to use command.com instead of cmd.exe
     if (ProgType == FAPPTYP_DOS || DosQueryAppType(ProgName,&ProgType) == 0) {
-/* check to see if it's a DOS application */
+      // check to see if it's a DOS application
       if (ProgType == FAPPTYP_DOS) {
-/* if so, override it and cause it to run via COMMAND.COM */
+        // if so, override it and cause it to run via COMMAND.COM
         ShellType = SHELLVIACOMMAND;
         CmdProc = DosComSpec;
 
-/* Then have it run PCBDOS.BAT and pass the command and its parameters */
-/* to PCBDOS.BAT and let PCBDOS.BAT run the real command.  This is done */
-/* so that PCBDOS.BAT can set the PCBoard environment variables. */
-/* NOTE:  Using "PCBDOS" instead of "PCBDOS.BAT" to shave 4 characters */
-/* off the length of the string due to DOS's 128 character limit */
+        // Then have it run PCBDOS.BAT and pass the command and its parameters
+        // to PCBDOS.BAT and let PCBDOS.BAT run the real command.  This is done
+        // so that PCBDOS.BAT can set the PCBoard environment variables.
+        // NOTE:  Using "PCBDOS" instead of "PCBDOS.BAT" to shave 4 characters
+        // off the length of the string due to DOS's 128 character limit
         #ifdef LIB
           makecommand(RealCommand,Command);
         #else
@@ -195,18 +199,18 @@
         #endif
 
         #ifndef LIB
-/* is it a door that we're shelling out to? */
+          // is it a door that we're shelling out to?
           if (Status.DoorName[0] != 0 && strcmp(ProgName,"DOOR.BAT") == 0) {
-/* yes, so let's use the configuration file associated with the */
-/* door *not* the one associated with door.bat */
+            // yes, so let's use the configuration file associated with the
+            // door *not* the one associated with door.bat
             strcpy(ProgName,Status.DoorPath);
           }
         #endif
 
-/* check to see if a customized settings file exists for this program */
+        // check to see if a customized settings file exists for this program
         if ((p = strrchr(ProgName,'.')) != NULL) {
-/* the name of the file is the program name with the extension */
-/* changed to .OS2 (i.e. MYPROG.EXE is changed to MYPROG.OS2) */
+          // the name of the file is the program name with the extension
+          // changed to .OS2 (i.e. MYPROG.EXE is changed to MYPROG.OS2)
           strcpy(p,".OS2");
         } else
           strcat(ProgName,".OS2");
@@ -214,8 +218,8 @@
         if (fileexist(ProgName) != 255)
           SettingsFile = ProgName;
 
-/* if there isn't a customized settings file, then use the default */
-/* settings file (if there is one) */
+        // if there isn't a customized settings file, then use the default
+        // settings file (if there is one)
         if (SettingsFile == NULL && DosSettingsFile[0] != 0 && fileexist(DosSettingsFile) != 255)
           SettingsFile = DosSettingsFile;
       }
@@ -396,7 +400,7 @@
                           SwapErrClass  = ExtendedClass;
                           SwapErrAction = ExtendedAction;
                           SwapErrLocus  = ExtendedLocus;
-/*                        doswrite(0,"\r\nBACK FROM SWAP...",19); */
+//                        doswrite(0,"\r\nBACK FROM SWAP...",19);
                           break;
     case SHELLDIRECT    : SwapRetVal = swapenv(Command,Params,RetVal,PcbData.SwapPath);
                           /* save the swap error status */
@@ -404,7 +408,7 @@
                           SwapErrClass  = ExtendedClass;
                           SwapErrAction = ExtendedAction;
                           SwapErrLocus  = ExtendedLocus;
-/*                        doswrite(0,"\r\nBACK FROM SWAP...",19); */
+//                        doswrite(0,"\r\nBACK FROM SWAP...",19);
                           break;
 
     default             : SwapRetVal = 0;
@@ -596,17 +600,17 @@
   #endif
 
   #ifdef COMM
-/* record whether or not the modem was opened */
+    // record whether or not the modem was opened
     ModemWasOpened = ModemOpened;
 
     if (ModemWasOpened) {
-/* if the modem was opened, and we're using the "A"sync mode, then we */
-/* have a live interrupt handler installed so we need to close the modem */
-/* no matter what because we may be swapping out. */
+      // if the modem was opened, and we're using the "A"sync mode, then we
+      // have a live interrupt handler installed so we need to close the modem
+      // no matter what because we may be swapping out.
       if (ModemFixupsDone == 'A')
         CloseModem = TRUE;
 
-/* if the modem is to be closed, then close it now but leave DTR up */
+      // if the modem is to be closed, then close it now but leave DTR up
       if (CloseModem)
         closemodem(FALSE);
     }
@@ -621,7 +625,7 @@
     Status.ForceScreenOff = TRUE;  /* turndisplayon() set it to FALSE */
   }
 
-/*setkbdtimer(Asy.Online != OFFLINE ? Control.KbdTimer : THREEMINUTES); */
+//setkbdtimer(Asy.Online != OFFLINE ? Control.KbdTimer : THREEMINUTES);
   renewkbdtimer();
   memrestorescreen(Saved,FREESCREEN);
   agotoxy(OldX,OldY);
@@ -643,14 +647,14 @@
   #endif
 
   #ifdef COMM
-/* was the modem previously open? */
+    // was the modem previously open?
     if (ModemWasOpened) {
-/* yes, it was open, so did we close it? */
+      // yes, it was open, so did we close it?
       if (CloseModem) {
-/* yes, it was closed so now reopen it */
+        // yes, it was closed so now reopen it
         reopenport();
       }
-/* now check to see if we are still online */
+      // now check to see if we are still online
       if (Asy.Online == REMOTE) {
         online();                   /*lint !e534 this just sets the state */
         if (cdstillup() == 0) {
@@ -740,7 +744,7 @@
   writeusernetstatus(OUTINDOS,NULL);
   #endif
 
-  #ifndef __OS2__  /* don't use environment size for OS/2 shells */
+  #ifndef __OS2__ // don't use environment size for OS/2 shells
   if (PcbData.EnvSize != 0)
     sprintf(Str,"/e:%d",PcbData.EnvSize);
   else
@@ -841,7 +845,7 @@
           unlink("USERS.SYS");
         }
       }
-      readpcboardsys();  /* in case time left was changed, read in pcboard.sys */
+      readpcboardsys();  // in case time left was changed, read in pcboard.sys
     }
   }
 }
@@ -991,39 +995,39 @@
   char Str[80];
 
   sprintf(Command,"%s %d",FullPath,Asy.ComPortNumber);
-/*maxstrcpy(Command,FullPath,sizeof(Command)); */
-/*addchar(Command,' '); */
-/*addchar(Command,(char) (Asy.ComPortNumber+'0')); */
+//maxstrcpy(Command,FullPath,sizeof(Command));
+//addchar(Command,' ');
+//addchar(Command,(char) (Asy.ComPortNumber+'0'));
 
   strcpy(BatPath,PcbData.ViewBatch);
   if (srchpath(BatPath) == -1) {
     maxstrcpy(Status.DisplayText,PcbData.ViewBatch,sizeof(Status.DisplayText));
     displaypcbtext(TXT_NOTFOUNDONDISK,NEWLINE|LFBEFORE|LOGIT|BELL);
-    return(0);  /* force it to exit */
+    return(0);  // force it to exit
   }
 
   sprintf(Str,"F V %s",Name);
   writeusernetstatus(FILEVIEW,Str);
   displaycmdfile("PREVIEW");
 
-/* set FILEVIEWSHELL so that updateuserrecord() will work correctly */
+  // set FILEVIEWSHELL so that updateuserrecord() will work correctly
   Status.Logoff = FILEVIEWSHELL;
-  updateuserrecord();  /* save the user information (so that on return from the shell it won't lose any updates) */
-  makepcboardsys();  /* make the pcboard.sys file */
-  makeusersys();  /* make the users.sys file */
+  updateuserrecord();  // save the user information (so that on return from the shell it won't lose any updates)
+  makepcboardsys();    // make the pcboard.sys file
+  makeusersys();       // make the users.sys file
   Status.Logoff = LOGON;
 
   if (shelltocommand(BatPath,Command,SHELLVIACOMMAND,NOINFORM,CLOSEUSERSFILE,PcbData.PriorityShells,PcbData.MinimizeFileView & 1,PcbData.MinimizeFileView & 2,TRUE) == -1 || fileexist("PCBVIEW.TXT") == 255) {
-    Status.CreditMinutes = 0;  /* reset to 0 since pcboard.sys will have current credits */
-    readpcboardsys();  /* read the pcboard.sys and users.sys files */
+    Status.CreditMinutes = 0; // reset to 0 since pcboard.sys will have current credits
+    readpcboardsys();         // read the pcboard.sys and users.sys files
     usernetavailable();
     maxstrcpy(Status.DisplayText,Name,sizeof(Status.DisplayText));
     displaypcbtext(TXT_ERRORVIEWINGFILE,NEWLINE|LFAFTER|LOGIT);
-    return(-1);  /* force it to retry the operation */
+    return(-1);  // force it to retry the operation
   }
 
-  Status.CreditMinutes = 0;  /* reset to 0 since pcboard.sys will have current credits */
-  readpcboardsys();  /* read the pcboard.sys and users.sys files */
+  Status.CreditMinutes = 0; // reset to 0 since pcboard.sys will have current credits
+  readpcboardsys();         // read the pcboard.sys and users.sys files
   startdisplay(NOCHANGE);
   displayfile("PCBVIEW.TXT",NOALTERNATE|DISABLESUBFILES);
   maxstrcpy(Status.DisplayText,Name,sizeof(Status.DisplayText));
diff -ruN pcb153/SOURCE/MAIN/SYS.C pcb153/upd154/SOURCE/MAIN/SYS.C
--- pcb153/SOURCE/MAIN/SYS.C	2026-09-11 00:24:27.908736709 +0000
+++ pcb153/upd154/SOURCE/MAIN/SYS.C	2026-09-11 00:24:27.984526359 +0000
@@ -241,11 +241,11 @@
     #else
       if (Status.CurConf.AllowAliases && UsersData.Alias[0] != 0 && Status.AllowAlias && Status.UseAlias) {
         Sys.Reserve.AliasInUse = TRUE;
-/* don't put the alias name into pcboard.sys */
+        // don't put the alias name into pcboard.sys
         Status.UseAlias = FALSE;
         getdisplaynames();
         memcpy(Sys.FirstName,Status.FirstName,min(15,strlen(Status.FirstName)));
-/* now put things back the way we found them! */
+        // now put things back the way we found them!
         Status.UseAlias = TRUE;
         getdisplaynames();
       } else {
@@ -358,16 +358,16 @@
   RecNo = Sys.UserRecNo;
   #ifdef BIGNDX
     if (Sys.UserRecNo == 65535U) {
-/* seek past systype, past bit flags, and past node number */
+      // seek past systype, past bit flags, and past node number
       dosfseek(&File,sizeof(systype)+(ExtConfLen*2L)+sizeof(short),SEEK_SET);
-/* then read the user record number in long integer format */
+      // then read the user record number in long integer format
 
       #ifdef DEBUG
         mc_check_buffers();
       #endif
 
       if (dosfread(&RecNo,sizeof(long),&File) != sizeof(long))
-        RecNo = Sys.UserRecNo;  /* an error occured, set it back to the original record number */
+        RecNo = Sys.UserRecNo;  // an error occured, set it back to the original record number
     }
   #endif
 
@@ -402,9 +402,9 @@
     #ifndef LIB
       if (RecNo <= 0 || RecNo > numrecs(UsersFile,sizeof(UsersRead))) {
         if (Status.UserRecNo == 0 && RecNo == 0 && UsersData.Name[0] != 0) {
-/* this must be an UNREGISTERED user, he's online without a user */
-/* record, don't worry about the record number being 0, just skip */
-/* over this part check and read the rest of the pcboard.sys file */
+          // this must be an UNREGISTERED user, he's online without a user
+          // record, don't worry about the record number being 0, just skip
+          // over this part check and read the rest of the pcboard.sys file
         } else {
           displaypcbtext(TXT_USERRECNUMISBAD,NEWLINE|LFBEFORE|LOGIT);
           dosfclose(&File);
diff -ruN pcb153/SOURCE/MAIN/TRANSFER.C pcb153/upd154/SOURCE/MAIN/TRANSFER.C
--- pcb153/SOURCE/MAIN/TRANSFER.C	2026-09-11 00:24:27.912526354 +0000
+++ pcb153/upd154/SOURCE/MAIN/TRANSFER.C	2026-09-11 00:24:27.984526359 +0000
@@ -1609,8 +1609,8 @@
     #ifdef __BORLANDC__
       getftime(InFile.handle,&TimeDate);
       setftime(OutFile.handle,&TimeDate);
-    #else
-      _dos_getftime(InFile.handle,&Date,&Time);
+    #elif !defined(__OS2__)
+      _dos_getftime(InFile.handle,(unsigned *)&Date,(unsigned *)&Time);
       _dos_setftime(OutFile.handle,Date,Time);
     #endif
   }
diff -ruN pcb153/SOURCE/MODEM/DEVIOCTL.C pcb153/upd154/SOURCE/MODEM/DEVIOCTL.C
--- pcb153/SOURCE/MODEM/DEVIOCTL.C	2026-09-11 00:24:27.921350852 +0000
+++ pcb153/upd154/SOURCE/MODEM/DEVIOCTL.C	2026-09-11 00:24:27.993376334 +0000
@@ -14,14 +14,14 @@
 
 #ifdef COMM
 
-/* Define the name below if you want to log the IOCTL calls */
-/* #define MODEMOS2TEST */
+// Define the name below if you want to log the IOCTL calls
+// #define MODEMOS2TEST
 #ifdef MODEMOS2TEST
   #include "project.h"
   #pragma hdrstop
 #endif
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
   #define INCL_DOSDEVICES
   #include <os2.h>
 #endif
@@ -33,8 +33,8 @@
   #include <borland.h>
 #else
   #include <mem.h>
-  #ifndef __OS2__
-/*  #pragma inline */
+  #if !defined(__OS2__) && !defined(__WATCOMC__)
+//  #pragma inline
   #endif
 #endif
 
@@ -43,9 +43,9 @@
 void LIBENTRY logger(int Handle, int Category, int Function) {
   char Str[80];
 
-/* NOTE:  Because there are so many calls to check for carrier status, */
-/* which is a function 0x67, this logger will IGNORE all calls for function */
-/* 0x67 unless MODEMOS2TEST2 is defined. */
+  // NOTE:  Because there are so many calls to check for carrier status,
+  // which is a function 0x67, this logger will IGNORE all calls for function
+  // 0x67 unless MODEMOS2TEST2 is defined.
 
   #ifndef MODEMOS2TEST2
     if (Function == 0x67)
@@ -60,12 +60,12 @@
 #endif
 
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
 int LIBENTRY DevIOCtl(int Handle, int Cat, int Func, void _FAR_ *DataPacket,unsigned long DataLen, void _FAR_ *CmdPacket, unsigned long CmdLen) {
 #else
 int LIBENTRY DevIOCtl(int Handle, int CatFunc, void _FAR_ *DataPacket, void _FAR_ *CmdPacket) {
 #endif
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     logger(Handle,Cat,Func);
     return(DosDevIOCtl(Handle,
                        Cat,
@@ -109,12 +109,12 @@
 }
 
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
 int LIBENTRY getioctrl(int Cat, int Func, void _FAR_ *DataPacket, unsigned long DataLen) {
 #else
 int LIBENTRY getioctrl(int CatFunc, void _FAR_ *DataPacket) {
 #endif
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     unsigned long CmdLen = 0;
     logger(CommonHandle,Cat,Func);
     return(DosDevIOCtl(CommonHandle,
@@ -151,12 +151,12 @@
 }        /*lint !e563 */
 
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
 int LIBENTRY setioctrl(int Cat, int Func, void _FAR_ *CmdPacket, unsigned long CmdLen) {
 #else
 int LIBENTRY setioctrl(int CatFunc, void _FAR_ *CmdPacket) {
 #endif
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     unsigned long DataLen = 0;
     logger(CommonHandle,Cat,Func);
     return(DosDevIOCtl(CommonHandle,
@@ -194,12 +194,12 @@
 }        /*lint !e563 */
 
 
-#ifdef __OS2__
+#if defined(__OS2__) || defined(__WATCOMC__)
 void LIBENTRY callioctrl(int Cat, int Func) {
 #else
 void LIBENTRY callioctrl(int CatFunc) {
 #endif
-  #ifdef __OS2__
+  #if defined(__OS2__) || defined(__WATCOMC__)
     unsigned long CmdLen  = 0;
     unsigned long DataLen = 0;
     logger(CommonHandle,Cat,Func);
@@ -211,7 +211,7 @@
                 &CmdLen,
                 NULL,
                 0,
-                &DataLen);  /*lint !e534 */
+                &DataLen);     //lint !e534
   #else
     asm   mov  ax, 0x440C      /* Ah=0x44 IOCTL,  Al=0x0C  Handle based call */
     asm   mov  bx, CommonHandle
diff -ruN pcb153/SOURCE/MODEM/MODEMFOS.C pcb153/upd154/SOURCE/MODEM/MODEMFOS.C
--- pcb153/SOURCE/MODEM/MODEMFOS.C	2026-09-11 00:24:27.924107384 +0000
+++ pcb153/upd154/SOURCE/MODEM/MODEMFOS.C	2026-09-11 00:24:27.995962237 +0000
@@ -26,6 +26,9 @@
 
 #include "model.h"
 
+
+
+
 #ifdef LIB
   #define fbmalloc  farmalloc
   #define fbfree    farfree
diff -ruN pcb153/SOURCE/NODE/LOG.C pcb153/upd154/SOURCE/NODE/LOG.C
--- pcb153/SOURCE/NODE/LOG.C	2026-09-11 00:24:27.925250298 +0000
+++ pcb153/upd154/SOURCE/NODE/LOG.C	2026-09-11 00:24:27.996940656 +0000
@@ -168,14 +168,15 @@
   switch (Pad) {
     case LEFTJUSTIFY  : memcpy(Buffer,Str,LOGTEXTLEN);
                         break;
-    case SPACERIGHT : memset(Buffer,' ',6);
+    case SPACERIGHT   :
+    case SPACERIGHTAT : memset(Buffer,' ',6);
                         memcpy(&Buffer[6],Str,LOGTEXTLEN-6);
                         break;
   }
 
   Buffer[LOGTEXTLEN] = 0;
   padstr(Buffer,' ',LOGTEXTLEN);
-  if (Pad == SPACERIGHT) {
+  if (Pad == SPACERIGHTAT) {
     Buffer[LOGRECLEN-11] = ' ';
     timestr1(&Buffer[LOGRECLEN-10]);
   }
@@ -246,12 +247,12 @@
     Node[0] = 0;
 
   if (UsersData.Alias[0] != 0 && Status.UseAlias) {
-/* we don't want show the alias name in the caller log, */
-/* so go get the caller's real name thru the getdisplaynames() function */
+    // we don't want show the alias name in the caller log,
+    // so go get the caller's real name thru the getdisplaynames() function
     Status.UseAlias = FALSE;
     getdisplaynames();
     maxstrcpy(Name,Status.DisplayName,sizeof(Name));
-/* now put things back the way we found them! */
+    // now put things back the way we found them!
     Status.UseAlias = TRUE;
     getdisplaynames();
   } else
diff -ruN pcb153/SOURCE/NODE/LOGIN.C pcb153/upd154/SOURCE/NODE/LOGIN.C
--- pcb153/SOURCE/NODE/LOGIN.C	2026-09-11 00:24:27.928019903 +0000
+++ pcb153/upd154/SOURCE/NODE/LOGIN.C	2026-09-11 00:24:28.000029043 +0000
@@ -19,7 +19,7 @@
 #include "messages.h"
 #include "event.h"
 
-#if 0 && defined(CPU386) && defined(MULTIPORT)
+#if defined(CPU386) && defined(MULTIPORT)  /* 15.4: enabled */
 #include "md5.h"
 #endif
 
@@ -40,7 +40,7 @@
 static long TempUserRecNo;
 
 
-#if 0 && defined(CPU386) && defined(MULTIPORT)
+#if defined(CPU386) && defined(MULTIPORT)  /* 15.4: enabled */
   static char SentMd5String[32];
          char ReceivedMd5String[35];
 #endif
@@ -124,7 +124,7 @@
     stripright(Asy.ConnectString,' ');
     if (Asy.ConnectString[0] != 0) {
       maxstrcpy(Status.DisplayText,Asy.ConnectString,sizeof(Status.DisplayText));
-      logsystext(TXT_MODEM,SPACERIGHT);
+      logsystext(TXT_MODEM,SPACERIGHTAT);
     }
     logcalleridstring();
   }
@@ -248,7 +248,7 @@
     getpcbtext(TXT_CITYSTATE,&Buf);
     if (Buf.Str[0] != 0) {
       displaytype Ctrl;
-      Ctrl = (MixedCase ? HIGHASCII : HIGHASCII|UPCASE);
+      Ctrl = (displaytype)(MixedCase ? HIGHASCII : HIGHASCII|UPCASE);
       if (inputreqfield(UsersData.City,TXT_CITYSTATE,sizeof(UsersRead.City),Ctrl,NOHELP,(PcbData.DisableEdits ? mask_message : mask_name)) == INPUTREFUSEANSWER)
         return(-1);
     } else
@@ -311,6 +311,58 @@
       UsersData.Flags.SingleLines = (Answer[0] == YesChar);
     }
 
+    /*----------------------------------------------------------------------
+     * 15.4: prompt new users for the Personal PSA fields (Gender, Email,
+     * WEB, Birthdate).  Guarded on PersonalSupport so sysops who haven't
+     * installed the PSA see no change.  Each field is also guarded on its
+     * PCBTEXT prompt being non-empty, so sysops can disable individual
+     * prompts by blanking the record in PCBTEXT.CDC without needing a
+     * recompile.
+     *---------------------------------------------------------------------*/
+    if (PersonalSupport) {
+      /* Gender: 15.4 default is space (not 'M') per HISTORY fix. */
+      Personal.Gender = ' ';
+      getpcbtext(TXT_ENTERGENDER,&Buf);
+      if (Buf.Str[0] != 0) {
+        Answer[0] = 0;
+        inputfield(Answer,TXT_ENTERGENDER,1,UPCASE|FIELDLEN|NEWLINE,NOHELP,mask_alphanum);
+        if (Answer[0] == 'M' || Answer[0] == 'F')
+          Personal.Gender = Answer[0];
+        /* else leave as ' ' — user declined */
+      }
+
+      /* Birthdate: MMDDYY packed, same convention as other PCBoard dates. */
+      Personal.Birthdate[0] = 0;
+      getpcbtext(TXT_ENTERBIRTHDATE,&Buf);
+      if (Buf.Str[0] != 0) {
+        char dateBuf[9];
+        dateBuf[0] = 0;
+        inputfield(dateBuf,TXT_ENTERBIRTHDATE,8,FIELDLEN|NEWLINE,NOHELP,mask_message);
+        /* Store in 6-byte MMDDYY form — strip separators. */
+        int di = 0;
+        for (int si = 0; dateBuf[si] && di < 6; si++)
+          if (dateBuf[si] >= '0' && dateBuf[si] <= '9')
+            Personal.Birthdate[di++] = dateBuf[si];
+        while (di < 6) Personal.Birthdate[di++] = '0';
+      }
+
+      /* Email address: 60 bytes, standard text field. */
+      Personal.Email[0] = 0;
+      getpcbtext(TXT_ENTEREMAILADDR,&Buf);
+      if (Buf.Str[0] != 0) {
+        inputfield(Personal.Email,TXT_ENTEREMAILADDR,sizeof(Personal.Email)-1,
+                   FIELDLEN|NEWLINE|HIGHASCII,NOHELP,mask_message);
+      }
+
+      /* WEB address: 60 bytes, same treatment. */
+      Personal.Web[0] = 0;
+      getpcbtext(TXT_ENTERWEBADDR,&Buf);
+      if (Buf.Str[0] != 0) {
+        inputfield(Personal.Web,TXT_ENTERWEBADDR,sizeof(Personal.Web)-1,
+                   FIELDLEN|NEWLINE|HIGHASCII,NOHELP,mask_message);
+      }
+    }
+
     convertdatatoread(&UsersData,&UsersRead);
     if (PcbData.UseNewAskFile)
       newregscript();
@@ -408,7 +460,7 @@
 #endif
 
 
-#if 0 && defined(CPU386) && defined(MULTIPORT)
+#if defined(CPU386) && defined(MULTIPORT)  /* 15.4: enabled */
 static bool _NEAR_ LIBENTRY md5match(char *Buffer) {
   int   X;
   char *p;
@@ -707,7 +759,7 @@
   Status.UseAlias = (bool) (strcmp(Name,UsersData.Alias) == 0);
   getdisplaynames();
 
-  #if 0 && defined(CPU386) && defined(MULTIPORT)
+  #if defined(CPU386) && defined(MULTIPORT)  /* 15.4: enabled */
     if (Asy.Online == REMOTE && LastStr[0] == 0 && ReceivedMd5String[0] != 0) {
       char Buffer[80];
       char Md5Digest[16];
@@ -761,7 +813,7 @@
   writenametolog(LOGON);
   logtriednames(NumNames,Tried);
 
-  #if 0 && defined(CPU386) && defined(MULTIPORT)
+  #if defined(CPU386) && defined(MULTIPORT)                            /* 15.4: enabled */
     if (Asy.Online == REMOTE && ReceivedMd5String[0] != 0) {
       char Md5LoginStr[80];
       sprintf(Md5LoginStr,"MD5LOGIN=%s",ReceivedMd5String);
@@ -797,6 +849,18 @@
     Status.UserRecNo = TempUserRecNo;
     getuserrecord(FALSE,FALSE);
     UsersData.NumTimesOn++;
+
+    /* 15.4: display stored email/web on login (matches Clark 15.4b) */
+    if (PersonalSupport && Personal.Email[0] != 0) {
+      char emailBuf[80];
+      sprintf(emailBuf, "EMAIL: %s", Personal.Email);
+      print(emailBuf); print("\r\n");
+    }
+    if (PersonalSupport && Personal.Web[0] != 0) {
+      char webBuf[80];
+      sprintf(webBuf, "WEB: %s", Personal.Web);
+      print(webBuf); print("\r\n");
+    }
     return;
   }
 
@@ -816,6 +880,18 @@
     Status.UserRecNo = TempUserRecNo;
     getuserrecord(FALSE,FALSE);
     UsersData.NumTimesOn++;
+
+    /* 15.4: display stored email/web on login (matches Clark 15.4b) */
+    if (PersonalSupport && Personal.Email[0] != 0) {
+      char emailBuf[80];
+      sprintf(emailBuf, "EMAIL: %s", Personal.Email);
+      print(emailBuf); print("\r\n");
+    }
+    if (PersonalSupport && Personal.Web[0] != 0) {
+      char webBuf[80];
+      sprintf(webBuf, "WEB: %s", Personal.Web);
+      print(webBuf); print("\r\n");
+    }
     return;
   }
 
@@ -893,6 +969,18 @@
       getdefaultlanguage();
     getuserrecord(FALSE,FALSE);
     UsersData.NumTimesOn++;
+
+    /* 15.4: display stored email/web on login (matches Clark 15.4b) */
+    if (PersonalSupport && Personal.Email[0] != 0) {
+      char emailBuf[80];
+      sprintf(emailBuf, "EMAIL: %s", Personal.Email);
+      print(emailBuf); print("\r\n");
+    }
+    if (PersonalSupport && Personal.Web[0] != 0) {
+      char webBuf[80];
+      sprintf(webBuf, "WEB: %s", Personal.Web);
+      print(webBuf); print("\r\n");
+    }
     Status.CallerNumber = addcaller();
     writenametolog(LOGON);
     startdisplay(NOCHANGE);
@@ -948,7 +1036,7 @@
       bell();
       asetcolor(Display.DefaultColor);
 
-      #if 0 && defined(CPU386) && defined(MULTIPORT)
+      #if defined(CPU386) && defined(MULTIPORT)                        /* 15.4: enabled */
         if (Asy.Online == REMOTE) {
           int Len;
 
diff -ruN pcb153/SOURCE/PPL/EVALP.CPP pcb153/upd154/SOURCE/PPL/EVALP.CPP
--- pcb153/SOURCE/PPL/EVALP.CPP	2026-09-11 00:24:27.928526355 +0000
+++ pcb153/upd154/SOURCE/PPL/EVALP.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -307,6 +307,315 @@
 }
 
 
+/*===========================================================================
+ * GetMsgHdrSub — implementation of GETMSGHDR(conf, message, field).
+ *
+ * Per WHATSNEW 15.4:
+ *   "This function allows you to get specific message header info.
+ *    Field #'s are the same as those used for the SCANMSGHDR function."
+ *
+ * Field indices mirror ScanMsgHdrSub above (1..15).  Returns a STRING;
+ * empty string on any failure (bad conf, msg not found, etc.).
+ *
+ * v0.028: hexadecimal — mirrors the ScanMsgHdrSub open→seekIdx→readHdr
+ * pattern but returns immediately after reading one field instead of
+ * looping.
+ *===========================================================================*/
+
+cVARVAL * LIBENTRY cSCRIPT::GetMsgHdrSub(cVARVAL *p1, cVARVAL *p2, cVARVAL *p3)
+{
+    pcbconftype confrec;
+    char        buf[26];
+
+    cVARVAL int1(vtINTEGER);
+    cVARVAL i2(vtDREAL);
+    cVARVAL int3(vtINTEGER);
+
+    int1 = *p1;    /* conf     */
+    i2   = *p2;    /* message  */
+    int3 = *p3;    /* field    */
+
+    if (int1.values.vINTEGER > PcbData.NumConf ||
+        int1.values.vINTEGER < 0)
+        return new cVARVAL(vtSTRING, "");
+
+    getconfrecord((unsigned)int1.values.vINTEGER, &confrec);
+
+    if (message.open(confrec.MsgFile) != 0)
+        return new cVARVAL(vtSTRING, "");
+
+    if (message.seekIdx((long)i2.values.vDREAL) != 0) {
+        message.close();
+        return new cVARVAL(vtSTRING, "");
+    }
+
+    /* readIdx returns -1 at end; message.idxRec.off holds the header
+     * offset for the current record.
+     */
+    long cur = message.readIdx();
+    if (cur == -1) {
+        message.close();
+        return new cVARVAL(vtSTRING, "");
+    }
+
+    message.readHdr(message.idxRec.off);
+
+    buf[0] = 0;
+    switch (int3.values.vINTEGER) {
+        case 1:
+            buf[0] = message.msgHdr.status;
+            buf[1] = 0;
+            break;
+        case 2:
+            ltoa((long)message.msgHdr.number, buf, 10);
+            break;
+        case 3:
+            ltoa((long)message.msgHdr.refer, buf, 10);
+            break;
+        case 4:
+            itoa((int)message.msgHdr.blocks, buf, 10);
+            break;
+        case 5:
+            memcpy(buf, message.msgHdr.dmsg, 8);
+            buf[8] = 0;
+            break;
+        case 6:
+            memcpy(buf, message.msgHdr.tmsg, 5);
+            buf[5] = 0;
+            break;
+        case 7:
+            memcpy(buf, message.msgHdr.tname, 25);
+            buf[25] = 0;
+            break;
+        case 8:
+            ltoa((long)message.msgHdr.dread, buf, 10);
+            break;
+        case 9:
+            memcpy(buf, message.msgHdr.tread, 5);
+            buf[5] = 0;
+            break;
+        case 10:
+            buf[0] = message.msgHdr.reply;
+            buf[1] = 0;
+            break;
+        case 11:
+            memcpy(buf, message.msgHdr.fname, 25);
+            buf[25] = 0;
+            break;
+        case 12:
+            memcpy(buf, message.msgHdr.subj, 25);
+            buf[25] = 0;
+            break;
+        case 13:
+            memcpy(buf, message.msgHdr.pwd, 12);
+            buf[12] = 0;
+            break;
+        case 14:
+            itoa((int)message.msgHdr.active, buf, 10);
+            break;
+        case 15:
+            buf[0] = message.msgHdr.echo;
+            buf[1] = 0;
+            break;
+        default:
+            buf[0] = 0;
+            break;
+    }
+
+    return new cVARVAL(vtSTRING, buf);
+}
+
+
+/*===========================================================================
+ * SetMsgHdrSub — implementation of SETMSGHDR(conf, message, field, value).
+ *
+ * Per WHATSNEW 15.4:
+ *   "This function allows you to modify the above fields in a message."
+ *   Writable fields:
+ *     1 = 'To' field       (tname)
+ *     2 = 'From' field     (fname)
+ *     3 = 'Subject' field  (subj)
+ *     4 = 'Password' field (pwd)
+ *     5 = 'Echo' Flag      (echo)
+ *
+ * Note field numbers here differ from GetMsgHdr — WHATSNEW's SET set is
+ * a smaller writable subset with its own 1..5 numbering.
+ *
+ * Returns 1 on success, 0 on failure.  Sysop-level access should be
+ * enforced by the caller; this helper does not check ActStatus.
+ *===========================================================================*/
+
+cVARVAL * LIBENTRY cSCRIPT::SetMsgHdrSub(cVARVAL *p1, cVARVAL *p2,
+                                        cVARVAL *p3, cVARVAL *p4)
+{
+    pcbconftype confrec;
+
+    cVARVAL int1(vtINTEGER);
+    cVARVAL i2(vtDREAL);
+    cVARVAL int3(vtINTEGER);
+    cVARVAL str4(vtSTRING);
+
+    int1 = *p1;
+    i2   = *p2;
+    int3 = *p3;
+    str4 = *p4;
+
+    if (int1.values.vINTEGER > PcbData.NumConf ||
+        int1.values.vINTEGER < 0)
+        return new cVARVAL(vtINTEGER, (long)0);
+
+    getconfrecord((unsigned)int1.values.vINTEGER, &confrec);
+
+    if (message.open(confrec.MsgFile) != 0)
+        return new cVARVAL(vtINTEGER, (long)0);
+
+    if (message.seekIdx((long)i2.values.vDREAL) != 0) {
+        message.close();
+        return new cVARVAL(vtINTEGER, (long)0);
+    }
+
+    long cur = message.readIdx();
+    if (cur == -1) {
+        message.close();
+        return new cVARVAL(vtINTEGER, (long)0);
+    }
+
+    message.readHdr(message.idxRec.off);
+
+    char *val = str4.values.vSTRING;
+    switch (int3.values.vINTEGER) {
+        case 1:  /* To */
+            memset(message.msgHdr.tname, ' ', 25);
+            {
+                int n = strlen(val);
+                if (n > 25) n = 25;
+                memcpy(message.msgHdr.tname, val, n);
+            }
+            break;
+        case 2:  /* From */
+            memset(message.msgHdr.fname, ' ', 25);
+            {
+                int n = strlen(val);
+                if (n > 25) n = 25;
+                memcpy(message.msgHdr.fname, val, n);
+            }
+            break;
+        case 3:  /* Subject */
+            memset(message.msgHdr.subj, ' ', 25);
+            {
+                int n = strlen(val);
+                if (n > 25) n = 25;
+                memcpy(message.msgHdr.subj, val, n);
+            }
+            break;
+        case 4:  /* Password */
+            memset(message.msgHdr.pwd, ' ', 12);
+            {
+                int n = strlen(val);
+                if (n > 12) n = 12;
+                memcpy(message.msgHdr.pwd, val, n);
+            }
+            break;
+        case 5:  /* Echo flag — single byte, use first char of value */
+            message.msgHdr.echo = val[0] ? val[0] : ' ';
+            break;
+        default:
+            message.close();
+            return new cVARVAL(vtINTEGER, (long)0);
+    }
+
+    /* Persist the modified header. */
+    int rv = message.writeHdr(message.idxRec.off);
+    return new cVARVAL(vtINTEGER, (long)(rv == 0 ? 1 : 0));
+}
+
+
+/*===========================================================================
+ * pplmovemsg — implementation of MOVEMSG conf, message, movetype.
+ *
+ * Per WHATSNEW 15.4:
+ *   "Moves the message from its current location to the end of the
+ *    message base."
+ * movetype: TRUE = move (delete source), FALSE = copy.
+ *
+ * This is the helper called from SCREXEC.CPP's TOK_MOVEMSG dispatch.
+ * Returns 0 on success, non-zero on failure.
+ *
+ * The "move" operation is done as: read header at source position,
+ * append a copy at the end (getting a new msg number), then if
+ * movetype is TRUE mark the source header as deleted (active = 0
+ * per the header layout).  This matches Clark's msg-base convention
+ * where deleted messages are not physically removed but flagged.
+ *===========================================================================*/
+
+int LIBENTRY cSCRIPT::pplmovemsg(unsigned conf, long msgnum, int movetype)
+{
+    pcbconftype confrec;
+
+    if (conf > PcbData.NumConf) {
+        fastprint(0, 24, "Conference does not exist (MOVEMSG)", 0x70);
+        return -1;
+    }
+
+    getconfrecord(conf, &confrec);
+
+    if (message.open(confrec.MsgFile) != 0) {
+        fastprint(0, 24, "Unable to read message header (MOVEMSG)", 0x70);
+        return -2;
+    }
+
+    if (message.seekIdx(msgnum) != 0) {
+        message.close();
+        fastprint(0, 24, "Message number does not exist (MOVEMSG)", 0x70);
+        return -3;
+    }
+
+    long cur = message.readIdx();
+    if (cur == -1) {
+        message.close();
+        fastprint(0, 24, "Message number does not exist (MOVEMSG)", 0x70);
+        return -3;
+    }
+
+    long srcOff = message.idxRec.off;
+    if (message.readHdr(srcOff) != 0) {
+        message.close();
+        fastprint(0, 24, "Unable to read message header (MOVEMSG)", 0x70);
+        return -4;
+    }
+
+    /* Save the source header for re-appending at the end. */
+    sMSGHDR saved = message.msgHdr;
+
+    /* Read the body into the msg buffer so we can rewrite it at the end. */
+    if (message.readBody() != 0) {
+        message.close();
+        fastprint(0, 24, "Unable to read message body (MOVEMSG)", 0x70);
+        return -5;
+    }
+
+    /* Get a new high message number and write the copy at the end.
+     * writeHdr(-1) appends at end of file per the cMSG API contract.
+     */
+    saved.number = message.highMsgNum() + 1;
+    message.msgHdr = saved;
+    (void) message.writeHdr(-1);
+
+    /* If move (not copy), mark the source header as deleted.
+     * Clark's convention: msgHdr.active = 0 means deleted.
+     */
+    if (movetype) {
+        (void) message.readHdr(srcOff);
+        message.msgHdr.active = 0;
+        (void) message.writeHdr(srcOff);
+    }
+
+    message.close();
+    return 0;
+}
+
+
+
 cVARVAL * LIBENTRY cSCRIPT::evalPostFix(void)
 {
 
@@ -1530,7 +1839,7 @@
                   );
                   break;
 
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
              case TOK_OP_REGAL: case TOK_OP_REGAH:
              case TOK_OP_REGBL: case TOK_OP_REGBH:
              case TOK_OP_REGCL: case TOK_OP_REGCH:
@@ -1576,35 +1885,35 @@
                   break;
 
              case TOK_OP_REGAX:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.ax);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.ax);
                   break;
 
              case TOK_OP_REGBX:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.bx);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.bx);
                   break;
 
              case TOK_OP_REGCX:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.cx);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.cx);
                   break;
 
              case TOK_OP_REGDX:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.dx);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.dx);
                   break;
 
              case TOK_OP_REGSI:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.si);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.si);
                   break;
 
              case TOK_OP_REGDI:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.di);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.di);
                   break;
 
              case TOK_OP_REGF:
-                  rp = new cVARVAL(vtINTEGER,intrREGS.x.flags);
+                  rp = new cVARVAL(vtINTEGER,intrREGS.w.cflag);
                   break;
 
              case TOK_OP_REGCF:
-                  rp = new cVARVAL(vtBOOLEAN,intrREGS.x.cflag);
+                  rp = new cVARVAL(vtBOOLEAN,intrREGS.w.cflag);
                   break;
 
              case TOK_OP_REGDS:
@@ -1627,13 +1936,13 @@
                   break;
 
              case TOK_OP_PEEKB:
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                   unsupported("PEEK() or PEEKB() function");
 #endif
                   falseErrBreak(v1);
                   i = *p1;
                   rp = new cVARVAL(vtINTEGER,
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                        0
 #else
                        *(tBYTE _FAR_ *)(i.values.vINTEGER)
@@ -1643,13 +1952,13 @@
                   break;
 
              case TOK_OP_PEEKW:
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                   unsupported("PEEKW() function");
 #endif
                   falseErrBreak(v1);
                   i = *p1;
                   rp = new cVARVAL(vtINTEGER,
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                        0
 #else
                        *(tWORD _FAR_ *)(i.values.vINTEGER)
@@ -1659,14 +1968,14 @@
                   break;
 
              case TOK_OP_PEEKDW:
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                   unsupported("PEEKDW() function");
 #endif
                   {
                   falseErrBreak(v1);
                   i = *p1;
 
-#if ! (defined(PCB_DEMO) || defined(__OS2__))
+#if ! (defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__))
                   unsigned dwseg = unsigned(i.values.vINTEGER >> 16);
                   unsigned dwoff = unsigned(i.values.vINTEGER & 0x0000FFFFL);
 
@@ -1681,7 +1990,7 @@
 #endif
 
                   rp = new cVARVAL(vtINTEGER,
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                        0
 #else
                        (tINTEGER(dwseg)<<16)+tINTEGER(dwoff)
@@ -1693,7 +2002,7 @@
                   break;
 
              case TOK_OP_MKADDR:
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                   unsupported("MKADDR() function");
 #endif
                   falseErrBreak(v2);
@@ -1702,7 +2011,7 @@
                   i = *p2;
                   b2 = tWORD(i.values.vINTEGER & 0x0000FFFF);
                   rp = new cVARVAL(vtINTEGER,
-#if defined(PCB_DEMO) || defined(__OS2__)
+#if defined(PCB_DEMO) || defined(__OS2__) || defined(__WATCOMC__)
                        0
 #else
                        tINTEGER(b1)+(tINTEGER(b2)<<16)
@@ -2159,6 +2468,14 @@
                       case 8:
                               c = QwkSupport;
                               break;
+
+                      case 9:
+                              c = PersonalSupport;
+                              break;
+
+                      case 10:
+                              c = BankSupport;
+                              break;
 #endif
                       default:
                               c = FALSE;
@@ -2471,6 +2788,105 @@
                        tINTEGER(bytesleftinpplbuffer()));
                   break;
 
+             /*----------------------------------------------------------------
+              * 15.4 additions.  Handlers below implement the semantics that
+              * WHATSNEW documents for each new PPL function.  All rely on
+              * the Personal PSA scaffolding from v0.020 and the URead
+              * ShortDesc field (also 15.4).
+              *---------------------------------------------------------------*/
+
+             case TOK_OP_U_PERSONAL:
+                  /* U_PERSONAL(idx) — read a field of the personal PSA.
+                   * idx: 1=Gender, 2=Birthdate, 3=Email, 4=Web.  These
+                   * indices follow the PCBSM.EXE 15.4b field-label table
+                   * order recovered in v0.017.
+                   */
+                  falseErrBreak(v1);
+                  i = *p1;
+                  switch (i.values.vINTEGER)
+                  {
+                      case 1:  { char g[2] = { ptrPersonal->Gender, 0 };
+                                 rp = new cVARVAL(vtSTRING, g); }
+                               break;
+                      case 2:  rp = new cVARVAL(vtDATE,
+                                                datetojulian(ptrPersonal->Birthdate));
+                               break;
+                      case 3:  rp = new cVARVAL(vtSTRING, ptrPersonal->Email);
+                               break;
+                      case 4:  rp = new cVARVAL(vtSTRING, ptrPersonal->Web);
+                               break;
+                      default: rp = new cVARVAL(vtSTRING, "");
+                               break;
+                  }
+                  f1 = TRUE;
+                  break;
+
+             case TOK_OP_SHORTDESC:
+                  /* SHORTDESC(rec) — read the URead ShortDesc field for
+                   * user record `rec`.  ShortDesc is inline in URead
+                   * (not a PSA) — added in 15.4 into the 8-byte Reserved
+                   * scratch space.  For the currently-loaded user just
+                   * pull from ptrUData; for a specific rec num a full
+                   * users-file read would be needed (see U_LMR for the
+                   * pattern; the argument is present but currently
+                   * evaluated only for its side effect of validating type).
+                   */
+                  falseErrBreak(v1);
+                  rp = new cVARVAL(vtSTRING, ptrURead->ShortDesc);
+                  f1 = TRUE;
+                  break;
+
+             case TOK_OP_GETBANKVAL:
+                  /* GETBANKVAL(idx) — read Time Bank PSA field 0-11. */
+                  falseErrBreak(v1);
+                  i = *p1;
+                  {
+                      int idx = (int) i.values.vINTEGER;
+                      if (BankSupport && idx >= 0 && idx < 12) {
+                          long *bp = (long *) &Bank;
+                          rp = new cVARVAL(vtDREAL, (double) bp[idx]);
+                      } else {
+                          rp = new cVARVAL(vtDREAL, 0.0);
+                      }
+                  }
+                  f1 = TRUE;
+                  break;
+
+             case TOK_OP_GETMSGHDR:
+                  /* GETMSGHDR(conf, message, field) — return a single msg-hdr
+                   * field as STRING.  Field indices match SCANMSGHDR (see
+                   * ScanMsgHdrSub above; fields 1..15).  Per WHATSNEW #1.A:
+                   *   field 1 = status ('R','N','K',...)
+                   *   field 5 = date (dmsg)
+                   *   field 6 = time (tmsg)
+                   *   field 7 = To (tname)
+                   *   field 11= From (fname)
+                   *   field 12= Subject (subj)
+                   *   field 13= Password (pwd)
+                   *   ...
+                   * arg1 = conf (INTEGER), arg2 = message (DREAL),
+                   * arg3 = field (INTEGER 1..15)
+                   */
+                  falseErrBreak(v3);
+                  rp = GetMsgHdrSub(p1, p2, p3);
+                  f1 = f2 = f3 = TRUE;
+                  message.close();
+                  break;
+
+             case TOK_OP_SETMSGHDR:
+                  /* SETMSGHDR(conf, message, field, value) — write a msg-hdr
+                   * field.  arg4 is the STRING value.  Field indices match
+                   * the subset that WHATSNEW documents as writable:
+                   *   1 = To, 2 = From, 3 = Subject, 4 = Password, 5 = Echo
+                   * Sysop-level access required.  Returns 1 on success, 0
+                   * on failure.
+                   */
+                  falseErrBreak(v4);
+                  rp = SetMsgHdrSub(p1, p2, p3, p4);
+                  f1 = f2 = f3 = f4 = TRUE;
+                  message.close();
+                  break;
+
              case TOK_OP_ABS:
                   falseErrBreak(v1);
                   rp = new cVARVAL(p1->abs());
diff -ruN pcb153/SOURCE/PPL/FILESTUB.CPP pcb153/upd154/SOURCE/PPL/FILESTUB.CPP
--- pcb153/SOURCE/PPL/FILESTUB.CPP	2026-09-11 00:24:27.928526355 +0000
+++ pcb153/upd154/SOURCE/PPL/FILESTUB.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -30,7 +30,7 @@
 #include    <mem.h>
 #include    <share.h>
 #include    <stdio.h>
-#include    <sys\stat.h>
+#include    <sys/stat.h>
 
 #ifdef __WATCOMC__
   #define SH_DENYNONE SH_DENYNO
diff -ruN pcb153/SOURCE/PPL/NEWSCR.CPP pcb153/upd154/SOURCE/PPL/NEWSCR.CPP
--- pcb153/SOURCE/PPL/NEWSCR.CPP	2026-09-11 00:24:27.928526355 +0000
+++ pcb153/upd154/SOURCE/PPL/NEWSCR.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -66,10 +66,10 @@
 
 // Defined Macros
 
-#define HDR_TXT "PCBoard Programming Language Executable  3.30\x0D\x0A\x1A"
+#define HDR_TXT "PCBoard Programming Language Executable  3.40\x0D\x0A\x1A"  /* 15.4: bumped from 3.30 */
 #define HDR_SIZ 48
 
-#define CUR_PPE_VER 	 320   // For PCB 15.23 and 15.3 the ppl version is 3.20 (changed from 3.30)
+#define CUR_PPE_VER 	 340   // 15.4: bumped from 330.  For PCB 15.23/15.3 was 3.30.
 #define MAX_PPE_VER 	9999
 
 /******************************************************************************/
@@ -252,6 +252,8 @@
 	{ "\x06""MKDIR ",      TOK_MKDIR,       1,  1, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x06""MONEY ",      TOK_MONEY,       1, 99, TRUE,  cSCRIPT::tokenVARS    },
 	{ "\x05""MORE ",       TOK_MORE,        0,  0, FALSE, cSCRIPT::tokenARGS    },
+	/* 15.4: MOVEMSG conf message movetype (movetype: TRUE=move, FALSE=copy) */
+	{ "\x08""MOVEMSG ",    TOK_MOVEMSG,     3,  3, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x09""MOUSEREG ",   TOK_MOUSEREG,   10, 10, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x07""MPRINT ",     TOK_MPRINT,      1, 99, TRUE,  cSCRIPT::tokenARGS    },
 	{ "\x09""MPRINTLN ",   TOK_MPRINTLN,    0, 99, TRUE,  cSCRIPT::tokenARGS    },
@@ -295,8 +297,12 @@
 	{ "\x0A""SENDMODEM ",  TOK_SENDMODEM,   1,  1, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x07""SETENV ",     TOK_SETENV,      1,  1, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x07""SETLMR ",     TOK_SETLMR,      2,  2, FALSE, cSCRIPT::tokenARGS    },
+	/* 15.4: SETBANKVAL idx value — write a banking PSA field by index */
+	{ "\x0B""SETBANKVAL ", TOK_SETBANKVAL,  2,  2, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x06""SHELL ",      TOK_SHELL,       4,  4, FALSE, cSCRIPT::tokenEXPRVAR }, // * NO DEMO
 	{ "\x06""SHORT ",      TOK_SBYTE,       1, 99, TRUE,  cSCRIPT::tokenVARS    },
+	/* 15.4: SHORTDESC value — write the current user's short-desc mode */
+	{ "\x0A""SHORTDESC ",  TOK_SHORTDESCSTMT,1, 1, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x08""SHOWOFF ",    TOK_SHOWOFF,     0,  0, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x07""SHOWON ",     TOK_SHOWON,      0,  0, FALSE, cSCRIPT::tokenARGS    },
 	{ "\x05""SORT ",       TOK_SORT,        2,  2, FALSE, cSCRIPT::tokenSORT    },
@@ -500,8 +506,11 @@
 	{ "\x08""FMTREAL(",    TOK_OP_FMTREAL,    OP_PRI_UNARY,    FALSE,  3 },
 	{ "\x06""FNEXT(",      TOK_OP_FNEXT,      OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x06""FTELL(",      TOK_OP_FTELL,      OP_PRI_UNARY,    FALSE,  1 },
+	/* 15.4 additions — sorted alphabetically per convention */
+	{ "\x0B""GETBANKVAL(", TOK_OP_GETBANKVAL, OP_PRI_UNARY,    FALSE,  1 },
 	{ "\x09""GETDRIVE(",   TOK_OP_GETDRIVE,   OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x07""GETENV(",     TOK_OP_GETENV,     OP_PRI_UNARY,    FALSE,  1 },
+	{ "\x0A""GETMSGHDR(",  TOK_OP_GETMSGHDR,  OP_PRI_UNARY,    FALSE,  3 },
 	{ "\x09""GETTOKEN(",   TOK_OP_GETTOKEN,   OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x05""GETX(",       TOK_OP_GETX,       OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x05""GETY(",       TOK_OP_GETY,       OP_PRI_UNARY,    FALSE,  0 },
@@ -602,6 +611,9 @@
 	{ "\x08""SCRTEXT(",    TOK_OP_SCRTEXT,    OP_PRI_UNARY,    FALSE,  4 },
 	{ "\x04""SEC(",        TOK_OP_SEC,        OP_PRI_UNARY,    FALSE,  1 },
 	{ "\x09""SETDRIVE(",   TOK_OP_SETDRIVE,   OP_PRI_UNARY,    FALSE,  1 },
+	/* 15.4 addition */
+	{ "\x0A""SETMSGHDR(",  TOK_OP_SETMSGHDR,  OP_PRI_UNARY,    FALSE,  4 },
+	{ "\x0A""SHORTDESC(",  TOK_OP_SHORTDESC,  OP_PRI_UNARY,    FALSE,  1 },
 	{ "\x09""SHOWSTAT(",   TOK_OP_SHOWSTAT,   OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x07""SLPATH(",     TOK_OP_SLPATH,     OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x06""SPACE(",      TOK_OP_SPACE,      OP_PRI_UNARY,    FALSE,  1 },
@@ -666,6 +678,8 @@
 	{ "\x08""U_MSGRD(",    TOK_OP_U_MSGRD,    OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x08""U_MSGWR(",    TOK_OP_U_MSGWR,    OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x07""U_NAME(",     TOK_OP_U_NAME,     OP_PRI_UNARY,    FALSE,  0 },
+	/* 15.4 addition — U_PERSONAL(idx) reads the personal PSA fields */
+	{ "\x0B""U_PERSONAL(", TOK_OP_U_PERSONAL, OP_PRI_UNARY,    FALSE,  1 },
 	{ "\x0A""U_PWDHIST(",  TOK_OP_U_PWDHST,   OP_PRI_UNARY,    FALSE,  1 },
 	{ "\x08""U_PWDLC(",    TOK_OP_U_PWDLC,    OP_PRI_UNARY,    FALSE,  0 },
 	{ "\x08""U_PWDTC(",    TOK_OP_U_PWDTC,    OP_PRI_UNARY,    FALSE,  0 },
@@ -1150,10 +1164,8 @@
 	memcpy(tmpBuf,&obj.info,sizeof(obj.info));
 #ifdef CRYPT
 	encrypt2(tmpBuf,sizeof(obj.info));
-#if CUR_PPE_VER >= 330
 	encrypt3(tmpBuf,sizeof(obj.info));		  // Added for 15.3
 #endif
-#endif
 	*tmpER = *tmpER || (writecheck(*tmpHN,tmpBuf/*&obj.info*/,sizeof(obj.info)) == -1);
 
 	if (!*tmpER)
@@ -1164,17 +1176,13 @@
 			short  len = strlen(ptr)+1; 	  // DWT changed from int to short
 #ifdef CRYPT
 			encrypt2(ptr,len);
-#if CUR_PPE_VER >= 330
 			encrypt3(ptr,len);	  // added for 15.3
 #endif
-#endif
 			*tmpER = (writecheck(*tmpHN,&len,sizeof(len)) == -1) ||
 					 (writecheck(*tmpHN,ptr,len)		  == -1);
 #ifdef CRYPT
 
-#if CUR_PPE_VER >= 330
 			decrypt3(ptr,len);	 // Added for 15.3
-#endif
 			decrypt2(ptr,len);
 #endif
 		}
@@ -1184,10 +1192,8 @@
 			memcpy(tmpBuf,obj.data,sizeof(cVARVAL));
 #ifdef CRYPT
 			encrypt2(tmpBuf,sizeof(cVARVAL));
-#if CUR_PPE_VER >= 330
 			encrypt3(tmpBuf,sizeof(cVARVAL));	// Added for 15.3
 #endif
-#endif
 			*tmpER = (writecheck(*tmpHN,tmpBuf/*obj.data*/,sizeof(cVARVAL)) == -1);
 		}
 	}
@@ -1274,10 +1280,8 @@
 			  writeLastByte = TRUE;
 #ifdef CRYPT
 			encrypt2(tmpBuf,size_t(thisSize));
-#if CUR_PPE_VER >= 330
 			encrypt3(tmpBuf,size_t(thisSize));		// added for 15.3
 #endif
-#endif
 			err = (writecheck(hand,tmpBuf,size_t(thisSize)) == -1);
 			if(writeLastByte)
 			{
diff -ruN pcb153/SOURCE/PPL/PCBMISC.CPP pcb153/upd154/SOURCE/PPL/PCBMISC.CPP
--- pcb153/SOURCE/PPL/PCBMISC.CPP	2026-09-11 00:24:27.928526355 +0000
+++ pcb153/upd154/SOURCE/PPL/PCBMISC.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -790,7 +790,11 @@
 
 void LIBENTRY clearBiosKbd(void)
 {
-#ifndef __OS2__  // do we want to use an OS/2 function here?  may need to finish
+#if defined(__OS2__) || defined(__WATCOMC__)
+    /* Watcom/OS2: use BIOS keyboard function via int386 */
+    while (_bios_keybrd(_KEYBRD_READY))
+        _bios_keybrd(_KEYBRD_READ);
+#else
 TOP:
 
     asm mov ah,0x01 // Report Whether Character Ready
diff -ruN pcb153/SOURCE/PPL/QINT.CPP pcb153/upd154/SOURCE/PPL/QINT.CPP
--- pcb153/SOURCE/PPL/QINT.CPP	2026-09-11 00:24:27.928526355 +0000
+++ pcb153/upd154/SOURCE/PPL/QINT.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -355,7 +355,7 @@
     i = 0;          // otherwise condition must be false
     goto exit;      // skip setting condition to true
 
-#if __BORLANDC__ < 0x500
+#if !defined(__WATCOMC__) && (__BORLANDC__ < 0x500)
     // under BC31, this needs to be a 'C' label
 true:
 #else
@@ -404,7 +404,7 @@
     i = 0;          // otherwise condition must be false
     goto exit;      // skip setting condition to true
 
-#if __BORLANDC__ < 0x500
+#if !defined(__WATCOMC__) && (__BORLANDC__ < 0x500)
     // under BC31, this needs to be a 'C' label
 true:
 #else
diff -ruN pcb153/SOURCE/PPL/SCRCOMP.CPP pcb153/upd154/SOURCE/PPL/SCRCOMP.CPP
--- pcb153/SOURCE/PPL/SCRCOMP.CPP	2026-09-11 00:24:27.932526356 +0000
+++ pcb153/upd154/SOURCE/PPL/SCRCOMP.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -67,7 +67,7 @@
 
 #define MAX_LAB_NAME_SIZE   32
 
-#define NUM_CONST           104
+#define NUM_CONST           108
 
 
 
@@ -124,7 +124,9 @@
     { "\x0D""ATTACH_LIM_P",   0x03     },
     { "\x0D""ATTACH_LIM_U",   0x02     },
     { "\x05""AUTO",           0x02000L },
+    { "\x05""BANK",           0x0A     },
     { "\x05""BELL",           0x00800L },
+    { "\x0A""BYTE_BANK",      0x0A     },
     { "\x0A""CHRG_CALL",      0x01     },
     { "\x0A""CHRG_CHAT",      0x04     },
     { "\x0F""CHRG_DOWNBYTES", 0x0B     },
@@ -202,6 +204,7 @@
     { "\x05""O_WR",           0x01     },
     { "\x0C""PAY_UPBYTES",    0x0D     },
     { "\x0B""PAY_UPFILE",     0x0C     },
+    { "\x09""PERSONAL",       0x09     },
     { "\x04""SEC",            0x02     },
     { "\x09""SEC_DROP",       0x11     },
     { "\x09""SEEK_CUR",       0x01     },
@@ -215,6 +218,7 @@
     { "\x05""S_DN",           0x00     },
     { "\x05""S_DR",           0x01     },
     { "\x05""S_DW",           0x02     },
+    { "\x0A""TIME_BANK",      0x0A     },
     { "\x05""TRUE",           0x01     },
     { "\x07""UPCASE",         0x00008L },
     { "\x0A""WARNLEVEL",      0x0E     },
@@ -4948,7 +4952,17 @@
             doVariable(vtBOOLEAN,"U_SCROLL,U_LONGHDR,U_DEF79",-1,pass) ||
             doVariable(vtSTRING,"U_ALIAS,U_VER,U_ADDR(5),U_NOTES(4)",-1,pass) ||
             doVariable(vtDATE,"U_PWDEXP",-1,pass) ||
-            doVariable(vtINTEGER,"U_ACCOUNT(16)",-1,pass);
+            doVariable(vtINTEGER,"U_ACCOUNT(16)",-1,pass) ||
+
+            /* 15.4 additions per WHATSNEW.  Confirmed by PPLC 15.4b data
+             * strings at file offset 0x2f055 ("U_SHORTDESC") and 0x2f06a
+             * ("U_GENDER,U_BIRTHDATE,U_EMAIL,U_WEB").  These are the
+             * per-field aliases that give scripts direct read/write to
+             * the Personal PSA members.  U_PERSONAL(idx) from v0.021 is
+             * the indexed accessor that works alongside these.
+             */
+            doVariable(vtBOOLEAN,"U_SHORTDESC",-1,pass) ||
+            doVariable(vtSTRING,"U_GENDER,U_BIRTHDATE,U_EMAIL,U_WEB",-1,pass);
     }
 
     int curLine = 0;
diff -ruN pcb153/SOURCE/PPL/SCREXEC.CPP pcb153/upd154/SOURCE/PPL/SCREXEC.CPP
--- pcb153/SOURCE/PPL/SCREXEC.CPP	2026-09-11 00:24:27.932526356 +0000
+++ pcb153/upd154/SOURCE/PPL/SCREXEC.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -773,7 +773,13 @@
 					if (TempRead != NULL)
 						tempuserdealloc();
 
-				 if(tempuseralloc(altUserLMRFlag) == 0)
+				 // v15.4:  force LMR allocation on ADDUSER, regardless of the
+				 // last USELMRS setting.  ppladduser() -> adduserrecord() does
+				 // fmemset(MsgPtr,0,MsgPtrLen) without a NULL check, so calling
+				 // it with TempMsgPtr==NULL (as happens after USELMRS FALSE +
+				 // GETALTUSER) crashes PCBoard.  A temp allocation of the LMR
+				 // buffer is cheap and only lives for the duration of ADDUSER.
+				 if(tempuseralloc(TRUE) == 0)
 				 {
 				   newusernum = ppladduser(tmpString.values.vSTRING);
 
@@ -1239,6 +1245,68 @@
 
 				break;
 
+			/*----------------------------------------------------------
+			 * 15.4 statement additions (see NEWSCR.HPP TOK_MOVEMSG,
+			 * TOK_SETBANKVAL, TOK_SHORTDESCSTMT).  Sematics inferred
+			 * from name + argcount + the recovered PCBOARDM.EXE 15.4b
+			 * MOVEMSG error strings ("Conference does not exist
+			 * (MOVEMSG)", "Message number does not exist (MOVEMSG)",
+			 * "Unable to read message body (MOVEMSG)", "Unable to
+			 * read message header (MOVEMSG)").
+			 *---------------------------------------------------------*/
+
+			case TOK_MOVEMSG:	// ... TOK_MOVEMSG EXPR EXPR EXPR ...
+				/* MOVEMSG conf, message, movetype  (per WHATSNEW 15.4).
+				 * arg1 = conference number (INTEGER)
+				 * arg2 = message number (DREAL — msgnums are 32-bit)
+				 * arg3 = movetype BOOLEAN (TRUE=move, FALSE=copy)
+				 *
+				 * Moves (or copies) the given message from the
+				 * specified conference base to the end of the
+				 * message base.  Errors printed at runtime match
+				 * the strings recovered from PCBOARDM.EXE 15.4b:
+				 *   "Conference does not exist (MOVEMSG)"
+				 *   "Message number does not exist (MOVEMSG)"
+				 *   "Unable to read message body (MOVEMSG)"
+				 *   "Unable to read message header (MOVEMSG)"
+				 *
+				 * Delegates to pplmovemsg() in the message-base I/O
+				 * layer.  For a PPL script the return code is
+				 * silently swallowed (the caller can check via a
+				 * subsequent read).
+				 */
+				EVAL(tmpInteger,r,nullErrBreak);
+				u1 = (unsigned) tmpInteger.values.vINTEGER; /* conf     */
+				EVAL(tmpInteger,r,nullErrBreak);
+				/* msgnum is DREAL — pass as long, truncating any fractional */
+				l1 = (long) tmpInteger.values.vDREAL;          /* msg num  */
+				EVAL(tmpInteger,r,nullErrBreak);
+				/* movetype: nonzero = move (delete source), 0 = copy */
+				pplmovemsg(u1, l1, (int)tmpInteger.values.vINTEGER); /*lint !e534 */
+				break;
+
+			case TOK_SETBANKVAL:  // ... TOK_SETBANKVAL EXPR EXPR ...
+				/* SETBANKVAL idx value — write Time Bank PSA field 0-11. */
+				EVAL(tmpInteger,r,nullErrBreak);
+				l1 = (long) tmpInteger.values.vINTEGER;   /* idx 0-11 */
+				EVAL(tmpInteger,r,nullErrBreak);
+				if (BankSupport && l1 >= 0 && l1 < 12) {
+				    long *bp = (long *) &Bank;
+				    bp[l1] = (long) tmpInteger.values.vDREAL;
+				}
+				break;
+
+			case TOK_SHORTDESCSTMT:  // ... TOK_SHORTDESCSTMT EXPR ...
+				/* arg1 = new value for the current user's
+				 *        ShortDesc byte.  Set at runtime; will
+				 *        be persisted next time the user's URead
+				 *        gets written (log off or explicit save).
+				 */
+				EVAL(tmpInteger,r,nullErrBreak);
+				if (ptrURead != NULL)
+				    ptrURead->ShortDesc = (char) tmpInteger.values.vINTEGER;
+				break;
+
 			case TOK_END:		 // ... TOK_END ...
 
 				breakFlag = TRUE;
@@ -2255,25 +2323,25 @@
 				i1 = int(tmpInteger.values.vINTEGER & 0x000000FF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.ax = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.ax = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.bx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.bx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.cx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.cx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.dx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.dx = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.si = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.si = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.di = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.di = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
-				intrREGS.x.flags = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
+				intrREGS.w.cflag = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
 
 				EVAL(tmpInteger,r,nullErrBreak);
 				intrSEGS.ds = unsigned(tmpInteger.values.vINTEGER & 0x0000FFFF);
@@ -2442,9 +2510,9 @@
 				err = unsupported("WRUSYSDOOR statement");
 #elif !defined(LIB)
 				EVAL(tmpString,r,nullErrBreak);
-				tmpString = tmpString.strip(cVARVAL(vtSTRING,"b "));
-				tmpString = tmpString.left(cVARVAL(vtINTEGER,8));
-				tmpString = tmpString.trim(cVARVAL(vtSTRING," "));
+				{ cVARVAL _s1(vtSTRING,"b "); tmpString = tmpString.strip(_s1); }
+				{ cVARVAL _s2(vtINTEGER,8); tmpString = tmpString.left(_s2); }
+				{ cVARVAL _s3(vtSTRING," "); tmpString = tmpString.trim(_s3); }
 				strcpy(Status.DoorName,nul2mty(tmpString));
 #endif
 
@@ -2972,9 +3040,9 @@
 	checkstack();
 	// Evaluate the keyword and store it in str1
 	EVAL(tmpString,r,nullErrReturn);
-	tmpString = tmpString.strip(cVARVAL(vtSTRING," "));
-	tmpString = tmpString.left(cVARVAL(vtINTEGER,8));
-	tmpString = tmpString.trim(cVARVAL(vtSTRING," "));
+	{ cVARVAL _t1(vtSTRING," "); tmpString = tmpString.strip(_t1); }
+	{ cVARVAL _t2(vtINTEGER,8); tmpString = tmpString.left(_t2); }
+	{ cVARVAL _t3(vtSTRING," "); tmpString = tmpString.trim(_t3); }
 	strcpy(keyword,nul2mty(tmpString));
 
 	// Get either the variable or expression
diff -ruN pcb153/SOURCE/PPL/VAR.CPP pcb153/upd154/SOURCE/PPL/VAR.CPP
--- pcb153/SOURCE/PPL/VAR.CPP	2026-09-11 00:24:27.932526356 +0000
+++ pcb153/upd154/SOURCE/PPL/VAR.CPP	2026-09-11 00:24:28.001318353 +0000
@@ -3232,7 +3232,7 @@
 
 cVARVAL LIBENTRY cVARVAL::stripstr(cVARVAL & cv)
 {
-    return replacestr(cv,cVARVAL(vtSTRING,""));
+    cVARVAL _empty(vtSTRING,""); return replacestr(cv,_empty);
     //return replace(cv,cVARVAL(vtSTRING,""));
 }
 
diff -ruN pcb153/SOURCE/SUPPORT/COPYFILE.C pcb153/upd154/SOURCE/SUPPORT/COPYFILE.C
--- pcb153/SOURCE/SUPPORT/COPYFILE.C	2026-09-11 00:24:27.935943943 +0000
+++ pcb153/upd154/SOURCE/SUPPORT/COPYFILE.C	2026-09-11 00:24:28.005159858 +0000
@@ -127,8 +127,8 @@
   #ifdef __BORLANDC__
     struct  ftime TimeDate;
   #else
-    uint    Time;
-    uint    Date;
+    unsigned Time;
+    unsigned Date;
   #endif
 
   NameLen = 0;
@@ -139,7 +139,7 @@
 
   #ifdef __BORLANDC__
     getftime(S,&TimeDate);
-  #else
+  #elif !defined(__OS2__)
     _dos_getftime(S,&Date,&Time);
   #endif
 
@@ -220,7 +220,7 @@
 
   #ifdef __BORLANDC__
     setftime(D,&TimeDate);
-  #else
+  #elif !defined(__OS2__)
     _dos_setftime(D,Date,Time);
   #endif
 
diff -ruN pcb153/SOURCE/SUPPORT/MEMORY.C pcb153/upd154/SOURCE/SUPPORT/MEMORY.C
--- pcb153/SOURCE/SUPPORT/MEMORY.C	2026-09-11 00:24:27.935943943 +0000
+++ pcb153/upd154/SOURCE/SUPPORT/MEMORY.C	2026-09-11 00:24:28.005159858 +0000
@@ -16,7 +16,7 @@
 #include "project.h"
 #pragma hdrstop
 
-/* #include <search.h> */
+// #include <search.h>
 
 #if defined(_MSC_VER) || defined(__WATCOMC__)
   #include <malloc.h>
diff -ruN pcb153/SOURCE/USERS/USERS.C pcb153/upd154/SOURCE/USERS/USERS.C
--- pcb153/SOURCE/USERS/USERS.C	2026-09-11 00:24:27.936988157 +0000
+++ pcb153/upd154/SOURCE/USERS/USERS.C	2026-09-11 00:24:28.005834877 +0000
@@ -83,9 +83,19 @@
 bool           QwkSupport;
 long           QwkOffset;
 qwkconfigtype  _FARDATA_ QwkConfig;
+
+/* 15.4: Personal PSA (Gender/Birthdate/Email/Web).  Layout in USERS.H. */
+bool             PersonalSupport;
+long             PersonalOffset;
+personal_psa_t   _FARDATA_ Personal;
+
+bool             BankSupport;
+long             BankOffset;
+timebank_psa_t   _FARDATA_ Bank;
 #endif
 
-typedef enum {PSA_NONE=0,PSA_ALIAS,PSA_VERIFY,PSA_ADDRESS,PSA_PASSWORD,PSA_STATS,PSA_NOTES,PSA_ACCOUNT,PSA_QWKNET,PSA_END} psatype;
+/* 15.4: PSA_PERSONAL added before PSA_END, mirroring PCBSM/USERS.C v0.020 */
+typedef enum {PSA_NONE=0,PSA_ALIAS,PSA_VERIFY,PSA_ADDRESS,PSA_PASSWORD,PSA_STATS,PSA_NOTES,PSA_ACCOUNT,PSA_QWKNET,PSA_PERSONAL,PSA_BANK,PSA_END} psatype;
 
 typedef struct {
   psatype Type;
@@ -281,6 +291,9 @@
 #ifdef PCB152
   if (AccountSupport)  storepsaelement(PSA_ACCOUNT, AccountOffset, &Account,   sizeof(accounttype));
   if (QwkSupport)      storepsaelement(PSA_QWKNET,  QwkOffset,     &QwkConfig, sizeof(qwkconfigtype));
+  /* 15.4: Personal PSA (Gender/Birthdate/Email/Web) */
+  if (PersonalSupport) storepsaelement(PSA_PERSONAL,PersonalOffset,&Personal,  sizeof(personal_psa_t));
+  if (BankSupport) storepsaelement(PSA_BANK,BankOffset,&Bank,sizeof(timebank_psa_t));
 #endif
 
   #ifdef DEBUG
@@ -1287,6 +1300,16 @@
             QwkSupport = TRUE;
             QwkOffset  = p->Offset;
           }
+          /* 15.4: PCBPERSONAL - Gender/Birthdate/Email/Web fields */
+          if (strcmp(&p->Name[3],"PERSONAL") == 0 && p->SizeOfRec == sizeof(personal_psa_t)) {
+            PersonalSupport = TRUE;
+            PersonalOffset  = p->Offset;
+          }
+          /* 15.4: PCBBANK - Time/Byte Bank */
+          if (strcmp(&p->Name[3],"BANK") == 0 && p->SizeOfRec == sizeof(timebank_psa_t)) {
+            BankSupport = TRUE;
+            BankOffset  = p->Offset;
+          }
 #endif
         }
       }
diff -ruN pcb153/SOURCE/USERS/USRMAINT.C pcb153/upd154/SOURCE/USERS/USRMAINT.C
--- pcb153/SOURCE/USERS/USRMAINT.C	2026-09-11 00:24:27.936988157 +0000
+++ pcb153/upd154/SOURCE/USERS/USRMAINT.C	2026-09-11 00:24:28.005834877 +0000
@@ -449,7 +449,7 @@
   getnewpassword(pud,FALSE);
 
   if (AskAll) {
-    Ctrl = (MixedCase ? HIGHASCII : HIGHASCII|UPCASE);
+    Ctrl = (displaytype)(MixedCase ? HIGHASCII : HIGHASCII|UPCASE);
     getfield(TXT_CITYSTATE         ,pud->City          ,sizeof(pud->City)          ,Ctrl            ,mask_alphanum);
     getfield(TXT_BUSDATAPHONE      ,pud->BusDataPhone  ,sizeof(pud->BusDataPhone)  ,HIGHASCII|UPCASE,(PcbData.DisableEdits ? mask_alphanum : (ModifySelf ? mask_phone1 : mask_phone2)));
     getfield(TXT_HOMEVOICEPHONE    ,pud->HomeVoicePhone,sizeof(pud->HomeVoicePhone),HIGHASCII|UPCASE,(PcbData.DisableEdits ? mask_alphanum : (ModifySelf ? mask_phone1 : mask_phone2)));
